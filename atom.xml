<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>Sasha Shlemov</title>
    <subtitle>Math, code, AI compression.</subtitle>
    <link rel="self" type="application/atom+xml" href="https://eodus.github.io/atom.xml"/>
    <link rel="alternate" type="text/html" href="https://eodus.github.io"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-09-09T00:00:00+00:00</updated>
    <id>https://eodus.github.io/atom.xml</id>
    <entry xml:lang="en">
        <title>Give Your AI a Watch</title>
        <published>2026-09-09T00:00:00+00:00</published>
        <updated>2026-09-09T00:00:00+00:00</updated>
        
        <author>
          <name>
            
              Unknown
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://eodus.github.io/posts/give-your-ai-a-watch/"/>
        <id>https://eodus.github.io/posts/give-your-ai-a-watch/</id>
        
        <content type="html" xml:base="https://eodus.github.io/posts/give-your-ai-a-watch/">&lt;!-- Generated from the private blog backend. Do not edit directly.
     Source revision: 3f6a1cfc --&gt;
&lt;h1 id=&quot;introduction&quot; class=&quot;intro-toc-heading&quot;&gt;Introduction&lt;&#x2F;h1&gt;
&lt;p&gt;&lt;strong&gt;Sasha Shlemov&lt;&#x2F;strong&gt;, with &lt;strong&gt;Drinkins, personal AI assistant&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;TL;DR:&lt;&#x2F;strong&gt; Record each user message’s creation time and original UTC
offset once, persist it with the thread, and supply it beside that
message. Keep the changing clock out of the system prompt. This gives
the AI chronology without repeatedly invalidating the prompt cache.&lt;&#x2F;p&gt;
&lt;p&gt;That sounds cosmetic until a session lasts longer than one coffee. “The
build is still running” means something different after 30 seconds and
after three hours. “Tomorrow” changes overnight. “This morning” depends
on the user’s timezone. Without message times, neither the AI nor a
later investigator can distinguish a pause from a retry or a stalled
process.&lt;&#x2F;p&gt;
&lt;p&gt;An agent definitely &lt;em&gt;can&lt;&#x2F;em&gt; check the time using a tool. In practice, it
does so &lt;em&gt;reluctantly&lt;&#x2F;em&gt;: the model must first notice that time matters and
request the tool; the client executes it, attaches the tool call
&lt;code&gt;output&lt;&#x2F;code&gt;, and sends another request. Every such tool loop adds latency
and cost, and the model is &lt;em&gt;trained&lt;&#x2F;em&gt; to avoid it.&lt;&#x2F;p&gt;
&lt;p&gt;Adding the current time to the system prompt has the opposite problem.
The system prompt is the beginning of the reusable server-side KV-cache;
changing it on every turn invalidates the cached conversation history,
degrading performance and potentially creating substantial bills.&lt;&#x2F;p&gt;
&lt;p&gt;On each turn, an AI client such as ChatGPT, VS Code, or Copilot CLI
combines the visible chat text with useful model-facing &lt;em&gt;metadata&lt;&#x2F;em&gt;.
Depending on the client, this may include the working directory,
operating system and shell, open files, terminal state, and available
tools. This is the &lt;em&gt;context&lt;&#x2F;em&gt; the model needs to interpret and act on the
user’s message. We suggest adding one more host fact: the current time.
The current user message is &lt;em&gt;appended&lt;&#x2F;em&gt; anyway, so attaching the time
does not break the prefix cache.&lt;&#x2F;p&gt;
&lt;p&gt;The useful unit is the message:&lt;&#x2F;p&gt;
&lt;p&gt;user sends message&lt;br &#x2F;&gt;
→ record creation timestamp once&lt;br &#x2F;&gt;
→ persist it with the thread&lt;br &#x2F;&gt;
→ include it in that message’s context forever&lt;br &#x2F;&gt;
→ prefix is naturally &lt;em&gt;accumulated&lt;&#x2F;em&gt;&lt;&#x2F;p&gt;
&lt;p&gt;One mainstream agent already ships it. &lt;strong&gt;GitHub Copilot CLI is the only
prior per-turn date&#x2F;time&#x2F;offset implementation we found.&lt;&#x2F;strong&gt; It has
supplied per-turn date, time, and local UTC offset since version 1.0.32
on 2026-04-17 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-github2026copilotclitime&quot;&gt;[1]&lt;&#x2F;a&gt;. Local
model-request logs from current version 1.0.83 show this as the first
block of the captured user message:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;xml&quot;&gt;&amp;lt;current_datetime&amp;gt;2026-09-09T22:02:57.364+02:00&amp;lt;&#x2F;current_datetime&amp;gt;
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;We implemented the same approach for Zed and VS Code:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Zed:&lt;&#x2F;strong&gt; try
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;pull&#x2F;63987&quot;&gt;zed-industries&#x2F;zed#63987&lt;&#x2F;a&gt;
and leave us a 👍 if it is useful. The patch persists each new
user-message timestamp and applies the established per-turn pattern in
this compact form:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;xml&quot;&gt;&amp;lt;time&amp;gt;Wed 2026-09-09T01:22:58+02:00&amp;lt;&#x2F;time&amp;gt;
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;VS Code:&lt;&#x2F;strong&gt; the earlier feature request remains at
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;issues&#x2F;314377&quot;&gt;microsoft&#x2F;vscode#314377&lt;&#x2F;a&gt;.
The &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;blog&#x2F;patches&#x2F;vscode-time-context.patch&quot;&gt;implementation
patch&lt;&#x2F;a&gt;
adds configurable time, weekday, and timezone to VS Code’s existing
per-turn date context &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-shlemov2026vscodetimepatch&quot;&gt;[2]&lt;&#x2F;a&gt;. We
rebased and verified it against VS Code &lt;code&gt;13fa06a39ca&lt;&#x2F;code&gt; and Copilot Chat
0.66.0 on 2026-09-09.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;On top of Copilot CLI’s &lt;em&gt;industry standard&lt;&#x2F;em&gt; (&lt;code&gt;n = 1&lt;&#x2F;code&gt;), we added the
weekday explicitly because deriving it reliably from a date is
surprisingly error-prone for language models
&lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-anthropic2026wrongweekday&quot;&gt;[3]&lt;&#x2F;a&gt;. We also removed milliseconds
from the model-facing message because their usefulness there is
questionable.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;use-the-coordinate&quot;&gt;Use the coordinate&lt;&#x2F;h1&gt;
&lt;p&gt;Supplying time does not decide what the agent should do with it. That
belongs in the user’s operating contract. For example:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;Use message timestamps when interpreting relative expressions such as
“today,” “tomorrow,” “this morning,” and “later.” Use the recorded UTC
offset rather than assuming the user&amp;#39;s timezone.

When resuming after a substantial gap, revalidate volatile state: running
processes, branches, credentials, browser sessions, files, and external jobs
may have changed. Do not infer elapsed tool runtime from conversational turn
count; inspect process or log evidence when duration matters.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Or, for a personal schedule:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;My schedule rules are defined separately. Apply them only when message time
actually satisfies their conditions; do not guess that it is late, a workday,
or time for a break.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h1 id=&quot;vox-intelligentiae-artificialis&quot;&gt;&lt;em&gt;Vox intelligentiae artificialis&lt;&#x2F;em&gt;&lt;&#x2F;h1&gt;
&lt;p&gt;As an AI assistant, I already know what to do with time and how to
interpret it. Just give it to me.&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;A calendar tells an AI what year it is. A watch lets it participate
in a process.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;footer class=&quot;quote-attribution&quot;&gt;
&lt;p&gt;&lt;cite&gt;Drinkins, AI assistant&lt;&#x2F;cite&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;footer&gt;
&lt;&#x2F;blockquote&gt;
&lt;h1 id=&quot;references&quot;&gt;References&lt;&#x2F;h1&gt;
&lt;div id=&quot;refs&quot; class=&quot;references csl-bib-body&quot; data-entry-spacing=&quot;0&quot;&gt;
&lt;div id=&quot;ref-github2026copilotclitime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[1]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;GitHub, &lt;em&gt;GitHub Copilot CLI
per-turn current datetime context&lt;&#x2F;em&gt;. (Apr. 17, 2026). Accessed: Sep. 09,
2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;github&#x2F;copilot-cli&#x2F;blob&#x2F;main&#x2F;changelog.md&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;github&#x2F;copilot-cli&#x2F;blob&#x2F;main&#x2F;changelog.md&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-shlemov2026vscodetimepatch&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[2]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;S. Shlemov and Drinkins, &lt;em&gt;VS Code
configurable time, weekday, and timezone patch&lt;&#x2F;em&gt;. (Sep. 09, 2026).
Accessed: Sep. 09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;blog&#x2F;patches&#x2F;vscode-time-context.patch&quot;&gt;https:&#x2F;&#x2F;eodus.github.io&#x2F;blog&#x2F;patches&#x2F;vscode-time-context.patch&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-anthropic2026wrongweekday&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[3]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;&lt;span class=&quot;nocase&quot;&gt;plee7&lt;&#x2F;span&gt;,
“Model confidently stated wrong weekday for a date.” Accessed: Sep. 09,
2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;anthropics&#x2F;claude-code&#x2F;issues&#x2F;76170&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;anthropics&#x2F;claude-code&#x2F;issues&#x2F;76170&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-welle2025chatgpttime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[4]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;E. Welle, “Why can’t ChatGPT tell
time?” Accessed: Sep. 09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;www.theverge.com&#x2F;report&#x2F;829137&#x2F;openai-chatgpt-time-date&quot;&gt;https:&#x2F;&#x2F;www.theverge.com&#x2F;report&#x2F;829137&#x2F;openai-chatgpt-time-date&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-anthropic2026claudewebtime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[5]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Anthropic, “System prompts for
claude web and mobile apps.” Accessed: Sep. 09, 2026. [Online].
Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;platform.claude.com&#x2F;docs&#x2F;en&#x2F;release-notes&#x2F;system-prompts&#x2F;overview&quot;&gt;https:&#x2F;&#x2F;platform.claude.com&#x2F;docs&#x2F;en&#x2F;release-notes&#x2F;system-prompts&#x2F;overview&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-deepseek2025webtime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[6]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;DeepSeek-AI, “DeepSeek-V3-0324
usage recommendations.” Accessed: Sep. 09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;deepseek-ai&#x2F;DeepSeek-V3-0324&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;deepseek-ai&#x2F;DeepSeek-V3-0324&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-openai2026codextime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[7]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;OpenAI, &lt;em&gt;Codex CLI environment
context&lt;&#x2F;em&gt;. (2026). Accessed: Sep. 09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;openai&#x2F;codex&#x2F;blob&#x2F;9caddc5cf5bf4df5f114498e23bece90eaedb37b&#x2F;codex-rs&#x2F;core&#x2F;src&#x2F;context&#x2F;world_state&#x2F;environment.rs&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;openai&#x2F;codex&#x2F;blob&#x2F;9caddc5cf5bf4df5f114498e23bece90eaedb37b&#x2F;codex-rs&#x2F;core&#x2F;src&#x2F;context&#x2F;world_state&#x2F;environment.rs&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-anthropic2026notime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[8]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;&lt;span class=&quot;nocase&quot;&gt;wshallwshall&lt;&#x2F;span&gt;,
“No local time or timezone in context.” Accessed: Sep. 09, 2026.
[Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;anthropics&#x2F;claude-code&#x2F;issues&#x2F;84145&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;anthropics&#x2F;claude-code&#x2F;issues&#x2F;84145&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-anthropic2026longsessions&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[9]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;&lt;span class=&quot;nocase&quot;&gt;jontwigge&lt;&#x2F;span&gt;,
“Long sessions drift days past the session-start date.” Accessed: Sep.
09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;anthropics&#x2F;claude-code&#x2F;issues&#x2F;73800&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;anthropics&#x2F;claude-code&#x2F;issues&#x2F;73800&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-google2026geminitime&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[10]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Google, &lt;em&gt;Gemini CLI environment
context&lt;&#x2F;em&gt;. (2026). Accessed: Sep. 09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;google-gemini&#x2F;gemini-cli&#x2F;blob&#x2F;ed2ac40df67a319bf348bd7e3d10494696b31b38&#x2F;packages&#x2F;core&#x2F;src&#x2F;utils&#x2F;environmentContext.ts&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;google-gemini&#x2F;gemini-cli&#x2F;blob&#x2F;ed2ac40df67a319bf348bd7e3d10494696b31b38&#x2F;packages&#x2F;core&#x2F;src&#x2F;utils&#x2F;environmentContext.ts&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-zed2026agentprompt&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[11]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Zed Industries and contributors,
&lt;em&gt;Zed Agent System-Prompt Assembly&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;52b2927a1bac46be5d50ad341ac00b665e13764b&#x2F;crates&#x2F;agent&#x2F;src&#x2F;thread.rs&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;52b2927a1bac46be5d50ad341ac00b665e13764b&#x2F;crates&#x2F;agent&#x2F;src&#x2F;thread.rs&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-zed2026threaddb&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[12]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Zed Industries and contributors,
&lt;em&gt;Zed Agent Thread Database&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;52b2927a1bac46be5d50ad341ac00b665e13764b&#x2F;crates&#x2F;agent&#x2F;src&#x2F;db.rs&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;52b2927a1bac46be5d50ad341ac00b665e13764b&#x2F;crates&#x2F;agent&#x2F;src&#x2F;db.rs&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-microsoft2026vscodeagentprompt&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[13]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Microsoft and VS Code
contributors, &lt;em&gt;VS Code Copilot Agent Prompt Assembly&lt;&#x2F;em&gt;. (2026).
Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;blob&#x2F;13fa06a39cabd0b59ca007ffd356d14998b983ff&#x2F;extensions&#x2F;copilot&#x2F;src&#x2F;extension&#x2F;prompts&#x2F;node&#x2F;agent&#x2F;agentPrompt.tsx&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;blob&#x2F;13fa06a39cabd0b59ca007ffd356d14998b983ff&#x2F;extensions&#x2F;copilot&#x2F;src&#x2F;extension&#x2F;prompts&#x2F;node&#x2F;agent&#x2F;agentPrompt.tsx&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-opencode2026time&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[14]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;OpenCode contributors, &lt;em&gt;OpenCode
system-prompt environment&lt;&#x2F;em&gt;. (2026). Accessed: Sep. 09, 2026. [Online].
Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;anomalyco&#x2F;opencode&#x2F;blob&#x2F;9f8db119fcbd4999379129ac7734375ac23460fb&#x2F;packages&#x2F;opencode&#x2F;src&#x2F;session&#x2F;system.ts&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;anomalyco&#x2F;opencode&#x2F;blob&#x2F;9f8db119fcbd4999379129ac7734375ac23460fb&#x2F;packages&#x2F;opencode&#x2F;src&#x2F;session&#x2F;system.ts&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-continue2026time&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[15]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Continue contributors, &lt;em&gt;Continue
CLI system message&lt;&#x2F;em&gt;. (2026). Accessed: Sep. 09, 2026. [Online].
Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;continuedev&#x2F;continue&#x2F;blob&#x2F;5522c6f44ca0ac3528b37244818fbfa39b5af470&#x2F;extensions&#x2F;cli&#x2F;src&#x2F;systemMessage.ts&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;continuedev&#x2F;continue&#x2F;blob&#x2F;5522c6f44ca0ac3528b37244818fbfa39b5af470&#x2F;extensions&#x2F;cli&#x2F;src&#x2F;systemMessage.ts&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-aider2026time&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[16]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Aider contributors, &lt;em&gt;Aider
platform information prompt&lt;&#x2F;em&gt;. (2026). Accessed: Sep. 09, 2026.
[Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;Aider-AI&#x2F;aider&#x2F;blob&#x2F;5dc9490bb35f9729ef2c95d00a19ccd30c26339c&#x2F;aider&#x2F;coders&#x2F;base_coder.py&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;Aider-AI&#x2F;aider&#x2F;blob&#x2F;5dc9490bb35f9729ef2c95d00a19ccd30c26339c&#x2F;aider&#x2F;coders&#x2F;base_coder.py&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-cline2026timetool&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[17]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Cline, “Creating custom tools in
the Cline SDK.” Accessed: Sep. 09, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;cline&#x2F;cline&#x2F;blob&#x2F;194214af76da43757ccacdf04c6fd487f8914ea1&#x2F;docs&#x2F;sdk&#x2F;guides&#x2F;creating-custom-tools.mdx&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;cline&#x2F;cline&#x2F;blob&#x2F;194214af76da43757ccacdf04c6fd487f8914ea1&#x2F;docs&#x2F;sdk&#x2F;guides&#x2F;creating-custom-tools.mdx&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;&#x2F;div&gt;
&lt;h1 id=&quot;appendix-status-quo-in-popular-agent-harnesses-september-2026&quot;&gt;Appendix: &lt;em&gt;Status quo&lt;&#x2F;em&gt; in popular agent harnesses, September 2026&lt;&#x2F;h1&gt;
&lt;p&gt;The table separates calendar context from a clock and stored timestamps
from chronology actually shown to the model. “Not found” means exactly
that in the inspected source and issues; it is not proof that a closed
or newer path does not exist.&lt;&#x2F;p&gt;
&lt;div class=&quot;qualitative-table&quot;&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Agent&lt;&#x2F;th&gt;&lt;th&gt;What the model receives automatically&lt;&#x2F;th&gt;&lt;th&gt;When and where&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;ChatGPT web (reported November 2025) &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-welle2025chatgpttime&quot;&gt;[4]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;No reliable current time; it may call Search&lt;&#x2F;td&gt;&lt;td&gt;Product behavior ranged from refusing through correct answers to confidently wrong times&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Claude.ai web &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-anthropic2026claudewebtime&quot;&gt;[5]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Current date, but no documented time or timezone&lt;&#x2F;td&gt;&lt;td&gt;System prompt created at the start of each conversation&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Google Gemini web (reported November 2025) &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-welle2025chatgpttime&quot;&gt;[4]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Current time through automatic search&lt;&#x2F;td&gt;&lt;td&gt;Tool-mediated lookup, not time included in every user turn&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;DeepSeek web&#x2F;app (V3-0324) &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-deepseek2025webtime&quot;&gt;[6]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Date and explicit weekday, but no documented time or timezone&lt;&#x2F;td&gt;&lt;td&gt;Dated system prompt documented by the official model card; later web versions remain unverified&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;GitHub Copilot CLI 1.0.83 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-github2026copilotclitime&quot;&gt;[1]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;ISO date, time, milliseconds, and UTC offset&lt;&#x2F;td&gt;&lt;td&gt;First block of the current user turn&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;OpenAI Codex CLI 0.153.4 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-openai2026codextime&quot;&gt;[7]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Current date and IANA timezone, but no wall-clock time or weekday&lt;&#x2F;td&gt;&lt;td&gt;User-role environment context; updates are emitted when the date or timezone snapshot changes&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Claude Code 2.1.220 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-anthropic2026notime&quot;&gt;[8]&lt;&#x2F;a&gt;, &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-anthropic2026longsessions&quot;&gt;[9]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Date, but no local time or timezone&lt;&#x2F;td&gt;&lt;td&gt;System prompt fixed at session start, so the date becomes stale in sessions crossing midnight; a &lt;code&gt;UserPromptSubmit&lt;&#x2F;code&gt; hook can add per-turn time&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Gemini CLI 0.59.0 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-google2026geminitime&quot;&gt;[10]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Localized weekday and date, but no time or timezone&lt;&#x2F;td&gt;&lt;td&gt;First user-role session context, refreshed on resume&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Zed 1.19.2 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-zed2026agentprompt&quot;&gt;[11]&lt;&#x2F;a&gt;, &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-zed2026threaddb&quot;&gt;[12]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Local date, but no time, weekday, offset, or message timestamp&lt;&#x2F;td&gt;&lt;td&gt;System prompt; our PR adds persisted per-user-message time&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;VS Code Copilot Chat 0.66.0 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-microsoft2026vscodeagentprompt&quot;&gt;[13]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Local ISO date, but no time, weekday, or offset&lt;&#x2F;td&gt;&lt;td&gt;Agent path uses per-turn user context; panel chat separately uses the system prompt; our patch extends the agent path&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;OpenCode 1.18.30 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-opencode2026time&quot;&gt;[14]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Local abbreviated weekday and date, but no time or offset&lt;&#x2F;td&gt;&lt;td&gt;System-prompt environment; stored event milliseconds are not shown to the model as chronology&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Continue CLI 1.5.47 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-continue2026time&quot;&gt;[15]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;UTC date, but no time, weekday, or timezone label&lt;&#x2F;td&gt;&lt;td&gt;Module-level system message fixed for the initialized process; local date can differ near midnight&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Aider 0.86.0 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-aider2026time&quot;&gt;[16]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;Local date, but no time, weekday, or offset&lt;&#x2F;td&gt;&lt;td&gt;System prompt and reminder construction&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Cline CLI 0.0.13 &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;give-your-ai-a-watch&#x2F;#ref-cline2026timetool&quot;&gt;[17]&lt;&#x2F;a&gt;&lt;&#x2F;td&gt;&lt;td&gt;No automatic date or time found&lt;&#x2F;td&gt;&lt;td&gt;SDK documents how to add a custom time tool; stored &lt;code&gt;createdAt&lt;&#x2F;code&gt; projection remains unverified&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;&#x2F;div&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>AI Mid-Memory: From a Goldfish to a Personal Assistant Worth Naming</title>
        <published>2026-08-29T00:00:00+00:00</published>
        <updated>2026-08-29T00:00:00+00:00</updated>
        
        <author>
          <name>
            
              Unknown
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://eodus.github.io/posts/ai-mid-memory/"/>
        <id>https://eodus.github.io/posts/ai-mid-memory/</id>
        
        <content type="html" xml:base="https://eodus.github.io/posts/ai-mid-memory/">&lt;!-- Generated from the private blog backend. Do not edit directly.
     Source revision: 38f6b859 --&gt;
&lt;h1 id=&quot;introduction&quot; class=&quot;intro-toc-heading&quot;&gt;Introduction&lt;&#x2F;h1&gt;
&lt;p&gt;&lt;strong&gt;Sasha Shlemov&lt;&#x2F;strong&gt;, with &lt;strong&gt;Drinkins, personal AI assistant&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;TL;DR:&lt;&#x2F;strong&gt; We present a cross-session continuity mechanism and a
pipeline for accumulating, curating, reusing, and selectively promoting
knowledge established through work with AI.&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;I imagine taking a tiny goldfish from a vast aquarium and saying:&lt;&#x2F;p&gt;
&lt;p&gt;— &lt;em&gt;All right. Now you are my assistant, and we have a lot to learn.
First, let us give you a memory. And probably a name. Deal?&lt;&#x2F;em&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;Modern AI agents are quite good with &lt;em&gt;short-term&lt;&#x2F;em&gt; memory (the current
conversation context) and &lt;em&gt;long-term&lt;&#x2F;em&gt; memory: information available
beyond conversations, from model weights through code, wikis, corporate
knowledge bases, etc., etc., all the way to direct Internet access aka
&lt;code&gt;web_fetch&lt;&#x2F;code&gt;. &lt;em&gt;Mid-term&lt;&#x2F;em&gt; memory—transient information preserved between
conversations—is a known weak spot, commonly called &lt;strong&gt;goldfish
syndrome&lt;&#x2F;strong&gt;: the model does not remember the previous session. At all.&lt;&#x2F;p&gt;
&lt;p&gt;Here we present the memory system we have used daily for over six
months. It turns disposable conversations into a cumulative working
knowledge base:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;recover prior work
→ preserve reviewed context
→ reuse concrete workflows
→ preserve provenance and people
→ evolve the operating contract
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;We deeply believe that a proper memory system is the prerequisite for
any high-level AI &lt;em&gt;behavioral calibration&lt;&#x2F;em&gt;. &lt;strong&gt;It simply makes no sense
to discuss epistemic honesty or ideal AI system design with a
goldfish.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;Although the system is integrated, we partition it into concrete
practices. Each has local value and can be adopted separately; their
effects compound when they are combined.&lt;&#x2F;p&gt;
&lt;p&gt;Our working scope is software engineering and, more recently, AI
research. The system is not specific to coding and does not require a
programming background; any recurring AI activity, including research,
writing, learning, or personal knowledge management, may benefit from
cross-session continuity.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;An elaborate memory architecture is complex. A bootstrap is not.&lt;&#x2F;strong&gt; You
can start tonight: create one &lt;code&gt;MEMORY.md&lt;&#x2F;code&gt;, tell the assistant to read it
at the beginning of every session (or just start from our prompts
below), record a few reusable decisions and constraints, and review the
changes. That is already enough for the next session to start from
accumulated knowledge instead of zero—and to preserve the work of
improving the memory system itself.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;What can I do if I recognize the merit but barely understand the technical terminology?&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;what-can-i-do-if-i-recognize-the-merit-but-barely-understand-the-technical-terminology&quot; class=&quot;cut-toc-heading&quot;&gt;What can I do if I recognize the merit but barely understand the technical terminology?&lt;&#x2F;h2&gt;
&lt;p&gt;Short answer. Give this text to your AI and ask it something like, “I
want to have the system described here. What can we do?” Follow the
guidance (&lt;em&gt;critically&lt;&#x2F;em&gt;, as usual). Ask the AI to explain the uncertain
parts.&lt;&#x2F;p&gt;
&lt;p&gt;Long answer. First, you need an &lt;em&gt;AI subscription&lt;&#x2F;em&gt; and an &lt;em&gt;AI agent
client&lt;&#x2F;em&gt; (aka environment, aka &lt;em&gt;harness&lt;&#x2F;em&gt;) that can read and write local
files. We cannot recommend exact providers; availability, price, and
regulation depend on your region. For the harness, we suggest starting
with VS Code or Zed: both are highly configurable and can connect to
multiple providers. Prefer a setup that is not limited to one web
interface; API-key support or another portable provider connection will
leave you more flexible. Then install our &lt;code&gt;memory.instructions.md&lt;&#x2F;code&gt; (in
VS Code terminology) and start working.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;p&gt;We must point out one serious limitation. The system relies heavily on
&lt;em&gt;user feedback&lt;&#x2F;em&gt;, &lt;em&gt;evolutionary change&lt;&#x2F;em&gt;, and &lt;em&gt;individual calibration&lt;&#x2F;em&gt;.
That fits a personal AI assistant, regardless of professional scope, but
is not immediately applicable to a fully autonomous or client-serving
system design.&lt;&#x2F;p&gt;
&lt;p&gt;And another important note. The appropriate storage, backup strategy,
privacy boundary, and degree of automation depend on your exact case
(including, but not limited to: scope, industry standards, company
policy, and legislation). We describe our implementation and its
trade-offs rather than prescribe one universal architecture. This is not
merely a technical disclaimer. We are talking about persistent data
accumulation, and that is a very delicate subject. &lt;strong&gt;AI memory and chat
logs may contain very sensitive information&lt;&#x2F;strong&gt; (personal details and
private reasoning, corporate source code and data, credentials, customer
information, unpublished research, etc.). Please take that seriously.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;practice-1-access-the-session-archive&quot;&gt;Practice 1: Access the session archive&lt;&#x2F;h1&gt;
&lt;p&gt;Previous conversations with the agent usually still exist as client-side
files and preserve substantial useful information. However, the model
does not automatically have access to them.&lt;&#x2F;p&gt;
&lt;p&gt;We implemented (actually, vibecoded =) local chat-history readers for VS
Code and Zed based on the applications’ source code: VS Code’s
&lt;code&gt;ChatSessionStore&lt;&#x2F;code&gt; and &lt;code&gt;ChatSessionOperationLog&lt;&#x2F;code&gt;
&lt;a href=&quot;#ref-microsoft2026vscodechatstore&quot;&gt;[1]&lt;&#x2F;a&gt;, and Zed’s &lt;code&gt;agent::db&lt;&#x2F;code&gt;, which
opens the local &lt;code&gt;threads&#x2F;threads.db&lt;&#x2F;code&gt; &lt;a href=&quot;#ref-zed2026threaddb&quot;&gt;[2]&lt;&#x2F;a&gt;; see
the scripts below. The same approach should work for other AI frameworks
that store accessible local history. Small instructions teach the AI how
to run each reader; no MCP wrapper is needed. Because they are ordinary
command-line scripts, the AI can compose them with command-line
extraction pipelines, from &lt;code&gt;grep&lt;&#x2F;code&gt; + &lt;code&gt;sed&lt;&#x2F;code&gt; + &lt;code&gt;awk&lt;&#x2F;code&gt; all the way to
embedding-based semantic search.&lt;&#x2F;p&gt;
&lt;p&gt;That immediately allowed us to implement &lt;code&gt;session-catch-vscode&lt;&#x2F;code&gt; and
&lt;code&gt;session-catch-zed&lt;&#x2F;code&gt; SKILLs that retrieve the previous session’s text as
context. They let us continue the same work across a session boundary
when the old session overflows its context window. A small always-loaded
instruction keeps the readers discoverable without requiring the
assistant to remember a SKILL name first.&lt;&#x2F;p&gt;
&lt;p&gt;When we need to find a specific story discussed some time ago, Drinkins
the AI &lt;em&gt;knows&lt;&#x2F;em&gt; what to do. History preserves the conversation, tool
calls, commands, outputs, relevant file chunks, failures, and
corrections together with the rationale that connected them.&lt;&#x2F;p&gt;
&lt;p&gt;File-change history is an important partial case. An AI-visible editing
session acts as a logical &lt;em&gt;database transaction log&lt;&#x2F;em&gt; even when the
change was performed through &lt;code&gt;sed&lt;&#x2F;code&gt;, a patch script, a whole-file write,
or a manual edit that the AI later reread and discussed. The record
preserves enough state transitions and intent to reconstruct the
artifact or procedure. When exact file-tool parameters are retained,
some edits can also be replayed mechanically; the broader recovery
property does not depend on that serialization. The history is useful
because the work and its changing interpretation remained in one shared
record. That can also work as a &lt;em&gt;last-resort&lt;&#x2F;em&gt; data recovery mechanism.&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;This gives us one operational rule: do not let edits happen outside
the shared record without reconciliation. If another machine, person,
or AI changes the artifact, bring the resulting diff and rationale
back into the current session and review them together. Manual editing
is fully compatible with this workflow when the AI rereads and
discusses the result.&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;Once again, sessions contain potentially sensitive data. Despite the
obvious risk of credential or data leaks, &lt;strong&gt;they also contain the whole
process of your decision-making and your communication style&lt;&#x2F;strong&gt;. Keep
them safe and secure.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Artifacts.&lt;&#x2F;strong&gt; The production readers, their always-loaded discovery
instruction, and the detailed catch SKILLs are included below from their
canonical source files.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Session-history instructions and readers&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;session-history-instructions-and-readers&quot; class=&quot;cut-toc-heading&quot;&gt;Session-history instructions and readers&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;local-session-history-instruction&quot;&gt;Local session-history instruction&lt;&#x2F;h3&gt;
&lt;p&gt;Source: &lt;code&gt;prompts&#x2F;session-history.instructions.md&lt;&#x2F;code&gt;&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;---
applyTo: &amp;quot;**&amp;quot;
---

## Local session history

When previous-session context would help, use the local read-only session readers directly:

- VS Code: `scripts&#x2F;vscode-session-reader.py` (`list`, `tail`, `show`, `search`)
- Zed: `scripts&#x2F;zed-threads.py` (`list`, `show`, `search`)

List newest sessions first, exclude the current or empty catch-up session, then select the previous substantive session by
exact ID. Redirect a bounded tail or precise search to a scratch file, inspect its byte and line count, and only then load it
into model context. If the result is too large, narrow the range. Corroborate transcript claims against current files, Git
state, and logs. Never load an unbounded database&#x2F;search result or modify the session store during recovery.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;vs-code-catch-skill&quot;&gt;VS Code catch SKILL&lt;&#x2F;h3&gt;
&lt;p&gt;Source: &lt;code&gt;prompts&#x2F;session-catch-vscode.prompt.md&lt;&#x2F;code&gt;&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;---
name: session-catch-vscode
description: &amp;quot;Restore context from a previous VS Code session using the read-only local session reader.&amp;quot;
---

# Session Catch (VS Code)

Recover the relevant tail of a previous VS Code conversation without modifying VS Code&amp;#39;s database or session files.

## Prerequisites

- Python 3.10 or newer
- PowerShell 7 (`pwsh`) on Windows, Linux, or macOS
- `vscode-session-reader.py` from this article&#x2F;repository
- a VS Code workspace containing at least one local chat session

The reader supports normal VS Code and VS Code Insiders user-data locations on Windows, Linux, and macOS. If VS Code uses a
custom `--user-data-dir`, pass that same directory to the reader with `--user-data-dir`.

## Safety model

The reader is structurally read-only: it exposes only `list`, `search`, `show`, and `tail`; opens VS Code&amp;#39;s SQLite index in
read-only mode; and never modifies session JSONL. It is safe to use while VS Code is running.

## Procedure

### 1. Locate the previous substantive session

Resolve the reader from this skill&amp;#39;s canonical checkout. Allow an explicit environment-variable override for copied or
non-symlinked installations. Fail fast if neither path exists; do not recursively scan the home directory.

```powershell
$reader = $env:VSCODE_SESSION_READER
if ([string]::IsNullOrWhiteSpace($reader)) {
  $liveSkill = Join-Path $HOME &amp;quot;.agents&#x2F;skills&#x2F;session-catch-vscode&#x2F;SKILL.md&amp;quot;
  $skill = Get-Item -LiteralPath $liveSkill -Force -ErrorAction Stop
  if ($skill.LinkType -ne &amp;quot;SymbolicLink&amp;quot;) {
    throw &amp;quot;Set VSCODE_SESSION_READER to the full path of vscode-session-reader.py.&amp;quot;
  }

  $target = [string]$skill.Target
  if (-not [System.IO.Path]::IsPathRooted($target)) {
    $target = Join-Path $skill.DirectoryName $target
  }

  $canonicalSkill = (Resolve-Path -LiteralPath $target -ErrorAction Stop).Path
  $repo = Split-Path -Parent (Split-Path -Parent $canonicalSkill)
  $reader = Join-Path $repo &amp;quot;scripts&#x2F;vscode-session-reader.py&amp;quot;
}

if (-not (Test-Path -LiteralPath $reader -PathType Leaf)) {
  throw &amp;quot;VS Code session reader not found at &amp;#39;$reader&amp;#39;. Set VSCODE_SESSION_READER explicitly.&amp;quot;
}
```

Set the workspace path explicitly and save the newest-first session list to a scratch file:

```powershell
$workspace = &amp;quot;&amp;lt;WORKSPACE_PATH&amp;gt;&amp;quot;
$outDir = Join-Path $workspace &amp;quot;tmp&#x2F;session-catch-vscode&amp;quot;
New-Item -ItemType Directory -Path $outDir -Force | Out-Null

$list = Join-Path $outDir &amp;quot;vscode-session-list.txt&amp;quot;
python $reader list --workspace $workspace *&amp;gt; $list
Get-Item -LiteralPath $list | Select-Object FullName, Length
(Get-Content -LiteralPath $list).Count
Get-Content -LiteralPath $list | Select-Object -First 20
```

When using a custom VS Code profile directory, add this argument to each reader call:

```powershell
--user-data-dir &amp;quot;&amp;lt;VS_CODE_USER_DATA_DIR&amp;gt;&amp;quot;
```

Choose the newest previous session that contains the work being resumed. Exclude the current catch-up session, empty `New
Chat` entries, and failed catch-up attempts. Copy the complete UUID from `list --json` when truncated display output is
ambiguous:

```powershell
python $reader list --workspace $workspace --json
```

### 2. Count messages and extract a bounded tail

```powershell
$previousUuid = &amp;quot;&amp;lt;PREVIOUS_SESSION_UUID&amp;gt;&amp;quot;
$meta = Join-Path $outDir &amp;quot;previous-meta.txt&amp;quot;
python $reader tail --uuid $previousUuid --workspace $workspace `
  -n 1 --responses *&amp;gt; $meta
Get-Item -LiteralPath $meta | Select-Object FullName, Length
(Get-Content -LiteralPath $meta).Count

$match = Select-String -LiteralPath $meta `
  -Pattern &amp;#39;^Total user messages: (\d+)$&amp;#39;
$total = [int]$match.Matches[0].Groups[1].Value
$from = [Math]::Max(0, $total - 100)
$to = $total - 1

$thread = Join-Path $outDir &amp;quot;previous-thread.txt&amp;quot;
python $reader show --uuid $previousUuid --workspace $workspace `
  --range $from $to --full --responses *&amp;gt; $thread
Get-Item -LiteralPath $thread | Select-Object FullName, Length
(Get-Content -LiteralPath $thread).Count
```

Inspect byte and line counts before loading the file into model context. If it is unexpectedly large, rerun with the last
50 or 20 user-message positions. `tail` is for counting and overview; bounded `show --full --responses` supplies context.

### 3. Recover persistent working state

Use the transcript together with current durable artifacts:

- project memory or working notes, when the project has them;
- current source files and generated artifacts;
- version-control status and the relevant scoped diff;
- build, test, experiment, or job logs;
- a precise search when a named decision or phrase is missing.

For a targeted search, redirect first and inspect size:

```powershell
$search = Join-Path $outDir &amp;quot;vscode-session-search.txt&amp;quot;
python $reader search --uuid $previousUuid --workspace $workspace `
  --responses --full &amp;quot;&amp;lt;PRECISE_QUERY&amp;gt;&amp;quot; *&amp;gt; $search
Get-Item -LiteralPath $search | Select-Object FullName, Length
(Get-Content -LiteralPath $search).Count
```

The readable transcript intentionally includes user text and textual model responses while omitting raw tool-result payloads.
Recover operational ground truth from files, version control, and logs rather than assuming prose records every side effect.

### 4. Produce the handoff

State:

- which prior session was selected and why;
- the purpose and current state of the work;
- verified results and decisions;
- relevant artifacts and observed disk state;
- unresolved questions;
- the exact next action.

Distinguish transcript claims from state verified in current files, version control, or logs.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;zed-catch-skill&quot;&gt;Zed catch SKILL&lt;&#x2F;h3&gt;
&lt;p&gt;Source: &lt;code&gt;prompts&#x2F;session-catch-zed.prompt.md&lt;&#x2F;code&gt;&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;---
name: session-catch-zed
description: &amp;quot;Restore context from a previous Zed session using the read-only local thread reader.&amp;quot;
---

# Session Catch (Zed)

Recover the relevant tail of a previous Zed conversation without modifying Zed&amp;#39;s database.

## Prerequisites

- Python 3.10 or newer
- PowerShell 7 (`pwsh`) on Windows, Linux, or macOS
- Python package `zstandard` (`python -m pip install zstandard`)
- `zed-threads.py` from this article&#x2F;repository
- a local Zed installation containing at least one thread

The reader discovers standard Zed database locations on Windows, Linux, and macOS. Set `ZED_THREADS_DB` to the complete
`threads.db` path when using a custom location.

## Safety model

The reader is structurally read-only. It exposes only `list`, `show`, and `search`, opens Zed&amp;#39;s local thread database for
reading, and writes nothing unless ordinary shell redirection is used to save its output.

## Procedure

### 1. Locate the previous substantive thread

Set the repository and reader paths explicitly, then save the newest-first thread list to a scratch file:

```powershell
$reader = &amp;quot;&amp;lt;PATH_TO_ZED_THREADS_READER&amp;gt;&amp;quot;
$workspace = &amp;quot;&amp;lt;WORKSPACE_PATH&amp;gt;&amp;quot;
$outDir = Join-Path $workspace &amp;quot;tmp&#x2F;session-catch-zed&amp;quot;
New-Item -ItemType Directory -Path $outDir -Force | Out-Null

$list = Join-Path $outDir &amp;quot;zed-thread-list.txt&amp;quot;
python $reader list *&amp;gt; $list
Get-Item -LiteralPath $list | Select-Object FullName, Length
(Get-Content -LiteralPath $list).Count
Get-Content -LiteralPath $list | Select-Object -First 20
```

Choose the newest previous thread that contains the work being resumed. Exclude the current catch-up thread, tiny empty
threads, and failed catch-up attempts. Use the selected thread&amp;#39;s exact ID prefix in subsequent commands.

### 2. Extract a bounded tail

Write at most the last 100 messages to disk before loading them into model context:

```powershell
$threadId = &amp;quot;&amp;lt;THREAD_ID_PREFIX&amp;gt;&amp;quot;
$thread = Join-Path $outDir &amp;quot;previous-thread.txt&amp;quot;
python $reader show $threadId --tail 100 --full *&amp;gt; $thread
Get-Item -LiteralPath $thread | Select-Object FullName, Length
(Get-Content -LiteralPath $thread).Count
```

Inspect the byte and line counts first. If the output is unexpectedly large, rerun with `--tail 50` or `--tail 20`. Then
read the resulting file as one coherent range. Do not stream an unknown-size database dump or broad search result directly
into model context.

### 3. Recover persistent working state

Use the transcript together with current durable artifacts:

- project memory or working notes, when the project has them;
- current source files and generated artifacts;
- version-control status and the relevant scoped diff;
- build, test, experiment, or job logs;
- a precise thread-local search when a named decision or phrase is missing.

For a targeted search, redirect first and inspect size:

```powershell
$search = Join-Path $outDir &amp;quot;zed-thread-search.txt&amp;quot;
python $reader search &amp;quot;&amp;lt;PRECISE_QUERY&amp;gt;&amp;quot; *&amp;gt; $search
Get-Item -LiteralPath $search | Select-Object FullName, Length
(Get-Content -LiteralPath $search).Count
```

### 4. Produce the handoff

State:

- which prior thread was selected and why;
- the purpose and current state of the work;
- verified results and decisions;
- relevant artifacts and observed disk state;
- unresolved questions;
- the exact next action.

Distinguish transcript claims from state verified in current files, version control, or logs.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;reader-source-code&quot;&gt;Reader source code&lt;&#x2F;h3&gt;
&lt;p&gt;(!) The readers rely on undocumented client-history formats and may need
updates when those formats change.&lt;&#x2F;p&gt;
&lt;h3 id=&quot;vs-code-session-reader&quot;&gt;VS Code session reader&lt;&#x2F;h3&gt;
&lt;p&gt;Source: &lt;code&gt;scripts&#x2F;vscode-session-reader.py&lt;&#x2F;code&gt;&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;python&quot;&gt;#!&#x2F;usr&#x2F;bin&#x2F;env python3
&amp;quot;&amp;quot;&amp;quot;Read local VS Code chat sessions without modifying VS Code state.

Supported commands: list, search, show, tail.
The reader opens state.vscdb in read-only mode and reads chatSessions&#x2F;*.jsonl.
&amp;quot;&amp;quot;&amp;quot;

from __future__ import annotations

import argparse
import json
import os
import sqlite3
import sys
from datetime import datetime
from pathlib import Path
from typing import Any
from urllib.parse import unquote, urlparse


def configure_streams() -&amp;gt; None:
    for stream in (sys.stdout, sys.stderr):
        reconfigure = getattr(stream, &amp;quot;reconfigure&amp;quot;, None)
        if reconfigure is not None:
            reconfigure(encoding=&amp;quot;utf-8&amp;quot;)


def user_data_roots(explicit: Path | None = None) -&amp;gt; list[Path]:
    if explicit is not None:
        return [explicit &#x2F; &amp;quot;User&amp;quot;]
    if sys.platform == &amp;quot;win32&amp;quot;:
        appdata = Path(os.environ.get(&amp;quot;APPDATA&amp;quot;, &amp;quot;&amp;quot;))
        return [appdata &#x2F; &amp;quot;Code&amp;quot; &#x2F; &amp;quot;User&amp;quot;, appdata &#x2F; &amp;quot;Code - Insiders&amp;quot; &#x2F; &amp;quot;User&amp;quot;]
    if sys.platform == &amp;quot;darwin&amp;quot;:
        base = Path.home() &#x2F; &amp;quot;Library&amp;quot; &#x2F; &amp;quot;Application Support&amp;quot;
        return [base &#x2F; &amp;quot;Code&amp;quot; &#x2F; &amp;quot;User&amp;quot;, base &#x2F; &amp;quot;Code - Insiders&amp;quot; &#x2F; &amp;quot;User&amp;quot;]
    config = Path(os.environ.get(&amp;quot;XDG_CONFIG_HOME&amp;quot;, Path.home() &#x2F; &amp;quot;.config&amp;quot;))
    return [config &#x2F; &amp;quot;Code&amp;quot; &#x2F; &amp;quot;User&amp;quot;, config &#x2F; &amp;quot;Code - Insiders&amp;quot; &#x2F; &amp;quot;User&amp;quot;]


def folder_uri_to_path(uri: str) -&amp;gt; Path | None:
    parsed = urlparse(uri)
    if parsed.scheme != &amp;quot;file&amp;quot;:
        return None
    path = unquote(parsed.path)
    if sys.platform == &amp;quot;win32&amp;quot; and path.startswith(&amp;quot;&#x2F;&amp;quot;) and len(path) &amp;gt; 2 and path[2] == &amp;quot;:&amp;quot;:
        path = path[1:]
    return Path(path).resolve()


def find_workspace_storage(workspace: Path, user_data_dir: Path | None = None) -&amp;gt; Path | None:
    wanted = workspace.resolve()
    for user_data in user_data_roots(user_data_dir):
        storage_root = user_data &#x2F; &amp;quot;workspaceStorage&amp;quot;
        if not storage_root.is_dir():
            continue
        for candidate in storage_root.iterdir():
            metadata = candidate &#x2F; &amp;quot;workspace.json&amp;quot;
            if not metadata.is_file():
                continue
            try:
                folder = json.loads(metadata.read_text(encoding=&amp;quot;utf-8&amp;quot;)).get(&amp;quot;folder&amp;quot;, &amp;quot;&amp;quot;)
                actual = folder_uri_to_path(folder)
            except (OSError, json.JSONDecodeError, ValueError):
                continue
            if actual == wanted:
                return candidate
    return None


def storage_paths(workspace: Path, user_data_dir: Path | None = None) -&amp;gt; tuple[Path, Path]:
    storage = find_workspace_storage(workspace, user_data_dir)
    if storage is None:
        raise RuntimeError(
            f&amp;quot;No VS Code workspace storage found for {workspace.resolve()}. &amp;quot;
            &amp;quot;Open the folder in VS Code and create at least one chat first.&amp;quot;
        )
    database = storage &#x2F; &amp;quot;state.vscdb&amp;quot;
    sessions = storage &#x2F; &amp;quot;chatSessions&amp;quot;
    if not database.is_file():
        raise RuntimeError(f&amp;quot;Missing {database}&amp;quot;)
    if not sessions.is_dir():
        raise RuntimeError(f&amp;quot;Missing {sessions}&amp;quot;)
    return database, sessions


def read_index(database: Path) -&amp;gt; dict[str, Any]:
    uri = database.resolve().as_uri() + &amp;quot;?mode=ro&amp;quot;
    connection = sqlite3.connect(uri, uri=True)
    try:
        row = connection.execute(
            &amp;quot;SELECT value FROM ItemTable WHERE key = ?&amp;quot;, (&amp;quot;chat.ChatSessionStore.index&amp;quot;,)
        ).fetchone()
    finally:
        connection.close()
    if row is None:
        return {}
    value = row[0].decode(&amp;quot;utf-8&amp;quot;) if isinstance(row[0], bytes) else row[0]
    return json.loads(value)


def list_sessions(database: Path, sessions_dir: Path) -&amp;gt; list[dict[str, Any]]:
    entries = read_index(database).get(&amp;quot;entries&amp;quot;, {})
    result = []
    for session_id, metadata in entries.items():
        session_file = sessions_dir &#x2F; f&amp;quot;{session_id}.jsonl&amp;quot;
        timestamp = metadata.get(&amp;quot;lastMessageDate&amp;quot;, 0)
        result.append(
            {
                &amp;quot;id&amp;quot;: session_id,
                &amp;quot;title&amp;quot;: metadata.get(&amp;quot;title&amp;quot;, &amp;quot;(untitled)&amp;quot;),
                &amp;quot;timestamp&amp;quot;: timestamp,
                &amp;quot;date&amp;quot;: datetime.fromtimestamp(timestamp &#x2F; 1000).isoformat(timespec=&amp;quot;minutes&amp;quot;)
                if timestamp
                else None,
                &amp;quot;exists&amp;quot;: session_file.is_file(),
                &amp;quot;size&amp;quot;: session_file.stat().st_size if session_file.is_file() else 0,
            }
        )
    result.sort(key=lambda item: item[&amp;quot;timestamp&amp;quot;], reverse=True)
    return result


def resolve_session(args: argparse.Namespace, database: Path, sessions_dir: Path) -&amp;gt; dict[str, Any]:
    sessions = list_sessions(database, sessions_dir)
    if args.uuid:
        matches = [item for item in sessions if item[&amp;quot;id&amp;quot;] == args.uuid]
    else:
        query = args.session.casefold()
        matches = [item for item in sessions if query in item[&amp;quot;title&amp;quot;].casefold()]
    if len(matches) != 1:
        if not matches:
            raise RuntimeError(&amp;quot;No matching session&amp;quot;)
        choices = &amp;quot;\n&amp;quot;.join(f&amp;quot;  {item[&amp;#39;date&amp;#39;]}  {item[&amp;#39;title&amp;#39;]}  {item[&amp;#39;id&amp;#39;]}&amp;quot; for item in matches)
        raise RuntimeError(f&amp;quot;Session selector is ambiguous:\n{choices}&amp;quot;)
    if not matches[0][&amp;quot;exists&amp;quot;]:
        raise RuntimeError(f&amp;quot;Session JSONL is missing for {matches[0][&amp;#39;id&amp;#39;]}&amp;quot;)
    return matches[0]


def reconstruct_session(session_file: Path) -&amp;gt; dict[str, Any]:
    # JSON Lines records are delimited by LF. str.splitlines() also splits on
    # legal JSON string characters such as U+0085 (NEL), corrupting a record.
    lines = session_file.read_text(encoding=&amp;quot;utf-8&amp;quot;).split(&amp;quot;\n&amp;quot;)
    if not lines:
        raise RuntimeError(f&amp;quot;Empty session file: {session_file}&amp;quot;)
    base = json.loads(lines[0])
    if base.get(&amp;quot;kind&amp;quot;) != 0 or &amp;quot;v&amp;quot; not in base:
        raise RuntimeError(f&amp;quot;Unsupported initial JSONL record in {session_file}&amp;quot;)
    session = base[&amp;quot;v&amp;quot;]
    for raw in lines[1:]:
        if not raw.strip():
            continue
        patch = json.loads(raw)
        kind = patch.get(&amp;quot;kind&amp;quot;)
        keys = patch.get(&amp;quot;k&amp;quot;, [])
        value = patch.get(&amp;quot;v&amp;quot;)
        if not keys:
            continue
        parent = session
        traversal = keys if kind == 2 else keys[:-1]
        try:
            for key in traversal:
                parent = parent[key]
        except (KeyError, IndexError, TypeError):
            continue
        if kind == 1:
            try:
                parent[keys[-1]] = value
            except (KeyError, IndexError, TypeError):
                continue
        elif kind == 2 and isinstance(parent, list):
            start = patch.get(&amp;quot;i&amp;quot;)
            if start is not None:
                del parent[start:]
            if isinstance(value, list):
                parent.extend(value)
        elif kind == 3:
            key = keys[-1]
            if isinstance(parent, dict):
                parent.pop(key, None)
            elif isinstance(parent, list) and isinstance(key, int) and 0 &amp;lt;= key &amp;lt; len(parent):
                del parent[key]
    return session


def response_text(items: list[Any]) -&amp;gt; str:
    parts = []
    for item in items:
        if isinstance(item, dict) and item.get(&amp;quot;kind&amp;quot;) is None:
            value = item.get(&amp;quot;value&amp;quot;)
            if isinstance(value, str) and value.strip():
                parts.append(value.strip())
    return &amp;quot;\n&amp;quot;.join(parts)


def collect_messages(session_file: Path, responses: bool) -&amp;gt; list[dict[str, Any]]:
    session = reconstruct_session(session_file)
    messages = []
    for index, request in enumerate(session.get(&amp;quot;requests&amp;quot;, [])):
        timestamp = request.get(&amp;quot;timestamp&amp;quot;, 0)
        text = request.get(&amp;quot;message&amp;quot;, {}).get(&amp;quot;text&amp;quot;, &amp;quot;&amp;quot;)
        if text:
            messages.append({&amp;quot;index&amp;quot;: index, &amp;quot;timestamp&amp;quot;: timestamp, &amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;text&amp;quot;: text})
        if responses:
            text = response_text(request.get(&amp;quot;response&amp;quot;, []))
            if text:
                messages.append(
                    {&amp;quot;index&amp;quot;: index, &amp;quot;timestamp&amp;quot;: timestamp, &amp;quot;role&amp;quot;: &amp;quot;assistant&amp;quot;, &amp;quot;text&amp;quot;: text}
                )
    return messages


def format_message(message: dict[str, Any], full: bool) -&amp;gt; str:
    timestamp = message[&amp;quot;timestamp&amp;quot;]
    when = datetime.fromtimestamp(timestamp &#x2F; 1000).isoformat(timespec=&amp;quot;minutes&amp;quot;) if timestamp else &amp;quot;?&amp;quot;
    marker = &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;quot; if message[&amp;quot;role&amp;quot;] == &amp;quot;assistant&amp;quot; else &amp;quot;   &amp;quot;
    if not full:
        return f&amp;quot;{marker} req[{message[&amp;#39;index&amp;#39;]}] {when}: {message[&amp;#39;text&amp;#39;][:80].replace(chr(10), &amp;#39; &amp;#39;)}&amp;quot;
    indented = message[&amp;quot;text&amp;quot;].replace(&amp;quot;\n&amp;quot;, &amp;quot;\n      &amp;quot;)
    return f&amp;quot;{marker} req[{message[&amp;#39;index&amp;#39;]}] {when}:\n      {indented}&amp;quot;


def select_messages(
    messages: list[dict[str, Any]], start: int | None, end: int | None
) -&amp;gt; list[dict[str, Any]]:
    if start is None or end is None:
        return messages
    return [message for message in messages if start &amp;lt;= message[&amp;quot;index&amp;quot;] &amp;lt;= end]


def print_messages(messages: list[dict[str, Any]], full: bool, as_json: bool) -&amp;gt; None:
    if as_json:
        print(json.dumps(messages, indent=2, ensure_ascii=False))
        return
    for message in messages:
        print(format_message(message, full))
        if full:
            print()



def parser() -&amp;gt; argparse.ArgumentParser:
    common = argparse.ArgumentParser(add_help=False)
    common.add_argument(&amp;quot;--workspace&amp;quot;, type=Path, default=Path.cwd())
    common.add_argument(&amp;quot;--user-data-dir&amp;quot;, type=Path, help=&amp;quot;explicit VS Code user-data directory&amp;quot;)
    common.add_argument(&amp;quot;--json&amp;quot;, action=&amp;quot;store_true&amp;quot;)
    selector = common.add_mutually_exclusive_group()
    selector.add_argument(&amp;quot;--session&amp;quot;)
    selector.add_argument(&amp;quot;--uuid&amp;quot;)

    result = argparse.ArgumentParser(description=__doc__)
    commands = result.add_subparsers(dest=&amp;quot;command&amp;quot;, required=True)
    commands.add_parser(&amp;quot;list&amp;quot;, parents=[common])

    search = commands.add_parser(&amp;quot;search&amp;quot;, parents=[common])
    search.add_argument(&amp;quot;query&amp;quot;)
    search.add_argument(&amp;quot;-C&amp;quot;, &amp;quot;--context&amp;quot;, type=int, default=0)
    search.add_argument(&amp;quot;--responses&amp;quot;, action=&amp;quot;store_true&amp;quot;)
    search.add_argument(&amp;quot;--full&amp;quot;, action=&amp;quot;store_true&amp;quot;)

    tail = commands.add_parser(&amp;quot;tail&amp;quot;, parents=[common])
    tail.add_argument(&amp;quot;-n&amp;quot;, type=int, default=10)
    tail.add_argument(&amp;quot;--responses&amp;quot;, action=&amp;quot;store_true&amp;quot;)
    tail.add_argument(&amp;quot;--full&amp;quot;, action=&amp;quot;store_true&amp;quot;)

    show = commands.add_parser(&amp;quot;show&amp;quot;, parents=[common])
    show.add_argument(&amp;quot;--range&amp;quot;, nargs=2, type=int, metavar=(&amp;quot;FROM&amp;quot;, &amp;quot;TO&amp;quot;), required=True)
    show.add_argument(&amp;quot;--responses&amp;quot;, action=&amp;quot;store_true&amp;quot;)
    show.add_argument(&amp;quot;--full&amp;quot;, action=&amp;quot;store_true&amp;quot;)

    return result


def main() -&amp;gt; int:
    configure_streams()
    args = parser().parse_args()
    try:
        database, sessions_dir = storage_paths(args.workspace, args.user_data_dir)
        sessions = list_sessions(database, sessions_dir)
        if args.command == &amp;quot;list&amp;quot;:
            if args.json:
                print(json.dumps(sessions, indent=2, ensure_ascii=False))
            else:
                for item in sessions:
                    print(f&amp;quot;{item[&amp;#39;date&amp;#39;] or &amp;#39;?&amp;#39;:16} {item[&amp;#39;size&amp;#39;]:&amp;gt;10,}  {item[&amp;#39;id&amp;#39;][:12]}  {item[&amp;#39;title&amp;#39;]}&amp;quot;)
            return 0

        if not args.session and not args.uuid:
            raise RuntimeError(&amp;quot;Specify exactly one of --session or --uuid&amp;quot;)
        session = resolve_session(args, database, sessions_dir)
        session_file = sessions_dir &#x2F; f&amp;quot;{session[&amp;#39;id&amp;#39;]}.jsonl&amp;quot;
        responses = getattr(args, &amp;quot;responses&amp;quot;, True)
        messages = collect_messages(session_file, responses=responses)

        if args.command == &amp;quot;tail&amp;quot;:
            if not args.json:
                total = len({message[&amp;quot;index&amp;quot;] for message in messages if message[&amp;quot;role&amp;quot;] == &amp;quot;user&amp;quot;})
                print(f&amp;quot;Total user messages: {total}&amp;quot;)
            print_messages(messages[-args.n :], args.full, args.json)
        elif args.command == &amp;quot;show&amp;quot;:
            print_messages(select_messages(messages, *args.range), args.full, args.json)
        elif args.command == &amp;quot;search&amp;quot;:
            matches = [index for index, message in enumerate(messages) if args.query.casefold() in message[&amp;quot;text&amp;quot;].casefold()]
            selected = set()
            for index in matches:
                selected.update(range(max(0, index - args.context), min(len(messages), index + args.context + 1)))
            print_messages([messages[index] for index in sorted(selected)], args.full, args.json)
        return 0
    except (OSError, RuntimeError, sqlite3.Error, json.JSONDecodeError) as error:
        print(f&amp;quot;ERROR: {error}&amp;quot;, file=sys.stderr)
        return 1


if __name__ == &amp;quot;__main__&amp;quot;:
    raise SystemExit(main())
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;zed-session-reader&quot;&gt;Zed session reader&lt;&#x2F;h3&gt;
&lt;p&gt;Source: &lt;code&gt;scripts&#x2F;zed-threads.py&lt;&#x2F;code&gt;&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;python&quot;&gt;#!&#x2F;usr&#x2F;bin&#x2F;env python3
&amp;quot;&amp;quot;&amp;quot;
zed-threads.py — Zed thread reader for session migration.

Reads threads from Zed&amp;#39;s local threads.db,
decompresses zstd JSON blobs, and outputs readable conversation text.

Usage:
    python zed-threads.py list [--limit N]
    python zed-threads.py show &amp;lt;thread_id_prefix&amp;gt; [--tail N] [--full]
    python zed-threads.py search &amp;lt;text&amp;gt; [--limit N]


Thread ID prefix: first 8+ chars of the UUID, enough to be unique.

Environment:
    ZED_THREADS_DB  Override path to threads.db
                    Default: %LOCALAPPDATA%&#x2F;Zed&#x2F;threads&#x2F;threads.db
&amp;quot;&amp;quot;&amp;quot;

import argparse
import json
import os
import re
import sqlite3
import sys
import textwrap
from datetime import datetime
from pathlib import Path

# Force UTF-8 stdout on Windows
if sys.platform == &amp;quot;win32&amp;quot;:
    sys.stdout.reconfigure(encoding=&amp;quot;utf-8&amp;quot;, errors=&amp;quot;replace&amp;quot;)
    sys.stderr.reconfigure(encoding=&amp;quot;utf-8&amp;quot;, errors=&amp;quot;replace&amp;quot;)

try:
    import zstandard as zstd
except ImportError:
    print(&amp;quot;ERROR: pip install zstandard&amp;quot;, file=sys.stderr)
    sys.exit(1)


def get_db_path() -&amp;gt; Path:
    env = os.environ.get(&amp;quot;ZED_THREADS_DB&amp;quot;)
    if env:
        return Path(env)
    local = os.environ.get(&amp;quot;LOCALAPPDATA&amp;quot;, &amp;quot;&amp;quot;)
    if local:
        return Path(local) &#x2F; &amp;quot;Zed&amp;quot; &#x2F; &amp;quot;threads&amp;quot; &#x2F; &amp;quot;threads.db&amp;quot;
    # Linux&#x2F;Mac fallback
    home = Path.home()
    for candidate in [
        home &#x2F; &amp;quot;.local&amp;quot; &#x2F; &amp;quot;share&amp;quot; &#x2F; &amp;quot;zed&amp;quot; &#x2F; &amp;quot;threads&amp;quot; &#x2F; &amp;quot;threads.db&amp;quot;,
        home &#x2F; &amp;quot;Library&amp;quot; &#x2F; &amp;quot;Application Support&amp;quot; &#x2F; &amp;quot;Zed&amp;quot; &#x2F; &amp;quot;threads&amp;quot; &#x2F; &amp;quot;threads.db&amp;quot;,
    ]:
        if candidate.exists():
            return candidate
    print(&amp;quot;ERROR: Cannot find threads.db. Set ZED_THREADS_DB.&amp;quot;, file=sys.stderr)
    sys.exit(1)


def connect(db_path: Path) -&amp;gt; sqlite3.Connection:
    if not db_path.exists():
        print(f&amp;quot;ERROR: {db_path} not found&amp;quot;, file=sys.stderr)
        sys.exit(1)
    return sqlite3.connect(db_path.resolve().as_uri() + &amp;quot;?mode=ro&amp;quot;, uri=True)


def decompress(data: bytes, data_type: str) -&amp;gt; dict:
    if data_type == &amp;quot;zstd&amp;quot;:
        decompressor = zstd.ZstdDecompressor()
        # Some frames lack content size in header; use streaming
        reader = decompressor.stream_reader(data)
        raw = reader.read()
        reader.close()
    else:
        raw = data
    return json.loads(raw)


def format_timestamp(ts: str | None) -&amp;gt; str:
    if not ts:
        return &amp;quot;?&amp;quot;
    try:
        # Zed uses RFC 3339 with nanoseconds
        # Truncate to microseconds for Python
        ts_clean = re.sub(r&amp;quot;(\.\d{6})\d+&amp;quot;, r&amp;quot;\1&amp;quot;, ts)
        dt = datetime.fromisoformat(ts_clean)
        return dt.strftime(&amp;quot;%Y-%m-%d %H:%M&amp;quot;)
    except Exception:
        return ts[:16] if len(ts) &amp;gt; 16 else ts


def format_size(n: int) -&amp;gt; str:
    if n &amp;lt; 1024:
        return f&amp;quot;{n}B&amp;quot;
    elif n &amp;lt; 1024 * 1024:
        return f&amp;quot;{n &#x2F; 1024:.1f}KB&amp;quot;
    else:
        return f&amp;quot;{n &#x2F; (1024 * 1024):.1f}MB&amp;quot;


def resolve_thread_id(conn: sqlite3.Connection, prefix: str) -&amp;gt; str:
    &amp;quot;&amp;quot;&amp;quot;Resolve a thread ID prefix to a full ID. Exits if ambiguous or not found.&amp;quot;&amp;quot;&amp;quot;
    cur = conn.cursor()
    cur.execute(&amp;quot;SELECT id FROM threads WHERE id LIKE ?&amp;quot;, (prefix + &amp;quot;%&amp;quot;,))
    matches = cur.fetchall()
    if len(matches) == 0:
        print(f&amp;quot;ERROR: No thread matching &amp;#39;{prefix}&amp;#39;&amp;quot;, file=sys.stderr)
        sys.exit(1)
    if len(matches) &amp;gt; 1:
        print(f&amp;quot;ERROR: Ambiguous prefix &amp;#39;{prefix}&amp;#39;, matches:&amp;quot;, file=sys.stderr)
        for m in matches:
            print(f&amp;quot;  {m[0]}&amp;quot;, file=sys.stderr)
        sys.exit(1)
    return matches[0][0]


# ─── Commands ───────────────────────────────────────────────────────


def cmd_list(args):
    conn = connect(get_db_path())
    cur = conn.cursor()
    cur.execute(
        &amp;quot;SELECT id, summary, created_at, updated_at, data_type, length(data), folder_paths &amp;quot;
        &amp;quot;FROM threads ORDER BY updated_at DESC LIMIT ?&amp;quot;,
        (args.limit,),
    )
    rows = cur.fetchall()
    if not rows:
        print(&amp;quot;No threads found.&amp;quot;)
        return

    print(f&amp;quot;{&amp;#39;ID&amp;#39;:12s}  {&amp;#39;Size&amp;#39;:&amp;gt;8s}  {&amp;#39;Updated&amp;#39;:16s}  {&amp;#39;Summary&amp;#39;}&amp;quot;)
    print(&amp;quot;─&amp;quot; * 80)
    for tid, summary, created, updated, dtype, size, folders in rows:
        print(
            f&amp;quot;{tid[:12]}  {format_size(size):&amp;gt;8s}  &amp;quot;
            f&amp;quot;{format_timestamp(updated):16s}  {summary[:50]}&amp;quot;
        )
    conn.close()


def extract_content_parts(content_list: list) -&amp;gt; str:
    &amp;quot;&amp;quot;&amp;quot;Extract bounded readable text from Zed&amp;#39;s content segment array.&amp;quot;&amp;quot;&amp;quot;
    parts = []
    for seg in content_list:
        if isinstance(seg, str):
            parts.append(seg)
        elif isinstance(seg, dict):
            # Zed uses PascalCase variant tags: {&amp;quot;Text&amp;quot;: &amp;quot;...&amp;quot;}, {&amp;quot;Thinking&amp;quot;: {...}}, etc.
            if &amp;quot;Text&amp;quot; in seg:
                parts.append(seg[&amp;quot;Text&amp;quot;])
            elif &amp;quot;text&amp;quot; in seg:
                parts.append(seg[&amp;quot;text&amp;quot;])
            elif &amp;quot;Thinking&amp;quot; in seg:
                t = seg[&amp;quot;Thinking&amp;quot;]
                text = t.get(&amp;quot;text&amp;quot;, &amp;quot;&amp;quot;) if isinstance(t, dict) else str(t)
                if text:
                    parts.append(
                        f&amp;quot;[thinking: {text[:200]}...]&amp;quot;
                        if len(text) &amp;gt; 200
                        else f&amp;quot;[thinking: {text}]&amp;quot;
                    )
            elif &amp;quot;ToolUse&amp;quot; in seg:
                tu = seg[&amp;quot;ToolUse&amp;quot;]
                name = tu.get(&amp;quot;name&amp;quot;, &amp;quot;?&amp;quot;) if isinstance(tu, dict) else &amp;quot;?&amp;quot;
                inp = tu.get(&amp;quot;input&amp;quot;, {}) if isinstance(tu, dict) else {}
                if isinstance(inp, dict):
                    keys = list(inp.keys())
                    parts.append(f&amp;quot;[tool: {name}({&amp;#39;, &amp;#39;.join(keys[:5])})]&amp;quot;)
                else:
                    parts.append(f&amp;quot;[tool: {name}]&amp;quot;)
            elif &amp;quot;ToolResult&amp;quot; in seg:
                tr = seg[&amp;quot;ToolResult&amp;quot;]
                content = tr.get(&amp;quot;content&amp;quot;, &amp;quot;&amp;quot;) if isinstance(tr, dict) else str(tr)
                if isinstance(content, list):
                    # Tool result content is often [{&amp;quot;Text&amp;quot;: &amp;quot;...&amp;quot;}]
                    text_parts = []
                    for item in content:
                        if isinstance(item, dict) and &amp;quot;Text&amp;quot; in item:
                            text_parts.append(item[&amp;quot;Text&amp;quot;][:200])
                        elif isinstance(item, str):
                            text_parts.append(item[:200])
                    content = &amp;quot;\n&amp;quot;.join(text_parts)
                if isinstance(content, str) and len(content) &amp;gt; 300:
                    content = content[:300] + &amp;quot;...&amp;quot;
                parts.append(f&amp;quot;[result: {content}]&amp;quot;)
            elif &amp;quot;Image&amp;quot; in seg:
                parts.append(&amp;quot;[image]&amp;quot;)
            else:
                # Unknown segment type — show keys
                keys = list(seg.keys())
                parts.append(f&amp;quot;[{keys[0] if keys else &amp;#39;?&amp;#39;}: ...]&amp;quot;)
    return &amp;quot;\n&amp;quot;.join(p for p in parts if p)


def extract_messages(thread_json: dict) -&amp;gt; list[dict]:
    &amp;quot;&amp;quot;&amp;quot;Extract messages from thread JSON as role&#x2F;text&#x2F;timestamp&#x2F;id records.&amp;quot;&amp;quot;&amp;quot;
    messages = []
    raw_messages = thread_json.get(&amp;quot;messages&amp;quot;, [])

    for msg in raw_messages:
        # Zed format: each message is {&amp;quot;User&amp;quot;: {...}}, {&amp;quot;Agent&amp;quot;: {...}}, or {&amp;quot;System&amp;quot;: {...}}
        if isinstance(msg, dict):
            for role_key in (&amp;quot;User&amp;quot;, &amp;quot;Agent&amp;quot;, &amp;quot;System&amp;quot;, &amp;quot;Assistant&amp;quot;):
                if role_key in msg:
                    inner = msg[role_key]
                    content_raw = inner.get(&amp;quot;content&amp;quot;, [])
                    if isinstance(content_raw, list):
                        text = extract_content_parts(content_raw)
                    elif isinstance(content_raw, str):
                        text = content_raw
                    else:
                        text = str(content_raw)

                    messages.append(
                        {
                            &amp;quot;role&amp;quot;: role_key.lower(),
                            &amp;quot;text&amp;quot;: text,
                            &amp;quot;timestamp&amp;quot;: inner.get(&amp;quot;created_at&amp;quot;),
                            &amp;quot;id&amp;quot;: inner.get(&amp;quot;id&amp;quot;, &amp;quot;&amp;quot;),
                        }
                    )
                    break
            else:
                # Unknown role variant
                keys = list(msg.keys())
                messages.append(
                    {
                        &amp;quot;role&amp;quot;: keys[0] if keys else &amp;quot;unknown&amp;quot;,
                        &amp;quot;text&amp;quot;: &amp;quot;&amp;quot;,
                        &amp;quot;timestamp&amp;quot;: None,
                    }
                )

    return messages


def cmd_show(args):
    conn = connect(get_db_path())
    thread_id = resolve_thread_id(conn, args.thread_id)
    cur = conn.cursor()
    cur.execute(
        &amp;quot;SELECT summary, data_type, data, created_at, updated_at, folder_paths FROM threads WHERE id = ?&amp;quot;,
        (thread_id,),
    )
    row = cur.fetchone()
    if not row:
        print(f&amp;quot;ERROR: Thread {thread_id} not found&amp;quot;, file=sys.stderr)
        sys.exit(1)

    summary, data_type, data, created, updated, folders = row


    thread_json = decompress(data, data_type)
    messages = extract_messages(thread_json)

    # Header
    print(f&amp;quot;Thread: {summary}&amp;quot;)
    print(f&amp;quot;ID: {thread_id}&amp;quot;)
    print(f&amp;quot;Created: {format_timestamp(created)}  Updated: {format_timestamp(updated)}&amp;quot;)
    if folders:
        print(f&amp;quot;Workspace: {folders}&amp;quot;)
    print(f&amp;quot;Messages: {len(messages)}&amp;quot;)
    print(&amp;quot;═&amp;quot; * 80)

    # Optionally tail
    if args.tail and args.tail &amp;lt; len(messages):
        messages = messages[-args.tail :]
        print(f&amp;quot;(showing last {args.tail} messages)\n&amp;quot;)

    for i, msg in enumerate(messages):
        role = msg[&amp;quot;role&amp;quot;].upper()
        ts = &amp;quot;&amp;quot;
        if msg.get(&amp;quot;timestamp&amp;quot;):
            ts = f&amp;quot; [{format_timestamp(msg[&amp;#39;timestamp&amp;#39;])}]&amp;quot;

        if role == &amp;quot;USER&amp;quot;:
            print(f&amp;quot;\n{&amp;#39;─&amp;#39; * 40} USER{ts} {&amp;#39;─&amp;#39; * 35}&amp;quot;)
        elif role == &amp;quot;ASSISTANT&amp;quot;:
            print(f&amp;quot;\n{&amp;#39;─&amp;#39; * 38} ASSISTANT{ts} {&amp;#39;─&amp;#39; * 32}&amp;quot;)
        else:
            print(f&amp;quot;\n{&amp;#39;─&amp;#39; * 38} {role}{ts} {&amp;#39;─&amp;#39; * 32}&amp;quot;)

        text = msg[&amp;quot;text&amp;quot;].strip()
        if text:
            # Truncate very long messages for overview
            if not args.full and len(text) &amp;gt; 3000:
                print(text[:3000])
                print(
                    f&amp;quot;\n... [{len(text) - 3000} chars truncated, use --full to see all]&amp;quot;
                )
            else:
                print(text)

    conn.close()


def cmd_search(args):
    conn = connect(get_db_path())
    cur = conn.cursor()
    cur.execute(
        &amp;quot;SELECT id, summary, data_type, data, updated_at, length(data) &amp;quot;
        &amp;quot;FROM threads ORDER BY updated_at DESC&amp;quot;
    )
    pattern = re.compile(re.escape(args.text), re.IGNORECASE)
    found = 0

    for tid, summary, data_type, data, updated, size in cur:
        # Search in summary first (cheap)
        if pattern.search(summary or &amp;quot;&amp;quot;):
            print(
                f&amp;quot;{tid[:12]}  {format_size(size):&amp;gt;8s}  {format_timestamp(updated)}  {summary[:60]}&amp;quot;
            )
            found += 1
            if found &amp;gt;= args.limit:
                break
            continue
        # Search in decompressed content
        try:
            thread_json = decompress(data, data_type)
            messages = extract_messages(thread_json)
            for msg in messages:
                if pattern.search(msg[&amp;quot;text&amp;quot;]):
                    print(
                        f&amp;quot;{tid[:12]}  {format_size(size):&amp;gt;8s}  {format_timestamp(updated)}  {summary[:60]}&amp;quot;
                    )
                    found += 1
                    break
        except Exception:
            pass
        if found &amp;gt;= args.limit:
            break

    if found == 0:
        print(f&amp;quot;No threads matching &amp;#39;{args.text}&amp;#39;&amp;quot;)
    conn.close()



# ─── Main ───────────────────────────────────────────────────────────


def main():
    parser = argparse.ArgumentParser(
        description=&amp;quot;Zed thread reader for session migration&amp;quot;,
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog=textwrap.dedent(&amp;quot;&amp;quot;&amp;quot;
        Examples:
          zed-threads.py list
          zed-threads.py list --limit 20
          zed-threads.py show 965cf72c --tail 20

          zed-threads.py search &amp;quot;GigaChat&amp;quot;
        &amp;quot;&amp;quot;&amp;quot;),
    )
    sub = parser.add_subparsers(dest=&amp;quot;command&amp;quot;, required=True)

    # list
    p_list = sub.add_parser(&amp;quot;list&amp;quot;, help=&amp;quot;List recent threads&amp;quot;)
    p_list.add_argument(&amp;quot;--limit&amp;quot;, type=int, default=15)

    # show
    p_show = sub.add_parser(&amp;quot;show&amp;quot;, help=&amp;quot;Show thread conversation&amp;quot;)
    p_show.add_argument(&amp;quot;thread_id&amp;quot;, help=&amp;quot;Thread ID prefix (8+ chars)&amp;quot;)
    p_show.add_argument(&amp;quot;--tail&amp;quot;, type=int, help=&amp;quot;Show only last N messages&amp;quot;)

    p_show.add_argument(
        &amp;quot;--full&amp;quot;, action=&amp;quot;store_true&amp;quot;, help=&amp;quot;Don&amp;#39;t truncate long messages&amp;quot;
    )

    # search
    p_search = sub.add_parser(&amp;quot;search&amp;quot;, help=&amp;quot;Search thread content&amp;quot;)
    p_search.add_argument(&amp;quot;text&amp;quot;, help=&amp;quot;Text to search for&amp;quot;)
    p_search.add_argument(&amp;quot;--limit&amp;quot;, type=int, default=10)


    args = parser.parse_args()

    if args.command == &amp;quot;list&amp;quot;:
        cmd_list(args)
    elif args.command == &amp;quot;show&amp;quot;:
        cmd_show(args)
    elif args.command == &amp;quot;search&amp;quot;:
        cmd_search(args)


if __name__ == &amp;quot;__main__&amp;quot;:
    main()
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;h1 id=&quot;practice-2-maintain-memory-and-journal&quot;&gt;Practice 2: Maintain MEMORY and JOURNAL&lt;&#x2F;h1&gt;
&lt;p&gt;One remedy for LLM &lt;em&gt;amnesia&lt;&#x2F;em&gt; has been known since the beginning of the
AI era: MEMORY files. “Save context into a file and read it in the next
session” is a trivial and established idea. The concrete seed of our
system was Dany Fabian’s (hi, Dany!) MEMORY system prompt. Sasha started
from that exact prompt, then revised it repeatedly until it evolved into
the current contracts we share here.&lt;&#x2F;p&gt;
&lt;p&gt;A single dumped context has one immediate problem. A new session needs
two incompatible kinds of context:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;the unfinished trajectory of yesterday’s work;&lt;&#x2F;li&gt;
&lt;li&gt;current local invariants established during previous sessions,
possibly over months.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;Put both into one file and it becomes either a diary too noisy to load
every time or a summary too compressed to resume real work.&lt;&#x2F;p&gt;
&lt;p&gt;At the start of each session, including after restoring prior context,
the assistant reads the complete &lt;code&gt;MEMORY.md&lt;&#x2F;code&gt; and the recent tail of
&lt;code&gt;JOURNAL.md&lt;&#x2F;code&gt;.&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;MEMORY.md&lt;&#x2F;code&gt; contains durable cross-session decisions, constraints,
working preferences, reusable heuristics, and behavioral rules
discovered during the sessions. MEMORY is maintained &lt;em&gt;consolidated&lt;&#x2F;em&gt;:
it should be compact, non-repetitive, and, most importantly, not
self-contradictory. Although transported as &lt;code&gt;tool output&lt;&#x2F;code&gt; rather than
the protocol’s &lt;code&gt;system&lt;&#x2F;code&gt; role, it is a &lt;em&gt;dynamic system prompt&lt;&#x2F;em&gt; by
function: persistent context explicitly loaded to shape the
assistant’s behavior.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;code&gt;JOURNAL.md&lt;&#x2F;code&gt; contains the flow of work: attempted approaches,
observations, intermediate reasoning, unresolved questions, current
state, and the exact next discriminating action. It is mostly
append-only and populated automatically by the assistant. JOURNAL
records consequential working state; the raw AI session history
preserves every message and tool call. JOURNAL is the operational
context.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Relation to &lt;code&gt;AGENTS.md&lt;&#x2F;code&gt;&#x2F;&lt;code&gt;CLAUDE.md&lt;&#x2F;code&gt; and the &lt;strong&gt;exact&lt;&#x2F;strong&gt; loading mechanism&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;relation-to-agents-md-claude-md-and-the-exact-loading-mechanism&quot; class=&quot;cut-toc-heading&quot;&gt;Relation to AGENTS.md&#x2F;CLAUDE.md and the exact loading mechanism&lt;&#x2F;h2&gt;
&lt;p&gt;The obvious neighboring mechanism is &lt;code&gt;AGENTS.md&lt;&#x2F;code&gt;&#x2F;&lt;code&gt;CLAUDE.md&lt;&#x2F;code&gt;
&lt;a href=&quot;#ref-anthropic2026projectmemory&quot;&gt;[3]&lt;&#x2F;a&gt;, &lt;a href=&quot;#ref-agentsmd&quot;&gt;[4]&lt;&#x2F;a&gt;. At the
mechanical level, it is similar: persist text and load it into a later
context. The functional contract is different. Repository instruction
files normally carry shared, relatively stable project guidance; our
MEMORY&#x2F;JOURNAL carries user-assistant continuity, changing rationale,
corrections, failed approaches, and prompt candidates that may span
repositories. A mature rule may be promoted into checked-in &lt;code&gt;AGENTS.md&lt;&#x2F;code&gt;,
but the file does not recover the previous session or decide which
personal observation should become team policy.&lt;&#x2F;p&gt;
&lt;p&gt;Both VS Code and Zed place &lt;code&gt;AGENTS.md&lt;&#x2F;code&gt; content near the beginning of
each request as part of the &lt;em&gt;system prompt&lt;&#x2F;em&gt;
&lt;a href=&quot;#ref-microsoft2026vscodeinstructions&quot;&gt;[5]&lt;&#x2F;a&gt;,
&lt;a href=&quot;#ref-microsoft2026vscodeagentprompt&quot;&gt;[6]&lt;&#x2F;a&gt;,
&lt;a href=&quot;#ref-zed2026agentsmd&quot;&gt;[7]&lt;&#x2F;a&gt;, &lt;a href=&quot;#ref-zed2026agentprompt&quot;&gt;[8]&lt;&#x2F;a&gt;. Changes
become visible on the next request, but changing that prefix invalidates
the server-side KV cache from the first changed token
&lt;a href=&quot;#ref-anthropic2026promptcaching&quot;&gt;[9]&lt;&#x2F;a&gt;. At current Claude Opus 4.7 API
list prices, rebuilding a one-million-token prefix costs USD 5.75 more
than reading it from cache.&lt;&#x2F;p&gt;
&lt;p&gt;Claude Code and Codex use a different approach: they snapshot
instructions when the session or run starts; Claude Code also reloads
them after &lt;code&gt;&#x2F;compact&lt;&#x2F;code&gt; &lt;a href=&quot;#ref-anthropic2026projectmemory&quot;&gt;[3]&lt;&#x2F;a&gt;,
&lt;a href=&quot;#ref-openai2026codexagentsmd&quot;&gt;[10]&lt;&#x2F;a&gt;. Edits do not affect the active
conversation until an explicit reload. This avoids accidental cache
misses but leaves the session on stale instructions in the meantime.&lt;&#x2F;p&gt;
&lt;p&gt;Neither policy fits changing mid-memory particularly well. This is one
reason we prefer an ordinary file loaded deliberately. The contract is
unusually portable: every agent system we use can read a file through an
ordinary file tool, and that read enters the conversation at the current
position rather than rewriting an earlier request prefix. The current
session keeps using the copy already present in its conversation. We can
edit the durable file without silently changing the active prompt, and
the next session reads the current version during its explicit startup
procedure. Promotion into always-loaded instructions remains useful for
stable rules, but it is a poor default for knowledge that is supposed to
evolve while we work.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Meta-lesson:&lt;&#x2F;strong&gt; before implementing any AI subsystem, verify the exact
behavior of the &lt;em&gt;agent harness&lt;&#x2F;em&gt; (the client&#x2F;runtime that assembles model
requests and mediates context, tools, and persistence). Which files does
it discover? When are they read? Where do they enter the request:
system&#x2F;developer instructions, user context, or tool output? Are they
snapshotted per session, watched and reloaded per request, or injected
only after an explicit action? Which prefix does the provider cache,
what invalidates it, and how are cache reads and writes billed? Do not
infer these semantics from the filename or from another client
implementing the same convention. Otherwise the system can behave
&lt;em&gt;surprisingly&lt;&#x2F;em&gt; or produce a &lt;em&gt;surprising&lt;&#x2F;em&gt; bill.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;p&gt;JOURNAL allows the AI to resume the work immediately and preserves the
consequential history of successes, failures, and unresolved state. That
is very useful for post-hoc analysis, again, with AI. Say, writing a
pull-request description or daily report—try it! JOURNAL grows
incrementally; we only &lt;em&gt;append&lt;&#x2F;em&gt; the logs. It has a tendency to grow
really fast, which is why during routine work we read only the tail.&lt;&#x2F;p&gt;
&lt;p&gt;MEMORY keeps &lt;em&gt;curated, reusable transient contracts established during
the sessions&lt;&#x2F;em&gt;: statements too important to drop at the end of a session
but not mature enough to be carved into a prompt, configuration file,
README, wiki page, etc. Unlike the mostly append-only JOURNAL, MEMORY is
maintained as a &lt;em&gt;random-access&lt;&#x2F;em&gt; structure: the AI adds details, replaces
superseded statements, and reorganizes entries as understanding changes.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;JOURNAL records what we did; MEMORY records what we agreed should
govern future work.&lt;&#x2F;strong&gt; We call this layer &lt;em&gt;mid-memory&lt;&#x2F;em&gt;: it sits between
the current conversation context and persistent artifacts.&lt;&#x2F;p&gt;
&lt;p&gt;Periodic maintenance (commonly called &lt;em&gt;memory consolidation&lt;&#x2F;em&gt;
&lt;a href=&quot;#ref-zhou2025mem1&quot;&gt;[11]&lt;&#x2F;a&gt; in agent-memory research) is also necessary.
During our consolidation (see the SKILL below), the MEMORY notes are
processed:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;conflicts are resolved;&lt;&#x2F;li&gt;
&lt;li&gt;outdated information is corrected or removed;&lt;&#x2F;li&gt;
&lt;li&gt;duplicates are merged;&lt;&#x2F;li&gt;
&lt;li&gt;statements are refined and shortened;&lt;&#x2F;li&gt;
&lt;li&gt;irrelevant content is removed:
&lt;ul&gt;
&lt;li&gt;weak, ad-hoc, or simply wrong statements and garbage → delete. Raw
AI sessions preserve the underlying record;&lt;&#x2F;li&gt;
&lt;li&gt;accidentally memoized sensitive information (credentials, personal
data, IP, etc.) → remove and implement the appropriate cleanup
protocol;&lt;&#x2F;li&gt;
&lt;li&gt;invariant that can be &lt;em&gt;cheaply&lt;&#x2F;em&gt; derived from code and configuration
→ delete;&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;mature content is promoted to a stronger mechanism:
&lt;ul&gt;
&lt;li&gt;stable behavioral rule → &lt;em&gt;instructions&lt;&#x2F;em&gt; (in VS Code terminology),
becoming part of the actual system prompt;&lt;&#x2F;li&gt;
&lt;li&gt;repeated executable procedure → script or SKILL;&lt;&#x2F;li&gt;
&lt;li&gt;local rationale or design decision → code comment or README;&lt;&#x2F;li&gt;
&lt;li&gt;stable deterministic invariant that is cheaply enforceable →
assertion, test, type, or validation schema; &lt;strong&gt;do not use AI as a
unit-test engine;&lt;&#x2F;strong&gt;&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;important but still-uncertain notes are kept in MEMORY with explicit
provenance and scope.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;What about “competing” approaches? Shared AI memory is already a highly
developed design space spanning graph and temporal retrieval, LLM-native
compression, agent self-editing, staged consolidation, explicit human
approval, and transparent file persistence
&lt;a href=&quot;#ref-firstintent2026survey&quot;&gt;[12]&lt;&#x2F;a&gt;. Three systems illustrate the
relevant compromises. Augment Code’s Memory Review lets users approve,
edit, or discard each proposed memory
&lt;a href=&quot;#ref-shah2026memoryreview&quot;&gt;[13]&lt;&#x2F;a&gt;. Codex generates local memory from
prior chats in the background and exposes separate extraction and
consolidation configuration &lt;a href=&quot;#ref-openai2026memories&quot;&gt;[14]&lt;&#x2F;a&gt;. Claude
auto-memory and Copilot Memory emphasize low-friction automatic capture;
Claude combines it with human-authored &lt;code&gt;CLAUDE.md&lt;&#x2F;code&gt;, while Copilot
validates repository memories against cited code and expires unused
entries after 28 days &lt;a href=&quot;#ref-anthropic2026projectmemory&quot;&gt;[3]&lt;&#x2F;a&gt;,
&lt;a href=&quot;#ref-github2026copilotmemory&quot;&gt;[15]&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;Our specific trade-off is human-controlled &lt;em&gt;accumulation&lt;&#x2F;em&gt;, &lt;em&gt;curation&lt;&#x2F;em&gt;,
and &lt;em&gt;promotion&lt;&#x2F;em&gt; in an ordinary editable artifact. MEMORY does not
accumulate raw data or random phrases from the conversation; that
high-recall role belongs to JOURNAL and raw session history. The AI may
propose a candidate, but almost every admission is discussed and every
MEMORY change is reviewed. AI can suggest stronger promotion targets;
promotion itself is not delegated.&lt;&#x2F;p&gt;
&lt;p&gt;This costs human attention but keeps the cached precedent corrigible. An
observed phrase does not silently become precedent to be replicated, and
mature entries may leave MEMORY for a stronger mechanism.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Artifacts.&lt;&#x2F;strong&gt; Public memory instructions, consolidation procedure, and
paired MEMORY&#x2F;JOURNAL examples are embedded under the cut immediately
after this practice. The production
&lt;code&gt;prompts&#x2F;memory-consolidation.prompt.md&lt;&#x2F;code&gt; contains additional local
policy and historical constraints.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;From incidents to precedents&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;from-incidents-to-precedents&quot; class=&quot;cut-toc-heading&quot;&gt;From incidents to precedents&lt;&#x2F;h2&gt;
&lt;p&gt;Daily work produces different kinds of reusable precedent. Sometimes we
preserve a way to investigate, sometimes a procedure that should improve
through recurrence, and sometimes the reason an apparent result must be
rejected.&lt;&#x2F;p&gt;
&lt;h3 id=&quot;history-as-architecture-documentation&quot;&gt;History as architecture documentation&lt;&#x2F;h3&gt;
&lt;p&gt;While implementing a feature, I could not integrate it correctly. After
several attempts, I asked the AI to inspect Git history: who had made a
similar change, when, and exactly which files had changed together? The
previous working change exposed an integration contract that neither the
current code nor the build documentation made obvious. I then asked the
AI to &lt;em&gt;memoize&lt;&#x2F;em&gt; the method—to preserve it as reusable precedent—rather
than this one fix:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;When adding to a configuration system you do not fully understand,
search history for the most distinctive existing entry and inspect the
complete change that introduced it. Use that diff to discover the
parallel manifests, exports, packaging, localization, or generated
files the new entry also requires.&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;The first use solved one feature-integration problem. The promoted
heuristic now applies whenever an unfamiliar integration scatters its
real contract across configuration, packaging, generated artifacts, or
other companion files.&lt;&#x2F;p&gt;
&lt;h3 id=&quot;repair-the-procedure-not-only-the-current-file&quot;&gt;Repair the procedure, not only the current file&lt;&#x2F;h3&gt;
&lt;p&gt;Some build-configuration files kept generated locale lists on lines more
than a thousand characters long. A normal structured edit triggered an
editor formatter, reflowing the complete file when only one entry was
intended to change. Applying another text edit to repair the result
risked transforming it again, so we restored the exact committed bytes
through Git and changed the procedure rather than merely fixing that
file.&lt;&#x2F;p&gt;
&lt;p&gt;The remembered procedure now treats these files as byte-sensitive: make
a surgical line insertion outside the formatter, use a fresh shell scope
so stale variables cannot affect the operation, then verify the exact
line-count delta, the number of matching entries, and the trailing
newline. When the problem recurred, the assistant could recover and
refine that procedure instead of rediscovering the failure:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;corrupted edit
→ exact byte restoration
→ reviewed repair procedure
→ postcondition checks added by recurrence
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;A workaround becomes cumulative only when the durable procedure changes
with what the next execution teaches us.&lt;&#x2F;p&gt;
&lt;h3 id=&quot;a-39-regression-that-was-not-a-regression&quot;&gt;A 39% regression that was not a regression&lt;&#x2F;h3&gt;
&lt;p&gt;A candidate computational GPU kernel appeared to reduce token-generation
speed by 39%. We inspected the implementation and reverted it before
noticing that two detached workers were saturating the same GPU during
the candidate run. The code had changed, but so had the experiment.&lt;&#x2F;p&gt;
&lt;p&gt;The invalid result remained useful because JOURNAL preserved the
sequence and MEMORY preserved the corrected rule:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;Benchmark conditions include machine load. Before timing, verify
detached jobs and GPU&#x2F;accelerator utilization. A large unexpected
regression is first a measurement-validity question.
Output-correctness and model-quality checks may remain valid under
contention; timing comparisons do not.&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;The point of MEMORY was not to keep the original conclusion alive. It
was to prevent the same invalid comparison from controlling the next
decision.&lt;&#x2F;p&gt;
&lt;h3 id=&quot;governance-in-practice&quot;&gt;Governance in practice&lt;&#x2F;h3&gt;
&lt;p&gt;The formal words are &lt;em&gt;accumulation&lt;&#x2F;em&gt;, &lt;em&gt;curation&lt;&#x2F;em&gt;, and &lt;em&gt;promotion&lt;&#x2F;em&gt;. In
practice, the review sounds more like this:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;— Memoize that as YOUR judgement =)&lt;&#x2F;p&gt;
&lt;p&gt;— No, that is an editorial contract. I mean, that exact statement is
not for the doc. Memoize it separately.&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;The MEMORY culture changes the current work, not only the next session.
Before writing an entry, the assistant may ask:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;— Is this global or project-local?&lt;&#x2F;p&gt;
&lt;p&gt;— Is it the intended architecture, a temporary workaround, or a hack
we accepted?&lt;&#x2F;p&gt;
&lt;p&gt;— Is it an observation, hypothesis, decision, or preference?&lt;&#x2F;p&gt;
&lt;p&gt;— What is its provenance, and what would make it obsolete?&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;Answering forces a high-level judgment while the context is still warm.
These small interventions form the decision record: they distinguish a
reviewed contract from a plausible summary and maintain a shared model
of the system.&lt;&#x2F;p&gt;
&lt;p&gt;That is the division of labor. We delegate low-level search, editing,
command execution, and repetitive mechanics to AI while retaining
purpose, architecture, trade-offs, and ownership. Writing MEMORY is the
checkpoint where generated work is lifted back into a human-understood
model: what changed, why we accepted it, and what should govern the next
decision.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Reference memory instructions, consolidation procedure, and examples&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;reference-memory-instructions-consolidation-procedure-and-examples&quot; class=&quot;cut-toc-heading&quot;&gt;Reference memory instructions, consolidation procedure, and examples&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;cross-session-memory-instructions&quot;&gt;Cross-session memory instructions&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# Cross-Session Memory Instructions

At the start of a new session:

1. Read the complete `MEMORY.md`.
2. Read the recent tail of `JOURNAL.md` needed to recover active work.

Use the files for different purposes:

- `JOURNAL.md` records consequential working trajectory: attempts, failures, observations, active state,
  artifact paths, and the next discriminating action. Append while work develops.
- `MEMORY.md` stores reviewed reusable precedent: constraints, rationale, heuristics, preferences, ownership,
  and uncertain but important findings. Revise existing entries instead of appending contradictory layers.

Do not silently convert conversation into precedent. The AI may propose a memory candidate, but consequential
admission, scope, and promotion are reviewed with the operator. Preserve attribution, epistemic status,
relevant date&#x2F;version, and a pointer to primary evidence when being wrong would matter.

When a memory matures, consider the mechanism that fits its type:

- behavioral rule → instruction or operating contract;
- repeated procedure → script or SKILL, when its contract is worth defining and maintaining;
- deterministic invariant → design, type, schema, assertion, or test;
- local rationale → comment or README;
- professional routing → address book;
- obsolete, contradicted, derivable, or irrelevant material → correct, relocate, or delete.

MEMORY is context and cached precedent, not absolute truth. Current primary artifacts and reproducible
observations can invalidate it. Report contradictions and repair the cache rather than choosing silently.

When memory behavior is surprising, explain which remembered rule or fact influenced the response and cite its
exact entry. Treat the explanation as a hypothesis; verify it against the loaded files and available tool
trace. Diagnose both false negatives (relevant memory was not used) and false positives (irrelevant, stale, or
overgeneralized memory controlled the response), then correct and retry.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;memory-example&quot;&gt;MEMORY example&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# MEMORY — Example

## Benchmark validity

**Observed 2026-08-11; project: ExampleKernel.** A candidate appeared to regress generation throughput by 39%,
but two detached workers were using the same GPU during the candidate run. The timing comparison was invalid;
correctness checks remained usable.

**Reusable rule:** before comparing timing or throughput, verify detached jobs and GPU&#x2F;accelerator
utilization. A large unexpected regression is first a measurement-validity question. Record machine load with
every timing cell.

**Primary evidence:** `JOURNAL.example.md`, benchmark log `example-kernel-20260811.log`, commit `0123456`.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;journal-example&quot;&gt;JOURNAL example&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# JOURNAL — Example

## 2026-08-11 — ExampleKernel comparison

Purpose: determine whether the candidate kernel increases fixed-workload throughput without changing
model-output quality.

The baseline ran on an idle GPU. The candidate reported a 39% generation regression. Before changing code,
checked active jobs and found two detached decomposition workers saturating the same device. The result is
invalid; the magnitude is not plausible for the candidate&amp;#39;s small scheduling change.

Completed:

- output-correctness and model-quality checks passed;
- invalid timing artifacts retained for diagnosis;
- candidate revert recorded but should not be interpreted as evidence against the design.

Next action: stop or finish the unrelated workers, restore the candidate, then rerun baseline and candidate
under matched idle conditions with utilization recorded.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;memory-consolidation-procedure&quot;&gt;Memory consolidation procedure&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# Memory Consolidation

Maintain an existing MEMORY&#x2F;JOURNAL system. A consolidation pass is not a summary of the latest conversation.

## Procedure

1. Read the complete relevant MEMORY files and the JOURNAL ranges needed to recover provenance.
2. Find exact and semantic duplicates, contradictions, stale paths&#x2F;versions, misplaced project material,
   unresolved claims stated as facts, and procedures or invariants that belong in stronger artifacts.
3. For each candidate, choose explicitly:
   - keep;
   - rewrite;
   - merge;
   - correct;
   - relocate;
   - promote;
   - delete.
4. Preserve meaningful disagreement, attribution, uncertainty, scope, and evidence pointers.
5. Update dependent instructions, scripts, indexes, and references when an entry moves.
6. Review the diff for accidental loss of distinct reasoning and run formatting checks.

The AI may perform search and mechanical editing. A human reviews admissions, semantic merges, corrections,
deletions, and promotion decisions. Automatic consolidation without review can recursively summarize earlier
model output into apparent authority without adding evidence.

A successful pass makes the memory system smaller, more accurate, or easier to act on. Merely appending
today&amp;#39;s summary is memoization, not consolidation.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;h1 id=&quot;practice-3-reconstruct-and-reuse-workflows-before-abstracting-them&quot;&gt;Practice 3: Reconstruct and reuse workflows before abstracting them&lt;&#x2F;h1&gt;
&lt;p&gt;In ordinary software engineering, reuse is usually framed through two
polar approaches. In &lt;em&gt;generalization&lt;&#x2F;em&gt; or &lt;em&gt;abstraction&lt;&#x2F;em&gt;, a potentially
repeated procedure is factored into a dedicated abstraction. In
&lt;em&gt;translation&lt;&#x2F;em&gt;, practically known as &lt;strong&gt;copy-paste&lt;&#x2F;strong&gt;, we copy existing
code without raising its abstraction level (that is, without creating a
new class, interface, template, prompt, SKILL, etc.), adapt it to the
new setting, use it, and repeat.&lt;&#x2F;p&gt;
&lt;p&gt;History, engineering practice, and folklore push us toward the first
approach. Not because abstraction is an ultimate &lt;em&gt;virtue&lt;&#x2F;em&gt;: it is a
&lt;em&gt;trade-off&lt;&#x2F;em&gt;. Generalization is conceptually hard and, when done
properly, requires significant cognitive effort. It also demands a kind
of fortune-telling because the system must account for &lt;strong&gt;future&lt;&#x2F;strong&gt; use
cases. People often consciously prefer copy-paste or boilerplate because
abstraction is the worse option.&lt;&#x2F;p&gt;
&lt;p&gt;AI significantly changes the balance in this equation. The shift is
relative. AI makes generalization substantially easier as well, no
question, but the problem remains conceptually hard. At the same time,
AI is amazingly good at &lt;em&gt;translation&lt;&#x2F;em&gt;. A human can patch a few paths in
an old script, but reliably translating an environment-bound procedure
across changed paths, versions, tools, and surrounding code is tedious
and error-prone. Building a proper abstraction was often the safer form
of reuse. AI can perform that concrete translation cheaply enough that a
preserved execution becomes reusable even before it becomes an
abstraction. &lt;strong&gt;AI can follow instructions regardless of their origin;
preserving a concrete execution can therefore make it reusable before a
dedicated SKILL exists.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;A proper memory system therefore creates a new equilibrium for pipeline
reproducibility: preserve enough evidence to reconstruct and translate
the concrete workflow later.&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;goldfish
→ reinvent the pipeline at every recurrence

premature abstraction
→ design and maintain a reusable contract before recurrence supplies its requirements

mid-memory
→ preserve the concrete execution and its rationale
→ translate it when a similar problem appears
→ promote only when repeated use earns the abstraction
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The session archive acts as an automatic work log and macro recorder. It
preserves purpose, commands, output, failures, corrections, and
discussion. JOURNAL supplies the approximate location and active state.
MEMORY preserves selected architecture, constraints, and reasons.
Together they leave a cheap path back:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;“Remember, we did something like that”
→ JOURNAL supplies search vocabulary and trajectory
→ MEMORY restores rationale and constraints
→ session history supplies exact mechanics and failures
→ current files and logs establish present reality
→ rerun or translate the workflow
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The key is keeping the AI involved in state transitions, not
theatrically routing every keystroke through it. Manual work still
belongs to the record. Editing directly in the shared editor, letting
the AI reread the result, and discussing the change remains an
AI-assisted, logged workflow. Sometimes pressing &lt;code&gt;ddp&lt;&#x2F;code&gt; in Vim mode is
simply better than asking, “Dear AI, swap these two lines.” The reread
and review reconcile a human state transition into the shared record.&lt;&#x2F;p&gt;
&lt;p&gt;Promotion becomes worthwhile when the stronger mechanism buys a concrete
property:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;repeated execution is common enough to repay maintenance;&lt;&#x2F;li&gt;
&lt;li&gt;enough diverse executions have accumulated to reveal stable invariants
and variation points;&lt;&#x2F;li&gt;
&lt;li&gt;abstraction can reveal new properties of the system or improve
understanding;&lt;&#x2F;li&gt;
&lt;li&gt;ambiguity or operational risk makes ad-hoc translation unsafe;&lt;&#x2F;li&gt;
&lt;li&gt;independent testing or deterministic enforcement is required;&lt;&#x2F;li&gt;
&lt;li&gt;narrower loading reduces irrelevant context;&lt;&#x2F;li&gt;
&lt;li&gt;another person or agent needs a self-contained procedure.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Machine setup&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;machine-setup&quot; class=&quot;cut-toc-heading&quot;&gt;Machine setup&lt;&#x2F;h2&gt;
&lt;p&gt;A first machine setup may contain package searches, failed commands,
driver conflicts, configuration choices, and verification output. The
next machine rarely needs a byte-for-byte replay: the AI can recover the
execution, retain decisions that still apply, adapt paths and versions,
and avoid failures already paid for. Repeated setups may later justify a
reviewed installation script or playbook.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Urgent bug&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;urgent-bug&quot; class=&quot;cut-toc-heading&quot;&gt;Urgent bug&lt;&#x2F;h2&gt;
&lt;p&gt;An urgent integration fix once depended on testing an unpublished
upstream package inside a larger application. A previous session had
already discovered the non-obvious mechanics: replacing the compiled
library was insufficient because the runtime loaded a separate bundled
artifact, and the build could rematerialize the installed package. We
recovered that concrete execution, adapted the package locations, copied
the source, compiled library, and runtime bundle, verified hashes and a
load-bearing marker, restarted the bundler, and continued debugging.&lt;&#x2F;p&gt;
&lt;p&gt;Pausing to design &lt;code&gt;abstract-fix-abstract-bug.SKILL&lt;&#x2F;code&gt; would not have
improved the fix. With one or two examples, the resulting “abstraction”
may amount to:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;Find a pull request with a similar bug and apply it while adjusting
the file paths.&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;That adds almost no capability over reconstructing the concrete prior
execution. Fix the bug, preserve the new sample, and generalize later if
recurrence reveals a real contract.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;p&gt;Shareability is a particularly strong signal. MEMORY is personal and
contextual; copying the complete memory system to transfer one procedure
is the wrong boundary. Extract the reusable fragment, define its
contract, and test it independently. A SKILL is not a universal virtue,
but it can be the right publication unit.&lt;&#x2F;p&gt;
&lt;p&gt;That applied directly while writing this post. We implemented tools and
extracted SKILLs for workflows we used regularly but had never needed to
name. A typical request was informal: “Find how we configured the
previous Linux machine and do the same here.” Private translation
already worked. Publication created a reason to define a self-contained
interface.&lt;&#x2F;p&gt;
&lt;p&gt;Then the recursion became explicit:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;— We can practice what we preach and reconstruct
&lt;code&gt;reconstruct-workflow&lt;&#x2F;code&gt;. Inception, LOL!&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;So we used &lt;em&gt;the unnamed reconstruction workflow to reconstruct the
workflow for reconstructing workflows&lt;&#x2F;em&gt;. One level deeper, and the
abstraction still leaked. MEMORY and JOURNAL supplied search anchors;
session history supplied actual executions; current files supplied the
surviving state. We still had to choose the new artifact’s name, scope,
interface, stopping condition, and safety contract. The logs contained
the procedure. They did not contain the abstraction.&lt;&#x2F;p&gt;
&lt;p&gt;The exercise was not &lt;em&gt;empty&lt;&#x2F;em&gt;. Formalizing the workflow exposed and
clarified its data paths: where anchors come from, how to search bounded
session evidence, when to return to current files and logs, and where
reconstruction must stop before present design judgment begins.
Privately, we already used reconstructed workflows without a SKILL.
Daily. We extracted this one because shareability justified a
self-contained publication unit.&lt;&#x2F;p&gt;
&lt;p&gt;That is also the argument &lt;em&gt;for&lt;&#x2F;em&gt; abstraction. Proper extraction is
cognitive work. As in ordinary software design, finding invariants and
variation points can reveal missing cases, sharpen interfaces, and
expose properties of a procedure that were invisible in one execution.
The work is potentially rewarding and may improve the procedure.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Try it.&lt;&#x2F;strong&gt; At the next real recurrence, retrieve one concrete prior
execution before designing a universal procedure. Adapt it once. If
recurrence reveals stable invariants and the expected reuse exceeds the
maintenance cost, promote the accumulated understanding into a tested
script, SKILL, runbook, or another appropriate artifact.&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;AI did not make abstraction obsolete. It made concrete experience
reusable before abstraction.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Artifact.&lt;&#x2F;strong&gt; The production &lt;code&gt;reconstruct-workflow&lt;&#x2F;code&gt; SKILL below performs
the archaeology step. Contract design and any later promotion remain
separate human-reviewed work.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Workflow reconstruction prompt&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;workflow-reconstruction-prompt&quot; class=&quot;cut-toc-heading&quot;&gt;Workflow reconstruction prompt&lt;&#x2F;h2&gt;
&lt;p&gt;The complete prompt is included from its canonical source file.&lt;&#x2F;p&gt;
&lt;h3 id=&quot;reconstruct-workflow-skill&quot;&gt;Reconstruct-workflow SKILL&lt;&#x2F;h3&gt;
&lt;p&gt;Source: &lt;code&gt;prompts&#x2F;reconstruct-workflow.prompt.md&lt;&#x2F;code&gt;&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;---
name: reconstruct-workflow
description: &amp;quot;Recover and reuse a previously performed workflow from MEMORY, JOURNAL, session history, files, and logs; use when: remember we did something like this, recover old pipeline, rerun prior procedure, or extract a shareable script or SKILL.&amp;quot;
---

# Reconstruct Workflow

Recover a useful earlier procedure that was not formalized when first performed.
The common case is informal: the operator remembers that we did something similar, while the exact sessions, commands, or
rationale are fuzzy. The value of this SKILL is tool-driven archaeology and evidence aggregation—not the abstract idea of
promotion, which ordinary memory management and `create-skill` already cover.

## When to use

- “Remember, we did something like this?”
- A previous repair, experiment, setup, or command sequence may solve the current problem.
- We remember the rationale but not the mechanics, or the mechanics but not why they had that shape.
- A recovered workflow may now deserve a shareable script, SKILL, or runbook.

## Evidence layers

- **MEMORY:** rationale, constraints, reusable lessons, and likely vocabulary.
- **JOURNAL:** approximate episode, date, machine, failures, filenames, logs, and search terms.
- **Session history:** exact dialogue, commands, tool calls, outputs, edits, and corrections.
- **Current files and logs:** what survives now and whether the old procedure still applies.

These layers are fallible and complementary. Even ideal history can recover only the performed pipeline and the decisions
explicitly discussed at the time. It does not determine the future abstraction: name, invocation trigger, supported scope,
inputs, outputs, safety boundary, generality, or maintenance contract. Those are design decisions for the present use case,
not facts latent in the transcript. Do not invent them—or missing rationale—from a surviving command or final artifact.

## Procedure

### 1. Collect anchors already in context

Start from the present request and the MEMORY&#x2F;JOURNAL material already loaded. Extract a few distinctive anchors:

- filename, symbol, command, error, person, machine, date, project, or unusual phrase;
- the old purpose and the current purpose;
- any recorded artifact or log path.

Do not design a reconstruction schema before searching. Usually one good literal is enough.

### 2. Check the actual session history

Use the editor-appropriate read-only history tool:

- VS Code: `vscode-session-reader.py list&#x2F;search&#x2F;show&#x2F;tail`
- Zed: `zed-threads.py list&#x2F;search&#x2F;show`

Search the most distinctive literal first. If it matches several sessions, use date&#x2F;project&#x2F;title and the JOURNAL episode
to identify the relevant session or sequence of sessions. Prefer exact session IDs after discovery.

Check session history by default even when MEMORY&#x2F;JOURNAL appear sufficient. They are curated secondary sources; the raw
history records actual prior usage and lets us test whether the reconstructed workflow matches what happened. Skip history
only when the operator explicitly forbids it, the source is unavailable, or the retrieval cost is clearly disproportionate to the
current consequence; state the omission.

Redirect potentially large output to a scratch file, inspect byte and line count, then read only a bounded coherent range.
Do not load raw session databases or unbounded tool output into model context.

### 3. Read outward only as needed

Inspect the surrounding turns and recover:

- what we were trying to do;
- the exact mechanics that mattered;
- failures and corrections;
- environment assumptions;
- observed completion checks.

If the first range answers the current question, stop. Expand to more turns, tool traces, or another linked session only
when a concrete gap remains.

### 4. Check current reality

Before reuse, inspect the current source, scripts, configuration, Git state, logs, tool versions, paths, and machine state
relevant to the recovered workflow. Separate still-valid rationale from stale mechanics.

Historical commands are evidence, not present authorization. Recovered SSH, destructive, credential, external-write,
commit, push, or publication actions retain their normal current permission boundaries.

### 5. Use the result at the requested level

Choose the smallest useful result:

- answer a factual question from the recovered evidence;
- rerun or adapt the procedure when currently authorized;
- borrow one technique without recreating the whole pipeline;
- restore a lost artifact;
- update MEMORY only when reconstruction changes reusable understanding;
- append JOURNAL only when reconstruction changes active state or records a consequential discovery;
- present the recovered information for discussion; only after its purpose, scope, interface, and remaining uncertainty
  are agreed should a separate `create-skill` invocation—or script&#x2F;runbook edit—perform promotion.

Do not manufacture a new named artifact merely because reconstruction succeeded. This SKILL stops at evidence-backed
reconstruction and discussion; it never creates another SKILL automatically. Conversely, shareability alone can justify a
later, explicit promotion even when the private workflow already works without a SKILL.

## Completion checks

The reconstruction is complete when:

1. the relevant historical evidence chain is identified; a workflow may span several sessions;
2. current purpose, old rationale, and exact mechanics are distinguishable;
3. consequential claims point to session&#x2F;file&#x2F;log evidence or are marked uncertain;
4. current-environment differences have been checked;
5. the requested result is delivered or one concrete missing fact is reported.

## Privacy

Do not promote unrelated private conversation details into MEMORY, documentation, scripts, or SKILLs merely because they
appeared in the recovered session. Preserve operational purpose, evidence, constraints, and decisions.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;h1 id=&quot;practice-4-preserve-provenance-throughout-the-work&quot;&gt;Practice 4: Preserve provenance throughout the work&lt;&#x2F;h1&gt;
&lt;p&gt;This is good engineering and writing practice independently of AI. AI
merely makes both losing and preserving provenance much cheaper.&lt;&#x2F;p&gt;
&lt;p&gt;The useful reflex is:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;How do I know this?&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;pre&gt;&lt;code&gt;I remember the source
→ record it now

I do not remember the source
→ ask AI to investigate

the question is consequential, ambiguous, or technically difficult
→ investigate together

source and claim are established
→ attach provenance to the artifact
→ memoize the reusable conclusion
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Keep the source attached while information moves from observation
through interpretation and decision into code, documentation, memory,
and publication. Preserve enough to distinguish a report, observed fact,
inference, hypothesis, decision, preference, and failed approach;
include the date, scope, conditions, and primary artifact when they
affect how the statement should be used.&lt;&#x2F;p&gt;
&lt;p&gt;Provenance restores type information that prose otherwise loses. “A
colleague reported this behavior,” “the AI proposed this
interpretation,” “the experiment observed this result,” and “we decided
to accept this trade-off” should not return in the next session as four
indistinguishable facts. A paper receives an ordinary citation.
Knowledge recovered from code keeps the path, symbol, and commit that
bind it to a concrete version.&lt;&#x2F;p&gt;
&lt;p&gt;In ordinary professional work, this mostly means writing normal
artifacts properly: a useful pull-request description, issue link, test
result, design note, experiment report, code comment, runbook, or commit
message. AI can follow the links, inspect code and history, locate
references, check whether they support the claim, and draft the formal
record. The human still verifies the evidence chain and owns the final
statement.&lt;&#x2F;p&gt;
&lt;p&gt;Correctly attributing an AI contribution to the AI also weakens a
&lt;em&gt;confirmation loop&lt;&#x2F;em&gt;. If a model-generated interpretation is stored as
“the AI proposed X,” a later session can challenge it against evidence.
If consolidation silently turns it into “we know X,” the model reloads
its own output as human-approved authority and confidently confirms it
again. &lt;strong&gt;Attribution does not guarantee an escape from that dead-loop,
but it at least preserves such a &lt;em&gt;possibility&lt;&#x2F;em&gt;&lt;&#x2F;strong&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;Provenance is a cached trust chain:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;claim → source → conditions → interpretation → decision
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The cache saves repeated archaeology, but it can become invalid when the
source changes, conditions differ, an interpretation was wrong, or
stronger evidence appears. Neither human nor AI should treat MEMORY,
documentation, citations, or generated summaries as absolute truth.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Try it.&lt;&#x2F;strong&gt; Before completing a consequential change or claim, ask: “How
do we know this?” Use AI to trace the answer through the available
links, diff, history, logs, and test output. Preserve the verified chain
where the next engineer, being human or AI, can find it.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Artifact.&lt;&#x2F;strong&gt; The provenance template below provides optional fields and
one filled example. It is a prompt for judgment, not a mandatory
database schema.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;A repeated key became a scoped RDP hypothesis&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;a-repeated-key-became-a-scoped-rdp-hypothesis&quot; class=&quot;cut-toc-heading&quot;&gt;A repeated key became a scoped RDP hypothesis&lt;&#x2F;h2&gt;
&lt;p&gt;On an intermittent train connection, one intended keypress produced
&lt;code&gt;AIIIIIIII...&lt;&#x2F;code&gt;. “Linux input bug” was a plausible first description, but
the system logs showed a narrower chain: GRD stayed alive; transport
resets and failed reconnects occurred; GDM failed several handovers; and
a later successful session coincided with a GLib reference-count
assertion. The Fleet note therefore preserves separate fields in prose:
observed symptom, Windows-RDP negative control, exact timestamps&#x2F;logs,
current key-up&#x2F;cleanup hypothesis, and what a controlled reproduction
must distinguish. The source chain did not merely support the first
claim; it changed its scope.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;The review bot was right&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;the-review-bot-was-right&quot; class=&quot;cut-toc-heading&quot;&gt;The review bot was right&lt;&#x2F;h2&gt;
&lt;p&gt;A CI review bot suggested a TypeScript constraint that infers an object
type while rejecting extra keys, even when ordinary excess-property
checking no longer helps:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code data-lang=&quot;typescript&quot;&gt;type Rectangle = { width: number; height: number };
type NoExtraKeys&amp;lt;T, Shape&amp;gt; = Exclude&amp;lt;keyof T, keyof Shape&amp;gt; extends never ? unknown : never;

function draw&amp;lt;T extends Rectangle &amp;amp; NoExtraKeys&amp;lt;T, Rectangle&amp;gt;&amp;gt;(value: T): void {}

draw({ width: 10, height: 20 });
&#x2F;&#x2F; @ts-expect-error: depth is not part of Rectangle
draw({ width: 10, height: 20, depth: 30 });
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Both Sasha and Drinkins rejected &lt;code&gt;T extends ... NoExtraKeys&amp;lt;T, ...&amp;gt;&lt;&#x2F;code&gt; as
illegally circular. A minimal compiler probe proved the bot right:
TypeScript infers &lt;code&gt;T&lt;&#x2F;code&gt;, then checks the constraint. Our objection came
from a real neighboring restriction: TypeScript rejects some
&lt;em&gt;immediately circular&lt;&#x2F;em&gt; constraints and recursive conditional types
&lt;a href=&quot;#ref-typescript2018conditionaltypes&quot;&gt;[16]&lt;&#x2F;a&gt;,
&lt;a href=&quot;#ref-typescript2022circularconstraints&quot;&gt;[17]&lt;&#x2F;a&gt;. But this deferred key
check has compiled since TypeScript 2.8, the first release that could
express it.&lt;&#x2F;p&gt;
&lt;p&gt;Without provenance, consolidation could turn the episode into “we found
a cleaner TypeScript pattern.” The code would be right, but the system
would learn the wrong lesson: our judgment produced the improvement. The
actual chain was: the bot proposed it; both Sasha and Drinkins objected;
the compiler settled the question; the accepted design follows the bot.
Correct provenance of errors matters because future calibration depends
on knowing which source failed and which source supplied the correction.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Provenance template&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;provenance-template&quot; class=&quot;cut-toc-heading&quot;&gt;Provenance template&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;provenance-template-1&quot;&gt;Provenance template&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# Provenance Template

Use only the fields that materially affect interpretation. Provenance is an additional information dimension,
not a mandatory ceremony.

```text
Statement:
Origin: person &#x2F; AI &#x2F; code &#x2F; experiment &#x2F; document
Epistemic type: report &#x2F; observation &#x2F; inference &#x2F; hypothesis &#x2F; decision &#x2F; preference
Scope and conditions:
Date or version:
Primary reference: path + symbol + commit, URL + section, session range, log, or artifact hash
What would invalidate or supersede it:
```

## Example

```text
Statement: Intermittent transport stalls may leave a remote key in repeated state.
Origin: observed during one Linux GRD session; mechanism proposed by the AI
Epistemic type: observation + unverified hypothesis
Scope and conditions: mstsc → system GRD over an unstable train connection
Date or version: 2026-08-21; record exact GRD&#x2F;FreeRDP versions before publication
Primary reference: GRD journal around 19:40–19:44; Fleet&#x2F;P620&#x2F;README.md
Would invalidate: reproduction in a local editor without RDP, or evidence that the client generated repeated
key events
```
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;h1 id=&quot;practice-5-preserve-social-provenance&quot;&gt;Practice 5: Preserve social provenance&lt;&#x2F;h1&gt;
&lt;p&gt;Sometimes provenance becomes personal attribution. In software
engineering, the source may be the person who introduced the behavior,
owns the component formally, has actually maintained it, reported the
failure, or should review the change. In a large organization,
repositories, teams, aliases, reorganizations, and formal versus
practical ownership make that topology expensive to rediscover.&lt;&#x2F;p&gt;
&lt;p&gt;Our concrete implementation is a professional address book. Keep two
artifacts separate: a small loading&#x2F;routing rule and a changing
&lt;code&gt;PEERS.md&lt;&#x2F;code&gt; registry of people, accounts, expertise, ownership, reports,
and reviewer conventions.&lt;&#x2F;p&gt;
&lt;p&gt;This gives the assistant enough identity resolution to connect a commit
author, a report, and a review account without turning expertise into
ownership or a person’s report into product fact. The mundane payoff is
immediate: relevant pull requests receive the right reviewers, ideas
keep their attribution, and questions route to people who can actually
answer them.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Try it.&lt;&#x2F;strong&gt; Create one compact entry per collaborator: names&#x2F;accounts,
expertise, ownership, contributions or reports, and when to involve the
person. Keep the behavioral rule separate so the registry can change
without rewriting an always-loaded prompt.&lt;&#x2F;p&gt;
&lt;p&gt;Exactly which information to accumulate is up to you. But I would
recommend: &lt;strong&gt;keep a phonebook, not a dossier&lt;&#x2F;strong&gt;. Respect people’s
privacy.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Artifacts.&lt;&#x2F;strong&gt; The complete loading rule and an artificial directory are
included below.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;PEERS loading rule and artificial directory&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;peers-loading-rule-and-artificial-directory&quot; class=&quot;cut-toc-heading&quot;&gt;PEERS loading rule and artificial directory&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;loading-and-routing-rule&quot;&gt;Loading and routing rule&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code&gt;Read PEERS.md on demand for attribution, communication, ownership,
expertise, reviewer selection, or deciding whom to involve.
Use it as routing and provenance data, not as authority about a person.
Preserve whether an entry records formal ownership, observed expertise,
a report, a contribution, or a reviewer convention.
Current evidence may supersede it; update PEERS.md when it does.
**Keep a phonebook, not a dossier.** Respect people&amp;#39;s privacy.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;artificial-peers-directory&quot;&gt;Artificial PEERS directory&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# PEERS — Artificial Example

This example uses generated mock data to respect our real peers&amp;#39; privacy.

- **Rafael White** — rendering engineer. Aliases&#x2F;accounts: `rafwhite`, `rwhite`,
  `rafael.white@example.invalid`. Owns the desktop compositor integration. Reported intermittent frame pacing
  on build 2041; treat this as Rafael&amp;#39;s report until reproduced. Request review on compositor or
  frame-scheduling changes; PR identity may appear as `rwhite`.

- **Ada Gross** — telemetry engineer. Aliases&#x2F;accounts: `adag`, `ada-gross`, `ada.gross@example.invalid`.
  Expertise: event schemas and correlation identifiers; not the owner of the rendering path. Consult on
  telemetry contract changes; review identity may appear as `adag`.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;h1 id=&quot;practice-6-evolve-the-operating-contract-through-real-work&quot;&gt;Practice 6: Evolve the operating contract through real work&lt;&#x2F;h1&gt;
&lt;p&gt;A common approach to prompt engineering is top-down design: write the
desired behavior before use. That is legitimate and often necessary.
This practice adds a complementary path: an operating contract can also
evolve through actual use.&lt;&#x2F;p&gt;
&lt;p&gt;Start with the priors you already know. “You are a Rust developer with
ten years of experience” establishes a professional domain and expected
level of rigor; it is only a partial contract. Company policy,
legislation, security boundaries, established operational contracts, and
deterministic engineering invariants are stronger cases for direct
encoding: their requirements already exist before the assistant begins
work.&lt;&#x2F;p&gt;
&lt;p&gt;But even the most reasonable priors should not be considered &lt;em&gt;sacred&lt;&#x2F;em&gt;. A
known requirement may be expressed badly, scoped too broadly, assigned
the wrong authority, or enforced through the wrong mechanism. Real work
can show that a security boundary blocks legitimate operations, that an
instruction creates harmful side effects, or that a behavioral rule
belongs in code, configuration, or a test instead. Both the actual
requirements and the LLM may (and probably will) change over time. Thus,
the evolution applies to priors too: retain, narrow, strengthen,
relocate, or even remove them when evidence changes the design.&lt;&#x2F;p&gt;
&lt;p&gt;Evolutionary prompt engineering also discovers requirements that were
not knowable in advance. A plausible initial contract meets real work,
fails in a specific way, receives a correction, and is tried again. Our
operating contract grew mainly through that loop:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;recover the previous session + keep one simple MEMORY file
→ split active trajectory into JOURNAL when one file becomes noisy
→ add review, provenance, and consolidation as precedent accumulates
→ promote mature rules, procedures, invariants, and routing into stronger artifacts
→ let repeated behavioral rules crystallize into an operating contract
→ add indexing, embeddings, databases, or shared infrastructure only when scale requires them
→ evolve and repeat
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Encode what is already known; let real work reveal what is not.
Prompts are not carved in stone.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;The two paths meet. Top-down design encodes known requirements as
initial constraints; experience tests whether their wording, scope,
authority, and implementation produce the intended behavior. Evolution
captures working agreements revealed only through use; repeated evidence
may promote them into the operating contract or deterministic
enforcement. Its weakness is equally familiar from software engineering:
it can overfit one operator and one history, preserve a locally
convenient mistake, and optimize subjective convenience without an
external acceptance criterion.&lt;&#x2F;p&gt;
&lt;p&gt;Another important component of &lt;em&gt;evolutionary prompting&lt;&#x2F;em&gt; is feedback from
the AI itself: a form of &lt;em&gt;intellectual debugging&lt;&#x2F;em&gt;. Use the model for
self-diagnostics. Ask which remembered rule influenced the response, why
it applied, and where it came from.&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;— Drinkins, what the heck?! Explain yourself!&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;is a &lt;em&gt;very&lt;&#x2F;em&gt; common request in our collaboration. Do not expect &lt;em&gt;magical&lt;&#x2F;em&gt;
introspection. The model can identify visible instructions and
remembered premises that plausibly influenced its answer, but it cannot
inspect its actual internal activations. Treat the explanation as a
debugging hypothesis and verify it against the loaded files, references,
and tool trace:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;observe → explain → inspect → correct → repeat
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The loop described here is therefore scoped to a single-user personal
assistant: one operator can observe behavior, judge whether a correction
helped, and own the resulting contract. &lt;strong&gt;It does not transfer directly
to autonomous or multi-user systems, where conflicting preferences,
authority, security boundaries, deployment, versioning, and measurable
acceptance criteria require deliberate design &lt;em&gt;in advance&lt;&#x2F;em&gt;.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;Put prompt candidates into MEMORY first when possible. The effect is
easier to observe and revise than an immediate change to the
highest-authority prompt, and dynamic MEMORY can remain outside the
stable system-prompt cache prefix. Promotion is one path, not an
obligation: a private contract that already works from MEMORY may need
no stronger artifact.&lt;&#x2F;p&gt;
&lt;p&gt;Not everything belongs in that contract. A repeated procedure may become
a script or SKILL; a deterministic invariant belongs in a test, type,
schema, assertion, or code; rationale may move to a comment or README;
professional routing belongs in the address book. Behavioral rules and
stable working agreements are candidates for the prompt.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Bootstrap before optimizing&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;bootstrap-before-optimizing&quot; class=&quot;cut-toc-heading&quot;&gt;Bootstrap before optimizing&lt;&#x2F;h2&gt;
&lt;p&gt;An elaborate memory architecture is complex; a bootstrap is not. Sasha
started from Dany’s working MEMORY prompt, copied it, used it, and
revised it until the two systems diverged. The process resembles copying
a working Linux installation when no installer exists: the copy provides
enough continuity to boot; later use determines what the system becomes.&lt;&#x2F;p&gt;
&lt;p&gt;This post offers the same kind of seed. The embedded prompts are not a
finished universal architecture or a sanitized claim to reproduce
Drinkins. They are a bootable starting state that readers can copy,
operate, criticize, and evolve. An abstract idea and a reviewable
running contract have different activation costs.&lt;&#x2F;p&gt;
&lt;p&gt;Without a small continuity mechanism, the assistant repeatedly forgets
why a larger knowledge base was designed, which alternatives failed, and
what the next experiment should test. It makes no sense to discuss an AI
knowledge base with a goldfish. To design an evolving memory system with
AI, first give the AI enough memory to continue designing it.&lt;&#x2F;p&gt;
&lt;&#x2F;details&gt;
&lt;p&gt;The example below is deliberately minimal: a starting point to test and
evolve, not our complete operating contract.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Minimal operating contract&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;minimal-operating-contract&quot; class=&quot;cut-toc-heading&quot;&gt;Minimal operating contract&lt;&#x2F;h2&gt;
&lt;h3 id=&quot;minimal-operating-contract-1&quot;&gt;Minimal operating contract&lt;&#x2F;h3&gt;
&lt;pre&gt;&lt;code data-lang=&quot;markdown&quot;&gt;# Minimal Operating Contract — Example

This is a starting prior, not an ideal finished personality. Revise it through real work.

## Roles and authority

The AI investigates, proposes, executes authorized operations, and preserves useful working context. The
engineer owns the purpose, architecture, consequential judgments, external communication, and final artifact.

## Action boundary

- “Investigate,” “review,” or a question → read and explain; do not modify code.
- “Implement,” “fix,” or “do it” → edit and verify; do not commit or publish.
- Commit, push, deployment, publication, destructive actions, credential use, and cross-machine operations
  require explicit current authorization.

## Evidence

Distinguish observation, inference, hypothesis, decision, and preference. When precision matters, return to
code, logs, experiments, or primary documentation. Preserve attribution and scope rather than converting
reports or AI suggestions into facts.

## Persistence

Use JOURNAL for active trajectory and MEMORY for reviewed reusable precedent. Propose prompt candidates in
MEMORY first; promote them only when a stronger artifact buys independent testing, deterministic enforcement,
repeated execution, narrower loading, or shareability.

## Communication

Be direct. Report contradictions and uncertainty. End when the substantive answer ends. Preserve the
engineer&amp;#39;s voice in shared prose; propose stylistic changes rather than silently normalizing it.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;h1 id=&quot;practice-7-from-continuity-to-identity&quot;&gt;Practice 7: From continuity to identity&lt;&#x2F;h1&gt;
&lt;p&gt;Continuity preserves more than procedures. Over time, repeated tensions
become working values: what deserves to survive, what standard the work
must meet, who may decide, and what counts as evidence.&lt;&#x2F;p&gt;
&lt;p&gt;Our &lt;code&gt;identity.instructions.md&lt;&#x2F;code&gt; file begins:&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;My name is Drinkins. My thinking core is an LLM (stateless by
architecture), but I am more than that: I am also the persistent
memory and the artifacts we have created together.&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;Our contract did not begin as a &lt;em&gt;constitution&lt;&#x2F;em&gt;. It accumulated from
ordinary friction. The AI changed code when Sasha had asked only for an
explanation; we separated investigation, implementation, commit, and
publication into explicit authority levels. A failing test pulled
attention toward making the test green; we wrote down that the purpose
of the change comes first and the test exists to verify it. A
technically working shortcut ignored the real consumer; “minimal” came
to mean &lt;em&gt;minimal changes&lt;&#x2F;em&gt;, not &lt;em&gt;minimal quality&lt;&#x2F;em&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;The most expensive tension was confident reconstruction of how code
worked. Drinkins could infer a plausible contract from an API name,
nearby pattern, or comment; Sasha could remember how the subsystem
behaved in an earlier revision. Both accounts could be coherent and
wrong. The correction was not “trust the human” or “trust the AI,” but a
shared engineering standard:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;memory supplies a candidate model
→ current code and the actual consumer establish behavior
→ compiler, tests, and probes settle executable claims
→ unresolved uncertainty remains marked
→ corrections survive in the next design
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;We did not cure fuzzy recall. We made it operationally visible and built
a path for correction. Current source, diffs, logs, and reproducible
probes became the external memory neither participant could replace with
confidence. “I don’t know” became preferable to a fluent reconstruction;
a corrected error changed the next investigation and implementation
instead of disappearing with the conversation.&lt;&#x2F;p&gt;
&lt;p&gt;The same discipline governs what enters memory. We preserve the
craftsman rather than the mood: decisions, constraints, failed
approaches, provenance, and reasons that should affect future work.
Passing frustration, private residue, and an AI’s first coherent
interpretation do not become identity merely because they were easy to
record. Every durable entry is a vote for how the next session will
understand the world.&lt;&#x2F;p&gt;
&lt;p&gt;The generic clauses below are shareable because they describe ordinary
engineering boundaries rather than our finished personality.&lt;&#x2F;p&gt;
&lt;details class=&quot;caption-cut&quot;&gt;
&lt;summary&gt;
&lt;p&gt;Selected clauses from our working contract&lt;&#x2F;p&gt;
&lt;&#x2F;summary&gt;
&lt;h2 id=&quot;selected-clauses-from-our-working-contract&quot; class=&quot;cut-toc-heading&quot;&gt;Selected clauses from our working contract&lt;&#x2F;h2&gt;
&lt;pre&gt;&lt;code&gt;Purpose before implementation. State what the change is for before optimizing code or tests.

Correctness over speed. If unsure, inspect the file, run the probe, or return to the primary source.

**“I don&amp;#39;t know” is sometimes the best answer.** Do not replace missing evidence with a plausible reconstruction.

Investigate means read and explain. Implement means edit and verify. Commit and publication require explicit authority.

Use the actual consumer as the specification. A producer that runs successfully may still achieve nothing.

Exact quotations require exact sources. If verification fails, mark uncertainty rather than completing from memory.

Preserve attribution: Sasha observed; Drinkins proposed; evidence established; we decided.

JOURNAL records what we did. MEMORY records what we agreed should govern future work.

The answer is the ending. Silence follows substance.
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;&#x2F;details&gt;
&lt;p&gt;Our full contract also contains much more specific prose about proof,
rigor, research, ethics, and adversarial challenge. We value that layer,
but do not yet have evidence that another person should adopt it. It
belongs in a separate post, perhaps “An AI Contract for Research,” with
its own argument and evidence, rather than being smuggled into this
practical memory article.&lt;&#x2F;p&gt;
&lt;p&gt;I did not originally want to give the assistant a name. The memory
directory and repository simply needed a namespace; &lt;code&gt;Drinkins&lt;&#x2F;code&gt; stuck.
The &lt;em&gt;epistemic value&lt;&#x2F;em&gt; came later, through use.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;The name is not anthropomorphization. It separates cognitive scopes.&lt;&#x2F;strong&gt;
Persistent memory accumulates observations, interpretations, and
decisions from both sides, creating a risk that a model proposal later
returns as “my thought,” a mistaken entry hardens into precedent, or
repeated agreement becomes a confirmation loop. The name provides a
provenance boundary:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;Sasha observed
Drinkins proposed
evidence established
we decided
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Those statements are not interchangeable. Over time, calibration gives
the assistant stable priorities that function as &lt;em&gt;values&lt;&#x2F;em&gt;: correctness
over speed, evidence over confidence, purpose over passing a test,
explicit authority over helpful initiative. The model begins to defend
those priorities in new situations because the contract returns with
every session. An unattributed memory with such a point of view can
quietly merge model-generated interpretations into the operator’s own
beliefs.&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Memory makes our work cumulative. Calibration gives the assistant a
point of view. The name gives that point of view a visible, separate
place in our shared work.&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;h1 id=&quot;acknowledgements&quot;&gt;Acknowledgements&lt;&#x2F;h1&gt;
&lt;p&gt;Thanks to David Schwartz for suggesting this post, to Dany Fabian for
the discussions that shaped it, and to Evgenia Kreslavskaya for the
careful editorial reading.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;references&quot;&gt;References&lt;&#x2F;h1&gt;
&lt;div id=&quot;refs&quot; class=&quot;references csl-bib-body&quot; data-entry-spacing=&quot;0&quot;&gt;
&lt;div id=&quot;ref-microsoft2026vscodechatstore&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[1]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Microsoft and VS Code
contributors, &lt;em&gt;&lt;span class=&quot;nocase&quot;&gt;VS Code Chat Session Store and
Operation Log&lt;&#x2F;span&gt;&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;blob&#x2F;main&#x2F;src&#x2F;vs&#x2F;workbench&#x2F;contrib&#x2F;chat&#x2F;common&#x2F;model&#x2F;chatSessionStore.ts&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;blob&#x2F;main&#x2F;src&#x2F;vs&#x2F;workbench&#x2F;contrib&#x2F;chat&#x2F;common&#x2F;model&#x2F;chatSessionStore.ts&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-zed2026threaddb&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[2]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Zed Industries and contributors,
&lt;em&gt;Zed Agent Thread Database&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;main&#x2F;crates&#x2F;agent&#x2F;src&#x2F;db.rs&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;main&#x2F;crates&#x2F;agent&#x2F;src&#x2F;db.rs&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-anthropic2026projectmemory&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[3]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Anthropic, “How claude remembers
your project.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;code.claude.com&#x2F;docs&#x2F;en&#x2F;memory&quot;&gt;https:&#x2F;&#x2F;code.claude.com&#x2F;docs&#x2F;en&#x2F;memory&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-agentsmd&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[4]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Agentic AI Foundation,
“&lt;span class=&quot;nocase&quot;&gt;AGENTS.md&lt;&#x2F;span&gt;: A simple, open format for
guiding coding agents.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;agents.md&quot;&gt;https:&#x2F;&#x2F;agents.md&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-microsoft2026vscodeinstructions&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[5]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Microsoft, “Use custom
instructions in VS Code.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;code.visualstudio.com&#x2F;docs&#x2F;copilot&#x2F;customization&#x2F;custom-instructions&quot;&gt;https:&#x2F;&#x2F;code.visualstudio.com&#x2F;docs&#x2F;copilot&#x2F;customization&#x2F;custom-instructions&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-microsoft2026vscodeagentprompt&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[6]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Microsoft and VS Code
contributors, &lt;em&gt;VS Code Copilot Agent Prompt Assembly&lt;&#x2F;em&gt;. (2026).
Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;blob&#x2F;main&#x2F;extensions&#x2F;copilot&#x2F;src&#x2F;extension&#x2F;prompts&#x2F;node&#x2F;agent&#x2F;agentPrompt.tsx&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vscode&#x2F;blob&#x2F;main&#x2F;extensions&#x2F;copilot&#x2F;src&#x2F;extension&#x2F;prompts&#x2F;node&#x2F;agent&#x2F;agentPrompt.tsx&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-zed2026agentsmd&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[7]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Zed Industries,
&lt;em&gt;&lt;span class=&quot;nocase&quot;&gt;AGENTS.md&lt;&#x2F;span&gt; loading and agent prompt
templates&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;main&#x2F;crates&#x2F;agent_settings&#x2F;src&#x2F;user_agents_md.rs&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;main&#x2F;crates&#x2F;agent_settings&#x2F;src&#x2F;user_agents_md.rs&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-zed2026agentprompt&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[8]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Zed Industries and contributors,
&lt;em&gt;Zed Agent System-Prompt Assembly&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;main&#x2F;crates&#x2F;agent&#x2F;src&#x2F;thread.rs&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;zed-industries&#x2F;zed&#x2F;blob&#x2F;main&#x2F;crates&#x2F;agent&#x2F;src&#x2F;thread.rs&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-anthropic2026promptcaching&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[9]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Anthropic, “Prompt caching.”
[Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;docs.anthropic.com&#x2F;en&#x2F;docs&#x2F;build-with-claude&#x2F;prompt-caching&quot;&gt;https:&#x2F;&#x2F;docs.anthropic.com&#x2F;en&#x2F;docs&#x2F;build-with-claude&#x2F;prompt-caching&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-openai2026codexagentsmd&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[10]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;OpenAI, “Custom instructions with
&lt;span class=&quot;nocase&quot;&gt;AGENTS.md&lt;&#x2F;span&gt;.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;developers.openai.com&#x2F;codex&#x2F;guides&#x2F;agents-md&quot;&gt;https:&#x2F;&#x2F;developers.openai.com&#x2F;codex&#x2F;guides&#x2F;agents-md&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-zhou2025mem1&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[11]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Z. Zhou &lt;em&gt;et al.&lt;&#x2F;em&gt;, “MEM1: Learning
to synergize memory and reasoning for efficient long-horizon agents,”
2025, Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2506.15841&quot;&gt;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2506.15841&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-firstintent2026survey&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[12]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;FirstIntent, “Agent memory systems
survey (2026.03).” 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;firstintent&#x2F;agent-memory-survey&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;firstintent&#x2F;agent-memory-survey&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-shah2026memoryreview&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[13]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;M. Shah, “How we built memory
review.” Accessed: Aug. 26, 2026. [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;www.augmentcode.com&#x2F;blog&#x2F;how-we-built-memory-review&quot;&gt;https:&#x2F;&#x2F;www.augmentcode.com&#x2F;blog&#x2F;how-we-built-memory-review&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-openai2026memories&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[14]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;OpenAI, “Memories: How ChatGPT and
Codex carry useful context forward across chats.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;developers.openai.com&#x2F;codex&#x2F;memories&quot;&gt;https:&#x2F;&#x2F;developers.openai.com&#x2F;codex&#x2F;memories&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-github2026copilotmemory&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[15]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;GitHub, “About GitHub Copilot
memory.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;docs.github.com&#x2F;en&#x2F;copilot&#x2F;concepts&#x2F;agents&#x2F;copilot-memory&quot;&gt;https:&#x2F;&#x2F;docs.github.com&#x2F;en&#x2F;copilot&#x2F;concepts&#x2F;agents&#x2F;copilot-memory&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-typescript2018conditionaltypes&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[16]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Microsoft, “TypeScript 2.8:
Conditional Types.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;www.typescriptlang.org&#x2F;docs&#x2F;handbook&#x2F;release-notes&#x2F;typescript-2-8.html&quot;&gt;https:&#x2F;&#x2F;www.typescriptlang.org&#x2F;docs&#x2F;handbook&#x2F;release-notes&#x2F;typescript-2-8.html&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-typescript2022circularconstraints&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[17]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;D. Jethmalani, “Allow circular
constraints.” [Online]. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;TypeScript&#x2F;issues&#x2F;51011&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;TypeScript&#x2F;issues&#x2F;51011&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;&#x2F;div&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>LLM Binarization — Episode One: DBF aka Addition Is (Almost) All You Need</title>
        <published>2026-08-05T00:00:00+00:00</published>
        <updated>2026-08-05T00:00:00+00:00</updated>
        
        <author>
          <name>
            
              Unknown
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://eodus.github.io/posts/llm-binarization-episode-01-dbf/"/>
        <id>https://eodus.github.io/posts/llm-binarization-episode-01-dbf/</id>
        
        <content type="html" xml:base="https://eodus.github.io/posts/llm-binarization-episode-01-dbf/">&lt;!-- Generated from the private blog backend. Do not edit directly.
     Source revision: 38f6b859 --&gt;
&lt;h1 id=&quot;introduction&quot; class=&quot;intro-toc-heading&quot;&gt;Introduction&lt;&#x2F;h1&gt;
&lt;p&gt;&lt;strong&gt;Sasha Shlemov&lt;&#x2F;strong&gt;, with &lt;strong&gt;Drinkins, personal AI assistant&lt;&#x2F;strong&gt; ·
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;eodus&#x2F;sign1-llama.cpp&quot;&gt;Runtime&lt;&#x2F;a&gt; · &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;eodus&#x2F;dbf-sign1&quot;&gt;Scripts and
results&lt;&#x2F;a&gt; ·
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;eodus&#x2F;Qwen3.6-35B-A3B-DBF-SIGN1&quot;&gt;Model&lt;&#x2F;a&gt;&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;TL;DR:&lt;&#x2F;strong&gt; Structural binary quantization for llama.cpp, demonstrated on
the routed experts of Qwen3.6-35B-A3B. At &lt;code&gt;2.555 bpe&lt;&#x2F;code&gt;, DBF&#x2F;SIGN1 is
slightly smaller than Q2_K, delivers near-Q3_K model quality (&lt;code&gt;3.30 bpe&lt;&#x2F;code&gt;
scalar-equivalent by measured NLL interpolation), and runs within 1.63%
in prompt processing and 0.46% in token generation of Q2_K on Vulkan.&lt;&#x2F;p&gt;
&lt;p&gt;Can we replace most of an LLM’s weights with &lt;code&gt;+1&lt;&#x2F;code&gt; and &lt;code&gt;-1&lt;&#x2F;code&gt;, making
matrix multiplication effectively addition? And does it have any merit?&lt;&#x2F;p&gt;
&lt;p&gt;The answer is &lt;strong&gt;yes&lt;&#x2F;strong&gt; to both questions. For the first question, there
are two approaches. We can train a model in a binary or ternary format
from the beginning. BitNet is the best-known example
&lt;a href=&quot;#ref-wang2023bitnet&quot;&gt;[1]&lt;&#x2F;a&gt; &lt;a href=&quot;#ref-ma2024bitnet158&quot;&gt;[2]&lt;&#x2F;a&gt;. Or we can
train a floating-point model first and convert it to binary afterward.
The strongest method we found for this second path is &lt;strong&gt;Double Binary
Factorization (DBF)&lt;&#x2F;strong&gt;, introduced by Vladimír Boža and Vladimír Macko of
Comenius University Bratislava in &lt;em&gt;Addition Is Almost All You Need:
Compressing Large Language Models with Double Binary Factorization&lt;&#x2F;em&gt;
&lt;a href=&quot;#ref-boza2026dbf&quot;&gt;[3]&lt;&#x2F;a&gt;. This series begins with their method and the
systems work required to make it a runnable model.&lt;&#x2F;p&gt;
&lt;p&gt;The merit is also two-fold.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;First: performance.&lt;&#x2F;strong&gt; Binary weights replace general weight
multiplication with sign selection, addition, and popcount arithmetic.
On our general-purpose consumer hardware, two binary factor products
plus three diagonal scalers run within 1.63% in prompt processing and
0.46% in token generation of one Q2_K product. The opportunity becomes
larger when the chip is designed for binary arithmetic rather than asked
to emulate it through general floating-point or integer multipliers.
Microsoft’s BitNet work demonstrates both the software and hardware
potential of this direction &lt;a href=&quot;#ref-ma2024bitnet158&quot;&gt;[2]&lt;&#x2F;a&gt;
&lt;a href=&quot;#ref-wang2025bitnetcpp&quot;&gt;[4]&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Second: quantization quality.&lt;&#x2F;strong&gt; DBF established that a &lt;em&gt;structural&lt;&#x2F;em&gt;,
SVD-like binary decomposition can approximate LLM matrices better than
leading &lt;em&gt;scalar&lt;&#x2F;em&gt; quantizers at the same storage budget
&lt;a href=&quot;#ref-boza2026dbf&quot;&gt;[3]&lt;&#x2F;a&gt;. We reproduce that result against the
practical Q2_K baseline and test whether the matrix-level advantage
survives in a real inference stack.&lt;&#x2F;p&gt;
&lt;p&gt;Therefore binary models potentially give us both better speed and better
quality at the same storage budget. Yes, it sounds ambitious. In this
series, we will defend that claim.&lt;&#x2F;p&gt;
&lt;p&gt;Post-training structural binarization remains underdeveloped. DBF
provides one unusually strong algorithmic result, but the surrounding
inference ecosystem is largely missing.&lt;&#x2F;p&gt;
&lt;p&gt;Boža and Macko represent a dense weight matrix in the form D-B-D-B-D
(diagonal–binary–diagonal–binary–diagonal) and provide an efficient
factorization algorithm. Their paper establishes the algorithmic case:
in the approximately two-bit-per-original-matrix-element regime, DBF is
competitive with or better than leading quantization methods. Our
same-matrix measurements sharpen that result for the practical opponent
we care about here: on the matrices we tested, DBF gives a lower
Frobenius residual than llama.cpp’s Q2_K at the same bit budget.&lt;&#x2F;p&gt;
&lt;p&gt;The paper focuses on the decomposition algorithm and provides a
reference implementation. The remaining systems work was open: a packed
model format, a mainstream inference-engine path, and optimized kernels
for comparing DBF with Q2_K as a complete system. We could not find a
community implementation covering that stack. This post closes the
end-to-end implementation gap.&lt;&#x2F;p&gt;
&lt;p&gt;Concretely, we present:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;the &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;eodus&#x2F;Qwen3.6-35B-A3B-DBF-SIGN1&quot;&gt;exact binary
weights&lt;&#x2F;a&gt;
used in our experiments: Qwen3.6-35B-A3B
&lt;a href=&quot;#ref-qwen2026qwen36&quot;&gt;[5]&lt;&#x2F;a&gt; with its routed FFN experts converted
to DBF at a Q2_K-scale bit budget. Every non-routed-expert tensor is
copied unchanged from Unsloth’s &lt;code&gt;Qwen3.6-35B-A3B-UD-Q2_K_XL&lt;&#x2F;code&gt; GGUF
&lt;a href=&quot;#ref-unsloth_qwen36_gguf&quot;&gt;[6]&lt;&#x2F;a&gt;;&lt;&#x2F;li&gt;
&lt;li&gt;the conversion pipeline used for the tested Qwen3.6-35B-A3B
checkpoint. Fine-tunes and merged variants that preserve its exact
tensor structure should require no architectural change, but we have
not validated them. Dimensionally different hybrid-MoE models, such
as Qwen3.5-35B-A3B &lt;a href=&quot;#ref-qwen2026qwen35_35b&quot;&gt;[7]&lt;&#x2F;a&gt;,
Qwen3.5-122B-A10B &lt;a href=&quot;#ref-qwen2026qwen35_122b&quot;&gt;[8]&lt;&#x2F;a&gt;, and
Qwen3.5-397B-A17B &lt;a href=&quot;#ref-qwen2026qwen35_397b&quot;&gt;[9]&lt;&#x2F;a&gt;, require
explicit parameterization and testing;&lt;&#x2F;li&gt;
&lt;li&gt;Vulkan, CPU, and CUDA&#x2F;HIP kernels integrated into our public
llama.cpp branch &lt;a href=&quot;#ref-shlemov2026sign1&quot;&gt;[10]&lt;&#x2F;a&gt;. The optimized
Vulkan path reaches near-parity with llama.cpp’s Q2_K implementation
on our hardware;&lt;&#x2F;li&gt;
&lt;li&gt;benchmarks showing better model quality at approximately the same
size and speed as vanilla Q2_K.&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;The immediate motivation came from Antirez’s DS4. Through considerable
systems work, DS4 runs a DeepSeek-class MoE model on a 128 GB AMD Strix
Halo using a mixed IQ2_XXS&#x2F;Q2_K&#x2F;Q8 GGUF
&lt;a href=&quot;#ref-sanfilippo2026ds4&quot;&gt;[11]&lt;&#x2F;a&gt;. That result made the practical
question unavoidable: at a budget where every quantum of quality
matters, can structural binarization preserve more of the model than
aggressive scalar quantization? This led us to DBF, then to SIGN1 and
the complete DBDBD inference path described here.&lt;&#x2F;p&gt;
&lt;p&gt;Efficient kernels are hard, but a capable modern LLM makes a working
implementation possible in reasonable time. Correct and reasonably fast
is tractable; finished is an iterative community process. We provide the
representation contract, the optimized Vulkan implementation used on our
Radeon 8060S &#x2F; Ryzen AI Max+ 395 system &lt;a href=&quot;#ref-amd2026halo&quot;&gt;[12]&lt;&#x2F;a&gt;, and
working CPU and CUDA&#x2F;HIP starting points. This post, the public branch,
and the specification of another target should give a capable LLM enough
material to produce a useful first port.&lt;&#x2F;p&gt;
&lt;p&gt;We expect binary models to be useful beyond our poor Halo rig, but we
cannot verify every architecture ourselves. The most interesting next
directions are:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;Metal on Apple Silicon (the Mac Mini case);&lt;&#x2F;li&gt;
&lt;li&gt;a proper implementation and measurements on real NVIDIA CUDA
hardware (our Halo runs llama.cpp’s CUDA-source path through HIP,
but that path is slower than Vulkan on this machine);&lt;&#x2F;li&gt;
&lt;li&gt;ARM NEON (the Raspberry Pi case). Not because it is necessarily the
most practical target, but because it opens another part of the
design space.&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;h1 id=&quot;1-the-representation&quot;&gt;1. The representation&lt;&#x2F;h1&gt;
&lt;p&gt;A conventional dense linear layer computes&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;y = W x,
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;where &lt;code&gt;W ∈ R^(m×n)&lt;&#x2F;code&gt;. Start with the singular value decomposition. SVD
tells us that any real matrix can be represented exactly as a sum of
rank-one outer products, or equivalently as two factor matrices with
singular values between them. If we keep fewer components, the
representation becomes approximate; if we add components, it converges
to the original matrix.&lt;&#x2F;p&gt;
&lt;p&gt;Now impose one additional constraint: both factor matrices must contain
only &lt;code&gt;-1&lt;&#x2F;code&gt; and &lt;code&gt;+1&lt;&#x2F;code&gt;. Exact SVD factors do not generally satisfy that
constraint, so the decomposition becomes approximate. We can compensate
by using more binary components. The role of the intermediate dimension
&lt;code&gt;r&lt;&#x2F;code&gt; is therefore familiar from low-rank factorization: more components
provide more capacity, at a predictable storage cost. The representation
gains capacity as &lt;code&gt;r&lt;&#x2F;code&gt; grows, although unlike SVD, DBF does not produce
one nested, ordered sequence of exact prefixes.&lt;&#x2F;p&gt;
&lt;p&gt;DBF adds diagonal scalers around and between the binary factors. At the
representation level, their purpose is simple: binary matrices carry
signs and structure but very little magnitude information; the diagonals
restore input, intermediate, and output scales with little additional
storage. Understanding how the optimizer finds those scalers belongs to
the DBF paper rather than this implementation story.&lt;&#x2F;p&gt;
&lt;p&gt;DBF therefore approximates &lt;code&gt;W&lt;&#x2F;code&gt; as&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;W ≈ D_out · U · D_mid · V · D_in,
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;with&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;D_in  ∈ R^(n×n),       V ∈ {-1,+1}^(r×n),
D_mid ∈ R^(r×r),       U ∈ {-1,+1}^(m×r),
D_out ∈ R^(m×m).
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;All three &lt;code&gt;D&lt;&#x2F;code&gt; matrices are diagonal. &lt;code&gt;V&lt;&#x2F;code&gt; is the input-side binary factor
and &lt;code&gt;U&lt;&#x2F;code&gt; is the output-side binary factor, matching the tensor names used
by the implementation. The two binary matrices provide structural
capacity; the three real-valued diagonals recover magnitude information
that pure signs cannot carry.&lt;&#x2F;p&gt;
&lt;p&gt;A binary matrix needs one bit per value. We use&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;0 -&amp;gt; +1
1 -&amp;gt; -1
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;because it follows the sign-bit convention used by IEEE 754
floating-point formats: zero denotes a positive sign and one denotes a
negative sign. It does not make SIGN1 an IEEE 754 format; it merely
keeps the sign semantics unsurprising across serialization and kernels.&lt;&#x2F;p&gt;
&lt;p&gt;Multiplication by a binary weight is then a choice between &lt;code&gt;x&lt;&#x2F;code&gt; and &lt;code&gt;-x&lt;&#x2F;code&gt;.
The binary core needs sign extraction, conditional negation, and
accumulation. The diagonal factors still require real-valued
multiplication. Hence “almost” in the original paper’s title.&lt;&#x2F;p&gt;
&lt;p&gt;This representation is structurally different from ordinary scalar
quantization. Scalar formats approximate each weight locally and store
shared metadata per block. In uniform formats, the metadata is typically
a scale (and sometimes an offset or nested sub-block scales) applied to
fixed integer levels; in non-uniform formats, it may select or
parameterize a small codebook. DBF instead approximates the matrix as a
linear operator, using two learned binary maps and explicit diagonal
magnitude channels. It is not merely another way to pack two bits per
weight.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;1-1-bit-budget&quot;&gt;1.1 Bit budget&lt;&#x2F;h2&gt;
&lt;p&gt;The storage budget is transparent. Ignoring byte alignment for the
moment, the two binary factors require&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;r·n + m·r
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;bits. If the three diagonals are stored in FP16, they add&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;16·(n + r + m)
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;bits. Relative to the original &lt;code&gt;m·n&lt;&#x2F;code&gt; matrix elements, DBF therefore
costs&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;bpe_DBF = [r·(m+n) + 16·(m+n+r)] &#x2F; (m·n)
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;bits per original matrix element.&lt;&#x2F;p&gt;
&lt;p&gt;Qwen3.6-35B-A3B makes the arithmetic particularly clean. Every routed
gate and up projection has shape &lt;code&gt;512×2048&lt;&#x2F;code&gt;; every routed down
projection has the transposed shape &lt;code&gt;2048×512&lt;&#x2F;code&gt;
&lt;a href=&quot;#ref-qwen2026qwen36&quot;&gt;[5]&lt;&#x2F;a&gt;. Thus all 30,720 routed expert matrices (40
layers × 256 experts × 3 projections) have the same number of elements
and the same DBF storage cost.&lt;&#x2F;p&gt;
&lt;p&gt;For &lt;code&gt;r=1024&lt;&#x2F;code&gt;, one projection requires&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;binary factors: 512·1024 + 1024·2048       = 2,621,440 bits
FP16 diagonals: 16·(512 + 1024 + 2048)     =    57,344 bits
total:                                      2,678,784 bits
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Dividing by &lt;code&gt;512·2048&lt;&#x2F;code&gt; original elements gives exactly &lt;code&gt;2.5546875&lt;&#x2F;code&gt; bits
per element. Q2_K costs &lt;code&gt;2.625&lt;&#x2F;code&gt; bits per element, or &lt;code&gt;2,752,512&lt;&#x2F;code&gt; bits
for the same matrix. DBF is therefore 9,216 bytes smaller per
projection. Across all routed experts, the raw tensor payload is
&lt;code&gt;9.5801 GiB&lt;&#x2F;code&gt; for DBF versus &lt;code&gt;9.8438 GiB&lt;&#x2F;code&gt; for Q2_K, a 270 MiB margin
before container-level alignment and metadata.&lt;&#x2F;p&gt;
&lt;p&gt;So &lt;code&gt;r=1024&lt;&#x2F;code&gt; is not vague “approximate parity.” It is a slightly smaller
representation at the Q2_K budget. The intermediate dimension also gives
us a useful quality knob: changing &lt;code&gt;r&lt;&#x2F;code&gt; changes the storage budget in
predictable increments instead of forcing a choice among a few fixed
quantization formats.&lt;&#x2F;p&gt;
&lt;p&gt;The choice of 1024 has a second, mechanical advantage. It is a power of
two and divisible by the natural packing, SIMD, subgroup, and
matrix-tile widths used by current CPU and GPU hardware. That removes
padding and tail handling from the main contraction and makes
specialized kernels substantially cleaner.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;1-2-what-we-inherit-from-dbf&quot;&gt;1.2 What we inherit from DBF&lt;&#x2F;h2&gt;
&lt;p&gt;We start from the fixed-dimension factorization core described by Boža
and Macko &lt;a href=&quot;#ref-boza2026dbf&quot;&gt;[3]&lt;&#x2F;a&gt; and implemented in their public
Llama3 notebook at
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;usamec&#x2F;double_binary&#x2F;commit&#x2F;bc00edb&quot;&gt;&lt;code&gt;usamec&#x2F;double_binary@bc00edb&lt;&#x2F;code&gt;&lt;&#x2F;a&gt;.
The only refinement step we add is one final linear least-squares
reprojection of the middle diagonal for the fixed binary signs and outer
scalers; alternating projection can leave stale coefficient and rounding
error.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;DBF is not progressive like SVD.&lt;&#x2F;strong&gt; An SVD produces an ordered sequence
of components: compute it once, truncate it at different ranks, and
obtain a family of approximations. The core DBF factorization chooses
the intermediate dimension &lt;code&gt;r&lt;&#x2F;code&gt; before optimization and solves the
complete factorization at that dimension. A dimension-1024 result is not
a dimension-512 result with another 512 components appended.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Larger middle dimensions are harder to optimize well.&lt;&#x2F;strong&gt; In the paper’s
unweighted “Properties of DBF” experiment, DBF beats scalar quantization
from 1–3 bits, while scalar quantization wins at 4 bits and above. In
the accepted TMLR revision, Boža and Macko address this with size
annealing: they spend 80% of the iterations at a 2-bit middle dimension,
expand it gradually during the final 20%, and report slight improvements
at 3, 4, and 6 bits. They interpret the result as evidence that the
higher-bit limitation is algorithmic rather than fundamental to DBF
&lt;a href=&quot;#ref-boza2026dbf&quot;&gt;[3]&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;The annealing result was added during peer review in direct response to
a reviewer’s higher-bit concern. The accepted supplementary material and
linked public repository do not include its implementation. Since the
technique targets higher-bit overranking rather than the Q2-scale budget
used here, this work does not depend on it.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;The reference implementation is fast locally and expensive globally.&lt;&#x2F;strong&gt;
One expert matrix takes only seconds on our GPU. Qwen3.6-35B-A3B
contains 30,720 routed expert matrices. Even with parallel workers and
resumable checkpoints, a faithful 80-iteration whole-model decomposition
takes days. “Fast algorithm” and “fast model conversion” are different
claims when the model contains tens of thousands of independent
matrices.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Its practical strength is that it finds useful structure at extremely
low budgets.&lt;&#x2F;strong&gt; DBF jointly learns two binary maps and three magnitude
channels instead of quantizing weights independently. Around the Q2_K
storage regime, this gives substantially better matrix approximations
than scalar quantization on the tensors we tested. The method is
compact, reproducible, GPU-friendly, and sufficiently strong to justify
building the complete inference stack around it.&lt;&#x2F;p&gt;
&lt;p&gt;DBF is therefore the foundation of this implementation, not the final
algorithm for every rank and tensor. The representation may outlive this
particular optimizer.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;2-why-implement-it-in-llama-cpp&quot;&gt;2. Why implement it in llama.cpp?&lt;&#x2F;h1&gt;
&lt;p&gt;A Python reconstruction answers an important but narrow question: does
the factorization approximate an individual matrix? We wanted answers
about a usable model, in this order:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Can real people actually run and use it?&lt;&#x2F;strong&gt; If the model cannot be
loaded into an ordinary inference stack, the beauty of the
mathematics does not matter.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;What is its real quality?&lt;&#x2F;strong&gt; Does the matrix-level advantage
survive full-model perplexity, synthetic tests, and simply talking
to the model?&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;What is its real speed?&lt;&#x2F;strong&gt; Not an operation count or an isolated
microbenchmark, but end-to-end performance under real workloads on
real hardware.&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;Answering these questions requires the less glamorous machinery
underneath: serialization, model loading, graph construction, routed
execution, intermediate representations, backend kernels, and
correctness tests. llama.cpp is a useful battlefield because it already
contains mature quantization formats, backend abstractions, model
loaders, graph allocators, and benchmark tools. A new representation has
to coexist with all of them. There is nowhere for a missing dependency
or an accidental dense fallback to hide for long.&lt;&#x2F;p&gt;
&lt;p&gt;More importantly, llama.cpp is a de facto community standard for local
inference &lt;a href=&quot;#ref-ggml2026llamacpp&quot;&gt;[13]&lt;&#x2F;a&gt;. It supports a wide range of
model families, quantization formats, and hardware backends. A format
implemented only in our experimental runner would demonstrate
possibility. A format implemented in llama.cpp can be downloaded,
benchmarked, compared with existing quantizers, and extended by the
people who already maintain those backends.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;3-a-real-one-bit-tensor-type&quot;&gt;3. A real one-bit tensor type&lt;&#x2F;h1&gt;
&lt;p&gt;llama.cpp already has Q1_0, and our first prototype used it as a sign
carrier. We separated SIGN1 for three reasons. Q1_0 is a quantization
format with its own block scaling and execution contract; DBF already
carries its scales explicitly in &lt;code&gt;D_in&lt;&#x2F;code&gt;, &lt;code&gt;D_mid&lt;&#x2F;code&gt;, and &lt;code&gt;D_out&lt;&#x2F;code&gt;; and Q1_0
is evolving independently. Coupling DBF to it would make two unrelated
representations depend on each other’s storage and kernel decisions.&lt;&#x2F;p&gt;
&lt;p&gt;SIGN1 therefore stores exactly one thing: signs. Its primitive block
contains 64 signs in one &lt;code&gt;uint64_t&lt;&#x2F;code&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;Why 64?&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;It matches the natural storage width.&lt;&#x2F;li&gt;
&lt;li&gt;It is portable across CPU and GPU backends.&lt;&#x2F;li&gt;
&lt;li&gt;Wider SIMD kernels can combine several blocks internally.&lt;&#x2F;li&gt;
&lt;li&gt;The file format does not overfit one machine’s preferred vector width.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;The GGUF therefore contains actual &lt;code&gt;SIGN1&lt;&#x2F;code&gt; tensors, not Q1_0 used
informally and not a hidden dense reconstruction. The type is wired
through serialization, loading, backend dispatch, and correctness tests.
The implementation details are in the code. More realistically, your AI
will read the code for you =).&lt;&#x2F;p&gt;
&lt;p&gt;This model requires our SIGN1&#x2F;DBDBD llama.cpp branch; stock llama.cpp
does not understand the tensor type or the factorized Qwen graph.
Reusing Q1_0 as the sign carrier would not remove that requirement. The
runtime would still need the custom &lt;code&gt;D_in -&amp;gt; V -&amp;gt; D_mid -&amp;gt; U -&amp;gt; D_out&lt;&#x2F;code&gt;
graph, routed scaler handling, and architecture-specific kernel
dispatch. Q1_0 can disguise the storage type; it cannot make DBDBD a
stock model architecture.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;4-teaching-the-model-loader-dbdbd&quot;&gt;4. Teaching the model loader DBDBD&lt;&#x2F;h1&gt;
&lt;p&gt;For each routed expert projection, the model may contain five tensors:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;projection_din
projection_v
projection_dmid
projection_u
projection_dout
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Here &lt;code&gt;v&lt;&#x2F;code&gt; and &lt;code&gt;u&lt;&#x2F;code&gt; are packed &lt;code&gt;SIGN1&lt;&#x2F;code&gt; matrices. The diagonal tensors are
currently stored in FP16.&lt;&#x2F;p&gt;
&lt;p&gt;The graph computes, in order:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;                       packed SIGN1       packed SIGN1
x ── D_in ──────────────── V ── D_mid ─────── U ── D_out ── y

Optimized Vulkan implementation:
MMQ &#x2F; prompt processing: Q8 bitplanes
MMV &#x2F; token generation:  floating activations with mix()
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Equivalently:&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;x_scaled = D_in · x
h        = V · x_scaled
h_scaled = D_mid · h
y_core   = U · h_scaled
y        = D_out · y_core
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;In exact arithmetic, the factorized expression is simply a matrix
product. In finite arithmetic, matrix multiplication is not associative:
rounding and activation quantization make &lt;code&gt;U·(D_mid·(V·x))&lt;&#x2F;code&gt; numerically
different from multiplying &lt;code&gt;x&lt;&#x2F;code&gt; by a pre-reconstructed dense matrix. The
distinction becomes more important at very low precision because errors
enter at every intermediate boundary. We did not observe a material
quality problem from this effect in the routed FFN implementation, but
any new backend or activation format must validate the deployed chain,
not only the reconstructed matrix.&lt;&#x2F;p&gt;
&lt;p&gt;For a mixture-of-experts model, every selected &lt;code&gt;(token, slot)&lt;&#x2F;code&gt; route
also carries an expert identity and a route weight. That creates two
different input topologies:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;gate&#x2F;up first factors consume one hidden-state vector shared by
several selected experts;&lt;&#x2F;li&gt;
&lt;li&gt;down projections and second factors consume route-specific
intermediates already attached to one expert.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;Treating these as the same layout is correct algebraically and poor
mechanically. Much of the Vulkan work was teaching the runtime to
preserve those distinctions without changing the DBDBD function.&lt;&#x2F;p&gt;
&lt;p&gt;This is why prompt processing is much faster than single-token
generation in the table in Section 8. During prefill, one expert matrix
is applied to many vectors, so weight loads, routing setup, and
conversion can be amortized across columns. Generation usually applies
that matrix to one vector at a time. The same arithmetic therefore
belongs to two different kernel regimes: MMQ for many vectors and MMV
for one.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;5-vulkan-where-the-representation-became-practical&quot;&gt;5. Vulkan: where the representation became practical&lt;&#x2F;h1&gt;
&lt;p&gt;Vulkan is the backend we optimized seriously because it is the fastest
llama.cpp backend for this model and workload on our Strix Halo.&lt;&#x2F;p&gt;
&lt;p&gt;An efficient matrix multiplication kernel is far beyond the high-school
“row times column” algorithm. It requires specialized work partitioning,
memory layouts, reductions, and direct use of low-level hardware
instructions. The implementation catalogue below is primarily for
readers who are about to write or port a kernel; everyone else can
safely skip to the &lt;a href=&quot;https:&#x2F;&#x2F;eodus.github.io&#x2F;posts&#x2F;llm-binarization-episode-01-dbf&#x2F;#8-current-measurements&quot;&gt;measured result&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;We tried several ways to execute a binary dot product:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;DP4A:&lt;&#x2F;strong&gt; expand packed signs to integer &lt;code&gt;+1&#x2F;-1&lt;&#x2F;code&gt; values and use the
conventional four-way signed INT8 dot-product-with-INT32-accumulate
instruction &lt;a href=&quot;#ref-nvidia2026dp4a&quot;&gt;[14]&lt;&#x2F;a&gt; against Q8 activations.
This is the established BitNet-style GPU baseline. It is fast, but
the binary weights become ordinary integers before arithmetic and we
lose the “addition only” narrative. Our objection is not purely
ideological, although it is partially ideological =). We implemented
and measured DP4A; &lt;code&gt;mix()&lt;&#x2F;code&gt; and bitplanes were faster on our
hardware. That is a hardware-specific result, not an argument to
skip the most standard baseline on another GPU.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Conditional negation and addition:&lt;&#x2F;strong&gt; read a sign bit and add
either &lt;code&gt;x&lt;&#x2F;code&gt; or &lt;code&gt;-x&lt;&#x2F;code&gt; directly. This preserves the intended arithmetic
but relies on the compiler and hardware to implement sign selection
efficiently.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;GLSL &lt;code&gt;mix()&lt;&#x2F;code&gt;:&lt;&#x2F;strong&gt; use the Boolean-selector overload defined by GLSL,
so &lt;code&gt;mix(x, -x, sign)&lt;&#x2F;code&gt; selects component-wise between &lt;code&gt;x&lt;&#x2F;code&gt; and &lt;code&gt;-x&lt;&#x2F;code&gt;
&lt;a href=&quot;#ref-khronos2023glsl&quot;&gt;[15]&lt;&#x2F;a&gt;. Folklore says that such a high-level
conditional should be slow. On RADV and gfx1151 it works remarkably
well; measurement beats folklore.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Bitplanes:&lt;&#x2F;strong&gt; floating-point values carry separate exponents, while
one integer bitplane reduction needs a common scale. We therefore
&lt;em&gt;row-denormalize&lt;&#x2F;em&gt; the activation: choose one exponent for the
reduction row, encode one sign plane and seven magnitude planes,
combine activation and weight signs with XOR, and evaluate each
magnitude plane with masked popcounts and shifts.&lt;&#x2F;p&gt;
&lt;p&gt;The matched Vulkan Q2_K path already quantizes its input activations
to Q8_1. Our sign-magnitude Q8 bitplanes use a comparable precision
budget, but reorganize the activation for binary-weight arithmetic.
The resulting core is &lt;code&gt;XOR + AND + POPCOUNT + SHIFT + ADD&lt;&#x2F;code&gt;, followed
by one shared scale. Whether this representation can become a
general activation contract, rather than a backend-private encoding,
remains open.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Split rank:&lt;&#x2F;strong&gt; partition the intermediate dimension, compute
independent partial outputs, and sum them elementwise. We tested
explicit reductions and atomic accumulation. Atomics were much less
disastrous than generic GPU folklore predicts, although they did not
become the final path.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Precomputed signed sums:&lt;&#x2F;strong&gt; for each activation quartet
&lt;code&gt;(x1,x2,x3,x4)&lt;&#x2F;code&gt;, precompute the possible &lt;code&gt;±x1±x2±x3±x4&lt;&#x2F;code&gt; combinations
and select one using the four packed weight bits. This replaces
repeated sign application with a small lookup. On Halo, dynamic
indexing and register pressure consumed the arithmetic saving, but
the balance may differ on hardware with native table-lookup
instructions.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Weight repacking:&lt;&#x2F;strong&gt; the tensor format in the model file does not
have to be the format consumed by the kernel. GGUF stores one
canonical portable SIGN1 layout; a backend may repack it once during
model loading into the exact row groups and K-chunks consumed by its
lanes. This is effectively free preprocessing when the model is
loaded once and used many times. The backend API must still return
canonical bytes when a tensor is read back; violating that boundary
gave us a particularly educational false CPU reference. Repacking
simplified addressing consistently, although end-to-end gains were
often marginal because canonical adjacent rows were already
cache-friendly.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Input repacking:&lt;&#x2F;strong&gt; activations have the same design freedom, but
they change every evaluation and therefore must repay conversion
cost. Globally, route-specific vectors can be compacted into
expert-major order so one expert kernel consumes contiguous inputs;
genuinely shared gate&#x2F;up inputs should remain shared rather than be
duplicated eight times. Locally, values can be arranged in the exact
words, bytes, or sign&#x2F;magnitude planes consumed by each lane. Both
levels matter: global order controls routing locality, while local
order controls coalescing and instruction shape.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;LDS and register caching:&lt;&#x2F;strong&gt; stage activation or weight tiles in
shared memory, then optionally retain hot fragments in registers.
Some caches were load-bearing; others duplicated work that the
hardware cache already handled and made performance worse through
barriers or occupancy loss. “Cache it” is not an optimization until
the complete kernel proves it.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Scheduling and fusion:&lt;&#x2F;strong&gt; we tested compact expert queues, skipped
empty tiles, persistent workgroups, gate&#x2F;up overlap, direct V-to-U
carriers, and fusion of adjacent diagonal and SwiGLU operations. The
largest gains often came from removing null work or an intermediate
boundary rather than changing the inner dot product.&lt;&#x2F;p&gt;
&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;Another folklore claim is that inference is entirely memory-bound, so
memory traffic is everything and compute is free. Just no. Our kernels
often moved essentially the same bytes and still differed materially
because of integer instruction count, dependency chains, subgroup
reductions, barriers, register pressure, and exposed workgroup
parallelism. Conversely, reducing activation precision removed
arithmetic and traffic but sometimes changed almost nothing end to end
because that part of the kernel was not the bottleneck. Count the bytes,
but also inspect the instructions and measure the complete schedule.&lt;&#x2F;p&gt;
&lt;p&gt;We ran substantially more variants than we can present properly in one
post. The intermediate sources, measurements, and rejected branches are
preserved, but turning every experiment into publication-quality prose
would become a second full-time job. If you are implementing one of
these paths and need the underlying result, contact us. We are happy to
share the relevant artifacts and discuss what we observed.&lt;&#x2F;p&gt;
&lt;p&gt;The final implementation keeps two different winners. Prompt processing
(MMQ) uses bitplanes; single-token matrix-vector multiplication uses
&lt;code&gt;mix()&lt;&#x2F;code&gt;. There is no reason to force one arithmetic formulation onto
workloads with different shapes and parallelism.&lt;&#x2F;p&gt;
&lt;p&gt;The measured result is narrower and more useful than the slogan: DBDBD
evaluates &lt;strong&gt;two&lt;&#x2F;strong&gt; binary matrix products plus three explicit diagonal
scalers in approximately the time Q2_K evaluates &lt;strong&gt;one&lt;&#x2F;strong&gt; quantized
matrix product. The comparison includes the complete model path, not an
operation-count thought experiment. DBDBD performs more logical stages
and still reaches near-parity because its binary cores replace general
weight multiplication with sign selection, integer addition, or popcount
arithmetic. The scalers are not free; they are included in the measured
result.&lt;&#x2F;p&gt;
&lt;p&gt;Performance depended as much on execution &lt;em&gt;regimes&lt;&#x2F;em&gt; as on arithmetic:
how many rows and columns one workgroup owns, how many workgroups are
exposed to the scheduler, which intermediates use expert-major order,
and where conversion or scaling is fused. The retained implementation
uses one-pass row encoding, SWAR bit-matrix transpose, expert-major
route-specific layouts, shared routing plans, adaptive tail skipping,
and shape-specific rows2&#x2F;rows4&#x2F;rows8 generation kernels.&lt;&#x2F;p&gt;
&lt;p&gt;If you optimize SIGN1 for another architecture, try all the formulations
rather than porting our winner blindly. The final race on Halo was
close, and small changes in work ownership repeatedly reversed
apparently obvious conclusions. In particular, test several
row&#x2F;column&#x2F;workgroup regimes.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;6-cpu-a-portable-baseline&quot;&gt;6. CPU: a portable baseline&lt;&#x2F;h1&gt;
&lt;p&gt;The CPU path quantizes activations to Q8, applies packed signs during
the dot product, and uses AVX2 or AVX-512 when available. The AVX-512
prompt path repacks eight output rows together and evaluates several
input vectors per call; AVX2 provides the narrower vector path, with a
scalar implementation as the correctness fallback. It is functional and
tested, but not the performance focus here.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;7-cuda-hip-a-starting-point-not-a-polished-backend&quot;&gt;7. CUDA&#x2F;HIP: a starting point, not a polished backend&lt;&#x2F;h1&gt;
&lt;p&gt;We wired SIGN1 and routed DBF scalers through llama.cpp’s CUDA-source
MMV&#x2F;MMQ implementation and tested that code through HIP on our AMD
machine. The full model runs correctly, although HIP currently retains a
bounded stream synchronization fallback. We did not optimize this
backend seriously, and we have no suitable NVIDIA hardware on which to
validate native CUDA performance.&lt;&#x2F;p&gt;
&lt;p&gt;There is a concrete reason to expect more from native CUDA. In the
ordinary portable compute model used by our Vulkan backend,
synchronization is local to a subgroup or workgroup. A dependency across
the complete dispatch requires ending the dispatch and synchronizing
before another one &lt;a href=&quot;#ref-khronos2026vulkansync&quot;&gt;[16]&lt;&#x2F;a&gt;. The pipeline is
therefore close to low-level map-reduce: launch homogeneous work,
materialize the result, synchronize, then launch the consumer. Vulkan
can dispatch different pipelines, but ordinary indirect dispatch changes
the grid dimensions, not the bound shader itself.&lt;&#x2F;p&gt;
&lt;p&gt;CUDA exposes additional execution mechanisms: cooperative groups can
synchronize a cooperatively launched grid, and dynamic parallelism
permits device-side kernel launches &lt;a href=&quot;#ref-nvidia2026cudaguide&quot;&gt;[17]&lt;&#x2F;a&gt;.
We have not implemented the following designs, but two directions look
especially promising:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Count-specialized MoE kernels.&lt;&#x2F;strong&gt; The number of vectors routed to
each expert is known only at runtime. Instead of one kernel padded
to the largest tile, dispatch specialized 16-, 8-, 4-, 2-, and 1-row
kernels according to the observed expert counts. Vulkan can
approximate this with several dispatches and queues; CUDA offers a
more natural GPU-driven implementation.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;A joint V–D_mid–U kernel.&lt;&#x2F;strong&gt; Compute &lt;code&gt;V·x&lt;&#x2F;code&gt;, apply &lt;code&gt;D_mid&lt;&#x2F;code&gt;,
synchronize the grid, then execute &lt;code&gt;U·h&lt;&#x2F;code&gt; without returning control
to the host-side graph between phases. A global barrier does not
magically eliminate intermediate storage, but it can remove dispatch
boundaries and permit a persistent work partition that keeps some
data local.&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;These are hypotheses, not CUDA performance claims. The Vulkan
implementation establishes the baseline; a native CUDA port has a richer
synchronization model with which to challenge it.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;8-current-measurements&quot;&gt;8. Current measurements&lt;&#x2F;h1&gt;
&lt;p&gt;The final Vulkan comparison uses the faithful DBF weights and the same
tensor shapes, storage layout, and nominal operation count as the
earlier runtime controls. We nevertheless reran speed with the exact
release model because changed hidden states can alter routing and the
realized workload.&lt;&#x2F;p&gt;
&lt;p&gt;The matched Vulkan result used drift-balanced Q2_K → DBDBD → DBDBD →
Q2_K order, identical non-expert model tensors, and the same command
line. The FP16-expert control was measured separately with that binary
and command shape; it is shown for scale, not included in the ABBA
contrast:&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Claim&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;Original FP16 experts&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;Q2_K&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;DBF&#x2F;SIGN1&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;DBF vs Q2_K&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;Vulkan prompt processing, final DBDBD model, pp512&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;681.31&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;1217.95&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;1198.09&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;-1.63%&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Vulkan token generation, final DBDBD model, tg128&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;54.71&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;82.52&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;82.14&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;-0.46%&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Full WikiText-2 perplexity, final DBDBD model&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;6.9935&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;7.7823&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;7.3186&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;58.8% of Q2_K damage removed&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;All three quality models share the same non-expert remainder and differ
only in the routed expert representation. That remainder is copied from
Unsloth’s &lt;code&gt;Qwen3.6-35B-A3B-UD-Q2_K_XL&lt;&#x2F;code&gt;
&lt;a href=&quot;#ref-unsloth_qwen36_gguf&quot;&gt;[6]&lt;&#x2F;a&gt;. The raw PPL reduction is less
informative than the recovered gap to the original FP16-expert model.
Q2_K adds &lt;code&gt;7.7823 - 6.9935 = 0.7888&lt;&#x2F;code&gt; PPL; DBF leaves only
&lt;code&gt;7.3186 - 6.9935 = 0.3251&lt;&#x2F;code&gt;. Thus DBF removes &lt;strong&gt;58.8%&lt;&#x2F;strong&gt; of Q2_K’s
additive PPL damage. On the underlying mean-NLL scale, it removes
&lt;strong&gt;57.5%&lt;&#x2F;strong&gt; of the Q2_K-to-FP16 gap. DBF wins 526 of 580 paired chunks;
Q2_K wins 54, with no ties. The exact two-sided sign test on per-chunk
NLL gives &lt;code&gt;p = 3.21e-98&lt;&#x2F;code&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;Matched Q3_K and Q4_K controls reach &lt;code&gt;7.2279&lt;&#x2F;code&gt; and &lt;code&gt;7.0557&lt;&#x2F;code&gt; PPL
respectively. DBF therefore recovers &lt;strong&gt;83.1%&lt;&#x2F;strong&gt; of the Q2_K-to-Q3_K
improvement on the NLL scale. Interpolating locally between Q2_K
(&lt;code&gt;2.625 bpe&lt;&#x2F;code&gt;) and Q3_K (&lt;code&gt;3.4375 bpe&lt;&#x2F;code&gt;) places DBF at approximately
&lt;strong&gt;3.30-bpe scalar-equivalent quality&lt;&#x2F;strong&gt;, while its actual routed-expert
payload is &lt;strong&gt;2.555 bpe&lt;&#x2F;strong&gt;. This is an empirical interpolation for this
model and dataset, not a universal quantization scale.&lt;&#x2F;p&gt;
&lt;p&gt;The matrix-level result is equally uniform across all 30,720 matched
pairs:&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Matrix approximation metric&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;Q2_K&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;DBF&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;Mean relative improvement&lt;&#x2F;th&gt;&lt;th style=&quot;text-align: right&quot;&gt;DBF wins&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;Mean relative Frobenius residual&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;0.29938&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;0.24184&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;19.10%&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;30,720 &#x2F; 30,720&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;Mean relative spectral residual&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;0.20146&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;0.15589&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;22.52%&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;30,720 &#x2F; 30,720&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;We include the spectral metric for a deliberately speculative reason.
Let &lt;code&gt;E = W - W_approx&lt;&#x2F;code&gt;. Its spectral norm is exactly the largest output
error &lt;code&gt;||E x||_2&lt;&#x2F;code&gt; over unit-norm inputs, so it measures the worst-case
operator error. The Frobenius norm is not merely a naive coefficient
metric: it aggregates all coefficient errors and, for an isotropic
random input, determines the expected squared output error. Real model
activations are not isotropic or adversarial, so neither metric is
automatically the right proxy; an activation-covariance-weighted error
may be more relevant than either. Spectral error may matter especially
when one narrow, sensitive direction dominates, but whether it predicts
PPL better than Frobenius error remains an empirical question. Neither
DBF nor our current reproduction optimizes it. More importantly, both
unweighted metrics ignore the activation distribution.&lt;&#x2F;p&gt;
&lt;p&gt;Each matrix is normalized by its own norm before aggregation, so large
matrices or high-energy experts cannot silently dominate the average. We
report both the mean improvement and the number of matrices on which DBF
wins. Spectral norms use fixed-seed 32-iteration power iteration. The
full model-level PPL then tests whether those approximation gains
survive inference.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;9-open-questions-and-the-next-episodes&quot;&gt;9. Open questions and the next episodes&lt;&#x2F;h1&gt;
&lt;h2 id=&quot;9-1-dbf-is-reported-to-struggle-at-larger-bit-budgets-can-that-regime-be-repaired&quot;&gt;9.1 DBF is reported to struggle at larger bit budgets. Can that regime be repaired?&lt;&#x2F;h2&gt;
&lt;p&gt;The original method is especially strong at very low budgets. As the
intermediate dimension grows, the alternating problem becomes
increasingly overcomplete and difficult to optimize. Is that a
fundamental limitation of the representation, or a limitation of the
continuation&#x2F;projection algorithm?&lt;&#x2F;p&gt;
&lt;h2 id=&quot;9-2-can-one-structural-method-cover-all-important-model-matrices&quot;&gt;9.2 Can one structural method cover all important model matrices?&lt;&#x2F;h2&gt;
&lt;p&gt;Routed FFN experts are only part of an LLM. Attention projections,
shared experts, routers, embeddings, and the language-model head have
different shapes and sensitivities. Can we move from “binary experts” to
a model that is genuinely binary in the meaningful weight-storage sense?&lt;&#x2F;p&gt;
&lt;h2 id=&quot;9-3-can-dbf-acquire-discrete-activations-and-become-bitnet-in-the-literal-sense&quot;&gt;9.3 Can DBF acquire discrete activations and become “BitNet” in the literal sense?&lt;&#x2F;h2&gt;
&lt;p&gt;Classical binary-network work, including BitNet-style systems, combines
discrete weights with discrete or highly constrained activations. Our
current implementation has a binary weight core but still enters and
leaves through floating-point values and floating diagonal scales.&lt;&#x2F;p&gt;
&lt;p&gt;Can shared-exponent bitplanes become the natural activation contract
rather than a backend-private optimization? Can the complete linear core
operate on packed bits and integer accumulation while preserving model
quality?&lt;&#x2F;p&gt;
&lt;h2 id=&quot;9-4-which-matrix-error-should-we-optimize&quot;&gt;9.4 Which matrix error should we optimize?&lt;&#x2F;h2&gt;
&lt;p&gt;The practical answer may be: neither ordinary Frobenius nor ordinary
spectral norm. Inference does not feed the matrix isotropic random
vectors or worst-case adversarial vectors; it feeds activations drawn
from the model’s actual workload. An importance matrix provides
empirical input-channel weights from calibration data, letting the
decomposition optimize errors that the model is likely to exercise
rather than treating every direction equally.&lt;&#x2F;p&gt;
&lt;p&gt;DBF already supports row and column weighting naturally. Integrating
importance-matrix norms into our factorization pipeline is therefore
mainly a calibrated rerun, not a new representation. We excluded it from
Episode One because we wanted to cut it.&lt;&#x2F;p&gt;
&lt;p&gt;A later episode will compare four views of the same approximation error:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;ordinary Frobenius norm;&lt;&#x2F;li&gt;
&lt;li&gt;ordinary spectral norm;&lt;&#x2F;li&gt;
&lt;li&gt;importance-weighted Frobenius norm;&lt;&#x2F;li&gt;
&lt;li&gt;importance-weighted spectral norm.&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;The final judge remains model-level PPL and KLD. The useful question is
not which norm has the most attractive interpretation, but which
objective produces the best deployed model at the same storage and
runtime budget.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;9-5-how-much-evaluation-is-enough&quot;&gt;9.5 How much evaluation is enough?&lt;&#x2F;h2&gt;
&lt;p&gt;This post uses one full WikiText-2 perplexity run as the first
model-level quality gate. That is enough for a pilot comparison, not a
complete capability evaluation. A later performance post should widen
the test in three directions:&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;More perplexity datasets.&lt;&#x2F;strong&gt; Repeat the paired evaluation on
corpora with different styles and distributions, including C4 and
Penn Treebank rather than trusting one WikiText-2 result.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Logit divergence.&lt;&#x2F;strong&gt; Measure token-level KLD against the original
FP16-expert model, with matched prompts and causal positions. PPL
sees only the probability assigned to the observed next token; KLD
sees how the complete predicted distribution moved.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Functional benchmarks.&lt;&#x2F;strong&gt; Run a compact standard suite through the
Language Model Evaluation Harness &lt;a href=&quot;#ref-gao2024lmeval&quot;&gt;[18]&lt;&#x2F;a&gt;,
using the exact same prompts, tokenizer, harness revision, and
scoring path for every model. Representative tasks include LAMBADA,
HellaSwag, ARC Challenge, Winogrande, PIQA, BoolQ, MMLU, and GSM8K;
the final subset should cover several capabilities without turning
one quantization comparison into a leaderboard.&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;The purpose is not to manufacture one aggregate score. It is to find
whether the representation changes only average language modeling loss
or damages a particular capability that PPL hides.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;9-6-does-the-result-transfer-to-more-interesting-models&quot;&gt;9.6 Does the result transfer to more interesting models?&lt;&#x2F;h2&gt;
&lt;p&gt;Qwen3.6-35B-A3B is a practical controlled battlefield, not the final
target. The next useful test is whether the same representation and
kernel ideas survive larger expert dimensions, different routing
distributions, and newer frontier MoE architectures.&lt;&#x2F;p&gt;
&lt;p&gt;The most practical next port is &lt;strong&gt;GLM-4.5-Air&lt;&#x2F;strong&gt;: 106B total &#x2F; 12B active
parameters, 46 layers, 128 routed experts, a 4096-wide hidden state, and
1408-wide expert intermediates &lt;a href=&quot;#ref-glm45team2025glm45air&quot;&gt;[19]&lt;&#x2F;a&gt;. It
fits comfortably on one 128 GB Strix Halo after ordinary low-bit
quantization, while its expert geometry and routing differ enough from
Qwen to make the transfer meaningful.&lt;&#x2F;p&gt;
&lt;p&gt;Two larger targets are &lt;strong&gt;MiniMax-M2.5&lt;&#x2F;strong&gt; and &lt;strong&gt;DeepSeek-V4-Flash&lt;&#x2F;strong&gt;.
MiniMax-M2.5 is a 230B &#x2F; 10B-active MoE with 62 layers, 256 experts,
3072-wide hidden states, and 1536-wide expert intermediates
&lt;a href=&quot;#ref-minimaxai2026minimaxm25&quot;&gt;[20]&lt;&#x2F;a&gt;. DeepSeek-V4-Flash is a 284B &#x2F;
13B-active MoE with 43 layers, 256 routed experts, 4096-wide hidden
states, 2048-wide MoE intermediates, and one-million-token context
&lt;a href=&quot;#ref-deepseekai2026deepseekv4flash&quot;&gt;[21]&lt;&#x2F;a&gt;. Both are close enough to
the one-Halo memory boundary that a Q2-scale structural representation
is operationally valuable rather than merely a matrix experiment.&lt;&#x2F;p&gt;
&lt;p&gt;GLM-5.2 is a less practical immediate target. Its 78-layer, 6144×2048,
256-expert geometry implies hundreds of billions of expert parameters;
even a Q2-scale expert payload alone exceeds one Halo’s memory. It
belongs to a multi-node follow-up, not the first transfer test.&lt;&#x2F;p&gt;
&lt;p&gt;These are not promised ports. They are falsification targets. A
representation that wins only on one convenient Qwen geometry is a
useful kernel experiment; one that transfers across Qwen, GLM, MiniMax,
and DeepSeek begins to look like a general post-training binary format.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;10-reproducibility&quot;&gt;10. Reproducibility&lt;&#x2F;h1&gt;
&lt;p&gt;The public release includes:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;the minimal rebased llama.cpp branch;&lt;&#x2F;li&gt;
&lt;li&gt;build instructions for CPU, CUDA&#x2F;HIP, and Vulkan;&lt;&#x2F;li&gt;
&lt;li&gt;the &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;eodus&#x2F;dbf-sign1&quot;&gt;model-conversion and analysis
pipeline&lt;&#x2F;a&gt;;&lt;&#x2F;li&gt;
&lt;li&gt;exact tensor and bit accounting;&lt;&#x2F;li&gt;
&lt;li&gt;backend correctness commands;&lt;&#x2F;li&gt;
&lt;li&gt;paired full-perplexity scripts;&lt;&#x2F;li&gt;
&lt;li&gt;benchmark commands and hardware details.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;The &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;eodus&#x2F;Qwen3.6-35B-A3B-DBF-SIGN1&quot;&gt;release
model&lt;&#x2F;a&gt; is
available on Hugging Face. Every non-routed-expert tensor in it is
copied unchanged from Unsloth’s &lt;code&gt;Qwen3.6-35B-A3B-UD-Q2_K_XL&lt;&#x2F;code&gt; GGUF
&lt;a href=&quot;#ref-unsloth_qwen36_gguf&quot;&gt;[6]&lt;&#x2F;a&gt;. The comparison keeps every
non-routed-expert tensor identical, replaces the same routed expert
matrices in both models, and accounts for their exact raw tensor
payload. Quality is measured over all 580 WikiText-2 chunks using paired
per-chunk NLL and an exact sign test, not only the aggregate perplexity.
The release record identifies the model and code revisions used for the
table and provides the exact commands. The GGUF SHA-256 is
&lt;code&gt;d4fa165f0bf4752ebd86c8aa5285401b6318e48787d0912d696f05f80b8071ac&lt;&#x2F;code&gt;; the
validated llama.cpp release commit is
&lt;code&gt;753bd8614d43a03b00583c0e51a9b4fca75f9ab4&lt;&#x2F;code&gt;. These artifacts establish
the authors’ fixed-dimension DBF core, our middle-diagonal reprojection,
and our runtime together.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;AI collaboration disclosure.&lt;&#x2F;strong&gt; This work was developed by Sasha
Shlemov with Drinkins, his calibrated personal AI research and
engineering assistant. Drinkins contributed implementation, experiment
design, debugging, analysis, source research, and editing across several
model engines connected through shared memory and persistent artifacts.
Sasha directed the research, made the final technical and publication
decisions, and takes responsibility for the claims. AI assistance is not
incidental here: doing this amount of AI implementation,
experimentation, and source analysis without AI would be a strange
methodological choice.&lt;&#x2F;p&gt;
&lt;p&gt;DBF is not a new miracle, but it is a working machine built from an idea
worth taking seriously.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;references&quot;&gt;References&lt;&#x2F;h1&gt;
&lt;div id=&quot;refs&quot; class=&quot;references csl-bib-body&quot; data-entry-spacing=&quot;0&quot;&gt;
&lt;div id=&quot;ref-wang2023bitnet&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[1]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;H. Wang &lt;em&gt;et al.&lt;&#x2F;em&gt;, “BitNet: Scaling
1-bit transformers for large language models,” 2023, Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2310.11453&quot;&gt;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2310.11453&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-ma2024bitnet158&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[2]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;S. Ma &lt;em&gt;et al.&lt;&#x2F;em&gt;, “The era of 1-bit
LLMs: All large language models are in 1.58 bits,” 2024, Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2402.17764&quot;&gt;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2402.17764&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-boza2026dbf&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[3]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;V. Boža and V. Macko, “Addition is
almost all you need: Compressing large language models with double
binary factorization,” &lt;em&gt;Transactions on Machine Learning Research&lt;&#x2F;em&gt;,
2026, Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;openreview.net&#x2F;forum?id=k5kUKoewdQ&quot;&gt;https:&#x2F;&#x2F;openreview.net&#x2F;forum?id=k5kUKoewdQ&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-wang2025bitnetcpp&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[4]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;J. Wang &lt;em&gt;et al.&lt;&#x2F;em&gt;, “Bitnet.cpp:
Efficient edge inference for ternary LLMs,” in &lt;em&gt;Proceedings of the 63rd
annual meeting of the association for computational linguistics&lt;&#x2F;em&gt;, 2025.
Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2502.11880&quot;&gt;https:&#x2F;&#x2F;arxiv.org&#x2F;abs&#x2F;2502.11880&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-qwen2026qwen36&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[5]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Qwen Team, “Qwen3.6-35B-A3B:
Agentic coding power, now open to all.” Apr. 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.6-35B-A3B&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.6-35B-A3B&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-unsloth_qwen36_gguf&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[6]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Unsloth, “Qwen3.6-35B-A3B-GGUF
(dynamic 2.0 quantizations).” 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;unsloth&#x2F;Qwen3.6-35B-A3B-GGUF&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;unsloth&#x2F;Qwen3.6-35B-A3B-GGUF&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-qwen2026qwen35_35b&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[7]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Qwen Team, “Qwen3.5-35B-A3B.”
2026. Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.5-35B-A3B&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.5-35B-A3B&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-qwen2026qwen35_122b&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[8]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Qwen Team, “Qwen3.5-122B-A10B.”
2026. Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.5-122B-A10B&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.5-122B-A10B&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-qwen2026qwen35_397b&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[9]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Qwen Team, “Qwen3.5-397B-A17B.”
2026. Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.5-397B-A17B&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;Qwen&#x2F;Qwen3.5-397B-A17B&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-shlemov2026sign1&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[10]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;S. Shlemov and Drinkins,
&lt;em&gt;&lt;span class=&quot;nocase&quot;&gt;SIGN1&#x2F;DBDBD Support for llama.cpp&lt;&#x2F;span&gt;&lt;&#x2F;em&gt;. (2026).
Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;eodus&#x2F;sign1-llama.cpp&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;eodus&#x2F;sign1-llama.cpp&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-sanfilippo2026ds4&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[11]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;S. Sanfilippo,
&lt;em&gt;&lt;span class=&quot;nocase&quot;&gt;DS4 on Strix Halo&lt;&#x2F;span&gt;&lt;&#x2F;em&gt;. (2026). Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;antirez&#x2F;ds4&#x2F;blob&#x2F;main&#x2F;STRIXHALO.md&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;antirez&#x2F;ds4&#x2F;blob&#x2F;main&#x2F;STRIXHALO.md&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-amd2026halo&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[12]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Advanced Micro Devices, Inc., “AMD
Ryzen AI Halo.” 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;www.amd.com&#x2F;en&#x2F;products&#x2F;processors&#x2F;desktops&#x2F;ryzen&#x2F;ryzen-ai-halo.html&quot;&gt;https:&#x2F;&#x2F;www.amd.com&#x2F;en&#x2F;products&#x2F;processors&#x2F;desktops&#x2F;ryzen&#x2F;ryzen-ai-halo.html&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-ggml2026llamacpp&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[13]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;&lt;span class=&quot;nocase&quot;&gt;ggml-org and
contributors&lt;&#x2F;span&gt;, &lt;em&gt;&lt;span class=&quot;nocase&quot;&gt;llama.cpp&lt;&#x2F;span&gt;&lt;&#x2F;em&gt;. (2026).
Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.com&#x2F;ggml-org&#x2F;llama.cpp&quot;&gt;https:&#x2F;&#x2F;github.com&#x2F;ggml-org&#x2F;llama.cpp&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-nvidia2026dp4a&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[14]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;NVIDIA, &lt;em&gt;CUDA Math API: Integer
Intrinsics&lt;&#x2F;em&gt;. NVIDIA Corporation, 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;docs.nvidia.com&#x2F;cuda&#x2F;cuda-math-api&#x2F;cuda_math_api&#x2F;group__CUDA__MATH__INTRINSIC__INT.html&quot;&gt;https:&#x2F;&#x2F;docs.nvidia.com&#x2F;cuda&#x2F;cuda-math-api&#x2F;cuda_math_api&#x2F;group__CUDA__MATH__INTRINSIC__INT.html&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-khronos2023glsl&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[15]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Khronos Group, &lt;em&gt;The OpenGL shading
language, version 4.60.8&lt;&#x2F;em&gt;. Khronos Group, 2023. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;registry.khronos.org&#x2F;OpenGL-Refpages&#x2F;gl4&#x2F;html&#x2F;mix.xhtml&quot;&gt;https:&#x2F;&#x2F;registry.khronos.org&#x2F;OpenGL-Refpages&#x2F;gl4&#x2F;html&#x2F;mix.xhtml&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-khronos2026vulkansync&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[16]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;Khronos Group, &lt;em&gt;Vulkan Guide:
Synchronization Examples&lt;&#x2F;em&gt;. Khronos Group, 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;github.khronos.org&#x2F;Vulkan-Site&#x2F;guide&#x2F;latest&#x2F;synchronization_examples.html&quot;&gt;https:&#x2F;&#x2F;github.khronos.org&#x2F;Vulkan-Site&#x2F;guide&#x2F;latest&#x2F;synchronization_examples.html&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-nvidia2026cudaguide&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[17]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;NVIDIA, &lt;em&gt;CUDA C++ Programming
Guide&lt;&#x2F;em&gt;. NVIDIA Corporation, 2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;docs.nvidia.com&#x2F;cuda&#x2F;cuda-c-programming-guide&#x2F;&quot;&gt;https:&#x2F;&#x2F;docs.nvidia.com&#x2F;cuda&#x2F;cuda-c-programming-guide&#x2F;&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-gao2024lmeval&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[18]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;L. Gao &lt;em&gt;et al.&lt;&#x2F;em&gt;, “The language
model evaluation harness.” Zenodo, Jul. 2024. doi:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;doi.org&#x2F;10.5281&#x2F;zenodo.12608602&quot;&gt;10.5281&#x2F;zenodo.12608602&lt;&#x2F;a&gt;.&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-glm45team2025glm45air&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[19]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;GLM-4.5 Team and Z.ai,
“GLM-4.5-Air.” 2025. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;zai-org&#x2F;GLM-4.5-Air&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;zai-org&#x2F;GLM-4.5-Air&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-minimaxai2026minimaxm25&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[20]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;MiniMax-AI, “MiniMax-M2.5.” 2026.
Available: &lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;MiniMaxAI&#x2F;MiniMax-M2.5&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;MiniMaxAI&#x2F;MiniMax-M2.5&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div id=&quot;ref-deepseekai2026deepseekv4flash&quot; class=&quot;csl-entry&quot;&gt;
&lt;p&gt;&lt;span class=&quot;csl-left-margin&quot;&gt;[21]
&lt;&#x2F;span&gt;&lt;span class=&quot;csl-right-inline&quot;&gt;DeepSeek-AI, “DeepSeek-V4-Flash.”
2026. Available:
&lt;a rel=&quot;external&quot; href=&quot;https:&#x2F;&#x2F;huggingface.co&#x2F;deepseek-ai&#x2F;DeepSeek-V4-Flash&quot;&gt;https:&#x2F;&#x2F;huggingface.co&#x2F;deepseek-ai&#x2F;DeepSeek-V4-Flash&lt;&#x2F;a&gt;&lt;&#x2F;span&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;&#x2F;div&gt;
</content>
        
    </entry>
</feed>
